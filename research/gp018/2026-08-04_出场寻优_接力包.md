# 接力包：GUPIAO_018 出场参数寻优（2026-08-04 中午）

> **【已完成 2026-08-05 凌晨】终审九宫格全齐，结论：不交付（行情依赖）。**
> 详见 `research/gp018/2026-08-04_出场寻优_终审结论.md`。
> 下一步方向：给 champ3 加行情开关（regime filter）后重跑终审。

> 用途：新窗口/新会话继续当前工作。读完这个文件就知道：跑到哪了、结果在哪、下一步干什么。

## 一、现在在干什么

给捉妖公式（GUPIAO_018）找**止盈止损最优组合**，全部用**真 5 分钟数据**（日线口径数字不可信，已多次验证虚高 3-5 倍）。
调参窗口 = 2021-2023，终审窗口 = 2015-2020 和 2024-2026.7（终审不用调参窗口，防过拟合）。

## 二、进度（截至交接时）

- **第一轮（骨架轮）16 组**：硬止损{-6/-8/-12/关} × 移动止盈{关/3.5%-1%/6%-2%/8%-3%}，阶梯关、时间 12 天。
  **已完成 4/16**，两个进程在跑（会陆续出结果到 `research/gp018/exitopt/grid.csv`）。
- 已出结果（年化，真5m）：c6+关移动 -12.3% / c6+6%-2% -10.6% / **c6+3.5%-1% -0.08%（目前最好）** / c8+关移动 -13.1%。
  早期规律：关移动止盈全灭；-6% 止损太紧；重点等 -12% 止损的几组。
- **第二轮（血肉轮）未开始**：骨架前3 × 阶梯{关/5%·10%各50%/6%·15%各30%/一档+8%全卖} × 时间{12,20}。
- **终审未开始**：总排名前3 → 2015-2020 + 2024-2026.8 真 5m 验证。

## 三、关键文件（都在 E:/1target/VERA）

- 寻优工具：`research/gp018/exit_opt.py`（组合生成+断点+CSV汇总，已完成组合自动跳过）
- 结果：`research/gp018/exitopt/grid.csv`（每组一行：组合名/累计/年化/回撤/夏普/交易数/胜率/盈亏比）
- 信号：`research/gp018/results/server_gp018_2005_2026.parquet`（79605 条，2005-2026，全A非ST）
- 回测运行器：`research/gp018/run_skill.py`（支持 PERIOD=5m 环境变量切真5分钟）
- 方法论：`.claude/skills/tdx-strategy-research/SKILL.md` §8（出场参数寻优流程）
- 背景报告：`research/gp018/2026-08-04_GUPIAO_018_新体系研究报告_大白话版.md`
  （捉妖已验证：冠军方案 = 信号+前5日涨幅≤15%+量比≤3 过滤，出场 -12%/移动3.5%·1%/阶梯5%·10%/12天，真5m 年化+3.3%）

## 四、新窗口怎么接（照做）

1. **先看结果到了几组**：
   `cat research/gp018/exitopt/grid.csv`
2. **如果第一轮 16 组没跑完**（grid.csv 不足 16 行，且没有 python 进程在跑 exit_opt）：
   - 重启：`python -X utf8 research/gp018/exit_opt.py stage1 0 2`（后台）+
     `python -X utf8 research/gp018/exit_opt.py stage1 1 2`（后台）
   - 已完成的组合会自动跳过，不会重复跑。
3. **第一轮齐了**（16 行）：启动第二轮：
   `python -X utf8 research/gp018/exit_opt.py stage2 0 2` + `stage2 1 2`（各一个后台）。
4. **第二轮也齐了**：取 grid.csv 总排名前 3，在终审窗口验证（真5m）：
   `PERIOD=5m python -X utf8 research/gp018/run_skill.py base <stop变体> 20150101 20201231 final_<名>`
   和 `... 20240101 20260801 final2_<名>`。
   注意：run.py 里的 stop 变体名如果只支持 base/cost12/time20/trail15_3/trail10_2/trail6_1，
   需要按冠军组合参数在 research/mmt/run.py 或 run_skill.py 的 stop_variant 里补一个。
5. **判定标准**：终审年化 ≥ 骨架基线(s1_c6_t35_1_loff_d12, 即-0.08%) 的 1.2 倍，
   且回撤不显著更深，单笔自助显著性 P<0.05。只在调参窗赢、终审不赢 = 过拟合，不交付。

## 五、注意事项（血泪教训，别重蹈）

- **只用真 5 分钟口径做判断**（period=5m）；日线的出场类数字一律打折看。
- 每组约 10 分钟；两个并行进程内存各占 ~20GB，别超过 2 个，否则内存不够进程会被杀。
- 通达信客户端必须开着（TdxW.exe），不在就跑不起来。
- 另一会话可能有别的回测在跑（会抢文件锁，报 WinError 32 属正常，重跑即可）。
- 本窗口的定时汇报（每 25 分钟，任务号 a0e12941）只在本会话有效；
  新窗口如需进度播报，重新挂一个检查 grid.csv 的定时任务即可。
