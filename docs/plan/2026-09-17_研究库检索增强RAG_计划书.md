# 2026-09-17 研究库检索增强（RAG）计划书

> 入口：用户 2026-09-17 拍板「动 RAG，选 C（全量索引，接受检索片段随提示发给模型厂商），
> `docs/` 与 `research/` 一起上」。
> 一句话目标：把研究大脑从「闭卷考试」改成「开卷考试」——回答前先在自己的资料库里
> 检索，引用原文作答。

---

## 1. 现状实测（先摆事实，不凭印象）

### 1.1 引擎已经在跑，而且不弱

`brain/search_engine.py`（505 行）是一个可用的中文语义搜索引擎：

| 项 | 实测值 |
|---|---|
| 模型 | BAAI/bge-small-zh-v1.5（fastembed ONNX 版，首次自动从 hf-mirror 下载约 200MB） |
| 向量维度 | 512，手动 L2 归一化（`_embed`，第 163~171 行） |
| 索引落盘 | `data/brain_vectors/vectors.npy` + `meta.jsonl`，tmp + `os.replace` 原子写（第 209~227 行） |
| 检索 | 查询向量与全量矩阵点积，`argsort` 取 top-k，毫秒级（`search`，第 372~395 行） |
| 分块 | 按空行切段 → 合并 → 硬切，块长 200~450 字（`_chunk`，第 91~138 行） |
| 评测 | `eval_search` 按 `expect_path` 命中率打分（第 397~419 行），现成评测集 `brain/search_evalset.json` |
| 大脑接线 | **已经教过大脑用它**：`brain/prompts.py` 第 28~31 行写着"语义搜索 vault……Bash 运行 `python -m brain.search_engine search "查询词"`" |
| 增量更新 | `brain/archive.py` 第 163~169 行：每次对话归档即 `update_file(f)` 同步索引 |

**结论：引擎、落盘、评测、大脑接线四件都已存在。** 我上次说"底座已有"是对的。

### 1.2 但书没上架——这是真正的缺口

索引范围**写死**在 `brain/search_engine.py` 第 42 行：

```python
VAULT_DIRS = ("company", "对话沉淀")
```

而 `build_index` 的遍历是（第 312~316 行）：

```python
for dir_name in VAULT_DIRS:
    d = VAULT_ROOT / dir_name          # VAULT_ROOT = vera_obs_vault/
    for md_file in sorted(d.glob("*.md")):   # 只取顶层，不递归
```

所以进索引的只有 `vera_obs_vault/company` 和 `vera_obs_vault/对话沉淀` 两个目录的**顶层**
Markdown。用户真正的成果一篇都没进去：

| 目录 | 篇数 | 体量 | 在索引里吗 |
|---|---|---|---|
| `research/`（研究报告） | 55 篇 | 0.6 MB | **没有** |
| `docs/`（审计/计划/方法论等） | 213 篇 | 2.6 MB | **没有** |
| `vera_obs_vault/company` + `对话沉淀` | — | — | 有（唯一在索引里的） |

`docs/` 的 213 篇分布：`audit/` 108 篇、`plan/` 64 篇、根层 25 篇、`brief/` 7 篇、
`superpowers/` 3 篇、`adr/` 2 篇、`report/` 2 篇、`architecture/` 1 篇、`plans/` 1 篇。

**今天的直接后果**：问大脑"我 2026年9月16日那份 ETF 轮动改良研究报告得出了什么结论"，
它翻不到——那是本地文件，它没索引过。

## 2. 用户已拍板的决策（留痕，不再重议）

**选 C：全量索引，接受检索片段随提示发给外部模型厂商。**

- 事实依据：我实测核对过 `research/` 里 **16 篇**提到止损/止盈，其中至少 2 篇带具体参数
  （如 `research/2026-09-16_ETF轮动基线诊断与改良迭代_研究报告.md`）；`docs/` 里 **88 篇**
  提到止损。项目 `.gitignore` 第 58~59 行把这类报告标注为「含策略止盈止损参数 = 高级机密」。
- 三档大脑全部走外部 API（快速档 OpenAI 兼容 / 标准档 Anthropic 兼容 / 深度档 DSH），
  检索片段会跟着提示一起发出去。
- 用户已知悉并选择 C。**本计划不再讨论要不要脱敏或排除。**

**但保留一个可逆开关**（这是让决定可撤销，不是重议）：索引范围走配置而不是硬编码常量，
将来想收紧成"只索引 docs/"或"排除带参数的报告"，改配置 + 重建即可，不用改代码。

## 3. 关键设计：`source` 路径约定必须统一（本计划最大的技术风险）

现状有一处会造成**同一文件被索引两遍**的隐患：

- `build_index` 第 325 行：`source = str(md_file.relative_to(VAULT_ROOT).as_posix())`
  → 产出形如 `company/company_002390.md`（**vault 相对**）
- `update_file` 第 342 行：
  ```python
  source = str(filepath.relative_to(VAULT_ROOT).as_posix()) \
           if filepath.is_absolute() and VAULT_ROOT in filepath.parents \
           else filepath.as_posix()
  ```
  → 文件**不在** `vera_obs_vault/` 下时，`source` 退化成**绝对路径**

加了 `research/`、`docs/` 两个新根之后，这个分叉会立刻爆炸：
全量构建产出 `research/xxx.md`，而归档增量更新产出 `E:/1target/VERA/research/xxx.md`，
两个 key 指向同一个文件 → 索引里出现重复块，检索结果里同一篇文章挤占多个 top-k 名额。

**决策：`source` 统一为「项目根相对 posix 路径」**（如 `vera_obs_vault/company/company_002390.md`、
`research/2026-09-16_ETF轮动基线诊断与改良迭代_研究报告.md`）。

这带来三个必须一起做的连带动作：

1. **全量重建索引**（`build --force`）。旧索引的 `company/xxx.md` 形态与新形态不兼容。
2. **迁移评测集**。`brain/search_evalset.json` 现有 62 行全部形如
   `"expect_path": "company/company_002390.md"`，必须整体加上 `vera_obs_vault/` 前缀，
   否则 `eval_search` 的命中判定（第 410 行精确等值比较）会**全部判失败**。
3. **顺带修一个老毛病（净改善）**：现在 `_cmd_search` 打印的 `path` 是
   `company/company_002390.md`——**这不是一个从项目根可读的路径**，大脑拿到后要自己猜
   前面还得补 `vera_obs_vault/`。统一成项目根相对路径后，大脑可以直接把这个路径丢给
   `Read`。这一条属于本计划的附带修复，要写进 CHANGELOG。

### 3.1 【铁律】先收口，再扩范围 —— 顺序不能颠倒

用户 2026-09-17 明确要求「**这个技术风险要重视，要避免**」。据此把执行顺序定死：

| 步骤 | 做什么 | 做完的判据 |
|---|---|---|
| **第一步**（唯一允许先做的改动） | 把 `source` 计算收口成唯一函数 `_to_source(path)`，`build_index` 与 `update_file` 都改引它。**此时索引范围一个字都不改** | `build --force` 重建 + 跑评测，**命中率与改动前完全一致**（范围没变，结果就不该变） |
| **第二步** | 才去加 `research/` 与 `docs/` 两个新根 | 评测整体命中率 ≥ 第一步基线，新增研究类问题命中率明显上升 |

**为什么必须分开**：如果两件事一起做，一旦检索结果变差，**无法区分是「source 口径变了」
还是「新语料稀释了 top-k」**——两处变量同时动，出了事查不出来。分开做，每步都有干净对照。

### 3.2 三重防线（缺一不可，每条都要有测试锁）

| # | 防线 | 机制 | 防的是什么 |
|---|---|---|---|
| **①** | **唯一出口** | 全项目只有 `_to_source(path)` 一处算 source；加一条 grep 断言：`relative_to(VAULT_ROOT)` 在 `brain/search_engine.py` 里**只允许出现一次** | 再分叉出第二个路径口径（项目反复踩的"同一条规则手写 N 份必然漂移"） |
| **②** | **拒绝绝对路径** | `_to_source` 出口处断言结果**不是绝对路径**（`is_absolute()` 为真、或以盘符开头 → 抛错），不允许静默退化 | 现状第 342 行的行为：VAULT_ROOT 外的文件会退化成绝对路径，与新根的相对路径撞成两个 key |
| **③** | **构建后自检** | `build_index` 收尾断言 `len({m['source'] for m in meta})` == 本次处理的文件数；不等即有文件被记成两个 key，**直接失败、不落盘** | 同一文件重复入库、一篇报告挤占多个 top-k 名额 |

**三条测试**（新增到 `tests/test_brain_search_engine.py`）：

1. 同一文件分别经 `build_index` 与 `update_file`，`source` **字符串必须相等**（回归锁，防未来又分叉）
2. 传一个 `vera_obs_vault/` **之外**的临时文件给 `update_file`，`source` 必须是项目根相对路径，
   且**等于** `build_index` 对同一文件会给出的值
3. 人为构造 source 冲突（两个文件映射到同一 source），`build_index` 必须**报错**而不是静默写重

### 3.3 附带发现的并发风险（本轮实测踩到，需要知道）

我在 2026-09-17 这轮实测到一件事：**这个仓库同时有别的会话/工具在写**——
工作区里出现了不是我创建的文件（`docs/plan/2026-09-17_ETF轮动买侧在途台账与隔夜核销_计划书.md`、
`docs/audit/2026-09-17_全系统过度工程_审计报告.md`）。

对索引的直接影响：`build_index` 是**全量重建**，跑的过程中若别的会话正往 `docs/` 或
`research/` 写文件，可能收进写到一半的半截文件。处置：

- 构建前记录各根目录的文件清单 + mtime 快照，构建后比对；发现新增/变更就**再跑一轮**
  （全量重建是幂等的，代价只有几分钟）
- 索引落盘本来就是原子写（`_save_index` 第 209~227 行 tmp + `os.replace`），
  读者读到撕裂态的风险已被既有机制挡住——**这一点不用新做**

## 4. 要改什么（逐条）

### 4.1 索引范围改成配置驱动

新增 `config/brain_index.json`（`.gitignore` 已忽略 `*.json`，天然不入库）或直接在
`brain/search_engine.py` 里定义一个可被环境变量覆盖的常量。内容形如：

```json
{
  "roots": ["vera_obs_vault", "research", "docs"],
  "exclude_dirs": [".git", "__pycache__", "node_modules", "data", "output",
                   "logs", ".agent-teams", "vera_obs_vault/对话沉淀/_tmp"],
  "min_chars": 50
}
```

选择顺序：**先读 `config/brain_index.json`，读不到回落内置默认（= 现有 vault 两目录，
零行为变化）**。这样"没配置 = 老行为"，符合项目对默认值的纪律。

### 4.2 遍历改递归

`d.glob("*.md")` → `d.rglob("*.md")`，并在遍历时按 `exclude_dirs` 剪枝
（`docs/` 是嵌套的：`docs/audit/`、`docs/plan/` 都在子目录里，不递归等于没加）。

### 4.3 `source` 统一（见 §3）

`build_index` 与 `update_file` 共用同一个 `_to_source(path) -> str` 私有函数，
按项目根算相对路径。**两处必须引同一个函数**——这正是项目反复踩的"同一条规则
手写 N 份必然漂移"（买卖口径×2、行情陈旧判定×3、AI 设置三档）。测试里加一条：
同一个文件经 `build_index` 与 `update_file` 得到的 `source` 必须相等。

### 4.4 提示词更新

`brain/prompts.py` 第 28~31 行现在写的是"语义搜索 vault（概念类查找）"。改成分辨率更高的说明：

- 资料库 = vault 公司档案 + `research/` 研究报告 + `docs/` 审计与计划与方法论；
- 明确"问'我们以前研究过什么/某结论怎么来的/某个参数为什么这么定'时，**先搜资料库再答**"；
- 保留"引用具体文件路径"的既有铁律（第 132 行）。

注意：`SLIM_SYSTEM_PROMPT`（快路径瘦身版，第 159~194 行）**不含**模式 A/B 与固定打法段，
快路径本来就不调工具，故**不改**。`tests/test_brain_prompts.py` 第 11~13 行断言
`SYSTEM_PROMPT` 含 `python -m brain.search_engine search` 字符串——改提示词时**必须保留
这个字符串原样**，否则测试红。

### 4.5 评测闭环：先测基线，再改，再测

这是本计划唯一能量化"有没有变好"的动作，不能省：

1. **改动前先跑基线**：把新加的研究/文档类问题写进评测集，此时命中的是旧索引
   （新语料还没进去），`python -m brain.search_engine eval` 记下命中率。
2. 改代码 → `build --force` → **再跑一次**。
3. 验收判据：**整体命中率不低于改动前**（老的公司类问题必须仍然命中），
   且新增的研究类问题命中率有明确提升。

新增评测题建议 15~20 道，覆盖三类：

- 研究报告类：「ETF 轮动 20 日动量窗口是怎么定下来的」→ 期望命中
  `research/2026-09-16_ETF轮动基线诊断与改良迭代_研究报告.md`
- 审计/事故类：「缓存投毒事件是怎么发生的」→ 期望命中对应的审计或计划文档
- 方法论类：「公式因子体检的五条纪律是什么」→ 期望命中 `docs/公式因子体检方法论.md`

### 4.6 邮箱语料：纳入索引（用户 2026-09-17 拍板），落盘设计见下

用户 2026-09-17 让我用 `qqmail` 技能收了收件箱最新 9 封，实测发现一件计划外的事：

**有一个独立的「研究产物自动分发」流水线，把大量高质量研究报告发到这个邮箱**
（发件人 `研究产物自动分发 <dcliuzh@qq.com>`，最近 9 封时间跨度 2026-09-12 ~ 2026-09-13，
主题全是 A 股牛熊面板/回测验证/大盘研判，附件是 6.2MB、82 个文件的产物包）。

**这对本计划的含义**：

- 这些报告**不在仓库里**（既不在 `research/` 也不在 `docs/`），所以 §4.1 的两个新根
  **收不到它们**，大脑检索不到。
- 它们的内容质量不低（§4.7 与大盘位置计划书 §14 的改良项就是从里面来的），丢掉可惜。

**用户 2026-09-17 拍板：邮箱语料也进 RAG。** 采用下表选项 B，设计如下。

#### 4.6.1 落盘设计

```
qqmail 技能收信  →  docs/research_inbox/<日期>_<主题>/   ← 只落 .md，且保留原始相对路径
                              ↓
                    加进 brain/search_engine 的 roots
```

- **只索引 `.md`**。实测那批产物包里：`.md` 21 篇 / 237 KB、`.html` 20 篇 / **8 MB**、
  `.py` 78 个 / 1 MB、`.json` 24 个 / 2.3 MB。HTML 体积是 Markdown 的 34 倍且满是标签噪音，
  `.py`/`.json` 是源码与数据不是报告——**一概不收**。
- **落盘目录与用户自己的研究物理分开**：用户手写的在 `research/`，这批外部产物在
  `docs/research_inbox/`。好处有两个：①`source` 路径天然带上来源，检索结果一眼能区分
  "这是我们自己研究过的" 还是 "这是别人发来的"；②将来想整批清掉只需删一个目录。
- **同名去重**：实测同一篇 `A股牛熊面板-资金层升级与指标验证.md` 在 9 个包里出现了 2 次。
  落盘时按内容哈希去重（同内容只留一份），否则同一篇文章会挤占多个 top-k 名额
  ——这与 §3 要防的是同一类病。
- **每篇顶部注入来源元信息**（照 `_extract_file_prefix` 对 company 文件的既有做法）：
  `来源：邮箱 dcliuzh@qq.com ｜ 收到：2026-09-13 ｜ 原始附件：xxx.zip`。
  检索片段自带出处，大脑引用时能写清"这是外部研报说的"，**不会与用户自己的结论混淆**。
- **`.gitignore` 要加 `docs/research_inbox/`**：这是外部第三方产物，且体积随收信增长，
  不适合入库。它是可重建的（重跑收信脚本即可）。

#### 4.6.2 收信→落盘要单独写一个脚本，且**不并入索引器**

新增 `tools/mail_to_corpus.py`：调 `qqmail` 技能 → 解出 `.md` → 去重 → 落
`docs/research_inbox/`。理由：

- **索引器只管"读仓库里的文件"，不碰网络/邮箱**。这条边界必须守住，
  否则 §3 的 `source` 唯一口径会被"从邮件来的语料"搅浑（那正是选项 C 被否掉的原因）。
- 收信是"拉外部数据"，落盘后就是普通仓库文件，索引器视角完全统一。

#### 4.6.3 必须带的质量警告（这批语料不是权威）

实测证据（详见大盘位置计划书 §14.6）：**同一批产物、同一天，两封邮件给出相反结论**
（16:24 那封说"现在是牛市"，17:11 那封说"不算牛、下跌中继"）。所以：

- 检索结果里**必须**能看出这是外部产物（靠 §4.6.1 的来源前缀与目录名）；
- 提示词里要加一句：**引用外部研究库内容时，必须说明来源并提示"该结论未经本系统复核"**；
- 评测集里**不放**外部语料的问题（评测集只考"能不能找到用户自己的报告"），
  避免用外部内容拉高命中率掩盖真实问题。

#### 4.6.4 一个意外收获：这批语料里有 ETF 轮动的现成研究

82 文件那个大包里有一整个 `02_ETF策略研究/` 目录，含 6 份 ETF 报告
（动量轮动、高抛低吸遍历、外部专家报告验证、尾盘成交时点验证、双周 vs 每周复核）。
本项目的 ETF 轮动已做过三轮 244 组研究，**这 6 份是可交叉验证的外部参照**。
这属于 ETF 轮动的研究范畴，**不在本计划范围内**——此处只登记线索，
另立研究任务时再取用（语料已在索引里，届时大脑可直接检索到）。

### 4.7 读信读出来的一个改良项（转交大盘位置计划书）

9 封邮件里有一份《A股大盘研判：跌破年线 3.49% 还算牛吗？》（2026-09-13 17:11），
它**独立重现了我做的「历史照镜子」**（10 个特征 + Z-score + 欧氏距离 + 取最近邻），
但有三处做法比我的实现更严谨；另外 v5 面板的「五维验证框架」与两处图表设计也值得抄。
这些都不属 RAG，已转写到 `docs/plan/2026-09-17_大盘位置与趋势研判_计划书.md`
的新增 §14.1 / §14.2 / §14.3（照镜子三处缺陷）+ §14.7（五维验证框架）+
§14.8（图表设计）。此处只留指针，不重复展开。

## 5. 规模与耗时估算

| 项 | 估算 | 依据 |
|---|---|---|
| 新增语料（仓库内） | 268 篇 / 3.2 MB | 实测（`research/` 55 篇 0.6 MB + `docs/` 213 篇 2.6 MB） |
| 新增语料（邮箱产物） | 21 篇 / 237 KB | 实测（9 个产物包解出的 `.md`；`.html` 8 MB 与 `.py`/`.json` **不收**） |
| 新增块数 | 约 3000~5000 块 | 按 200~450 字/块，中文 1 字约 3 字节 |
| 索引体积 | 约 6~10 MB | 512 维 float32 = 2KB/块 |
| 嵌入耗时 | 约 1~3 分钟 | fastembed CPU（首次还要下约 200MB 模型） |
| 检索耗时 | 不变（仍是毫秒级） | 点积矩阵从 N 涨到 N+5000，量级未变 |

**注意**：`docs/research_inbox/` 在 `docs/` 之下，所以 §4.1 的 `roots` **不用改**
（`docs` 根已覆盖）。这是把邮箱语料落在这个位置的附带好处。

**结论：这是一件"一次性几分钟、之后边际成本为零"的事。**

## 6. 模块划分（开工前五问）

| 问 | 答 |
|---|---|
| **放哪层?** | 全部改在既有深模块 `brain/search_engine.py` 内部（它是引擎本体），**不新建模块**。配置读取下沉成一个小私有函数。 |
| **接口多大?** | 公开面**零新增**：`VaultSearchEngine` 仍只有 `build_index` / `update_file` / `search` / `eval_search` 四个方法，CLI 子命令不变。新增的 `_to_source` / `_load_scope` 一律带 `_` 前缀当内部接缝。 |
| **有没有第三份?** | 本次恰恰是**消除一份**：`source` 路径原本在 `build_index` 与 `update_file` 各写一遍且语义分叉，收口成一个函数。 |
| **删除测试** | 删掉 `search_engine.py`，语义检索能力原地消失（不是透传）→ 该留。 |
| **改完登记** | CLAUDE.md 架构骨架"公式系统/研究大脑"邻近行补一句 + CHANGELOG + commit。 |

## 7. 风险与边界

| 风险 | 处置 |
|---|---|
| `source` 分叉导致同一文件重复入库 | 收口 `_to_source` + 单测断言两侧一致（本计划最重要的一条） |
| 评测集没迁移 → eval 全判失败 → 误判"改了以后变差了" | 迁移评测集是 §3 的强制连带动作，写进验收标准 |
| 提示词改动踩掉 `tests/test_brain_prompts.py` 的断言 | 必须原样保留 `python -m brain.search_engine search` 字符串 |
| 索引变大后内存 | 512 维 × 约 8000 块 ≈ 16 MB float32，忽略不计 |
| `docs/` 里有生成物混进来 | `exclude_dirs` 剪枝 + `min_chars=50` 丢弃碎块；首次构建后抽查 `meta.jsonl` 的 source 分布 |
| 机密随检索片段外发 | **用户已选 C 并知悉**（§2），保留可逆开关 |

## 8. 不做（明确划界，防范围蔓延）

- **不做重排序模型**（rerank）：检索 top-k 后不再用交叉编码器精排。理由：要再下一个模型
  + 显著加延迟，而现在的瓶颈是"语料没进去"，不是"排序不精"。
- **不做混合检索**（关键词 BM25 + 向量）：大脑已有 `Grep` 做关键词精确匹配
  （`brain/prompts.py` 第 28 行明确二者互补），不需要在引擎里再实现一份。
- **不换 embedding 模型**：保持 bge-small-zh-v1.5。换模型要重下模型 + 全量重建，
  且向量维度变了旧索引直接作废，收益不确定。
- **不改 `SLIM_SYSTEM_PROMPT`**：快路径不调工具。

## 9. 验收标准

1. `python -m brain.search_engine build --force` 后，`data/brain_vectors/meta.jsonl` 里的
   `source` 同时出现 `vera_obs_vault/...`、`research/...`、`docs/...` 三类前缀。
2. `python -m brain.search_engine search "ETF 轮动改良研究的结论"` 的前 3 条里包含
   `research/2026-09-16_ETF轮动基线诊断与改良迭代_研究报告.md`。
3. `python -m brain.search_engine eval` 整体命中率 **≥ 改动前基线**，新增研究类问题命中率明显上升。
4. 单测：同一文件经 `build_index` 与 `update_file` 得到的 `source` **相等**
   （防重复入库的回归锁）。
5. `pytest tests/test_brain_search_engine.py tests/test_brain_prompts.py` 全绿。
6. 大脑端到端实测：问"我 2026年9月16日那份 ETF 轮动研究报告得出了什么结论"，
   回答中引用了该文件的真实路径与结论原文（不靠训练记忆编）。

## 10. 开工前待核实项（诚实标注，我没把握的部分）

1. **`docs/` 里是否有不该索引的生成物**。我只按体量做了分布统计（audit 108 篇 / plan 64 篇
   / 根层 25 篇等），**没有逐篇看过内容**。首次构建后我会抽查 `meta.jsonl`，
   把疑似生成物（如带时间戳的机器产物）补进 `exclude_dirs`。
2. **全量重建对现有 vault 检索质量的影响**。新增 3000+ 块后，老的公司类问题可能被
   稀释（top-k 名额被研究报告抢走）。这正是 §4.5 要"先测基线再改"的原因——
   如果老问题命中率掉了，备选方案是**分索引**（vault 一个索引、文档库一个索引，
   查询时各取 top-k 后合并），但这会让接口变大，**非必要不做**。

---

## 11. 外部最佳实践对照与采纳（2026-09-17 联网检索后新增）

检索来源：[RAG 分块策略指南（Atlan）](https://atlan.com/know/chunking-strategies-rag/)、
[RAG 索引漂移检测（Oracle）](https://blogs.oracle.com/developers/how-to-detect-rag-index-drift-deleted-docs-stale-chunks-and-duplicate-embeddings)。

### 11.1 逐条对照

| # | 外部实践 | 我们现状 | 采纳结论 |
|---|---|---|---|
| **R1** | **RAG 最难的失败来自「陈旧、失管的源数据」，而不是分块大小没调好**（Atlan 指南的核心结论） | §3.2 有构建后自检、§3.3 有并发快照，但**没有"索引新鲜度"这个概念** | ✅ **采纳**（§11.2） |
| **R2** | 索引漂移的三种命名病：**已删除文档仍在索引 / 陈旧块 / 重复嵌入** | 重复嵌入：§4.6.1 有哈希去重 ✅；陈旧块与已删除：**全量重建天然解决**，但**没有断言兜底** | ✅ 采纳（§11.2 的"双向集合相等"断言） |
| **R3** | **元数据增强检索，准确率最高可比纯内容检索高 5 倍** | §4.6.1 已对邮箱语料注入来源前缀 ✅，但**只做了那一类** | ✅ **采纳并推广**（§11.3） |
| **R4** | 固定大小分块会切断语义；**层级 / 父子分块**能提升质量 | `_chunk` 是按段落递归合并 + 硬切（比固定大小好），无父子结构 | ❌ **不采纳**：Atlan 自己说最难的失败不是分块大小；改分块要全量重建且收益不确定，**违反"先跑通再优化"** |
| **R5** | 评测不能只看命中率，要看 **MRR / nDCG / recall@k** | `eval_search` 只算"`expect_path` 是否在前 5"（= recall@5），**排名质量完全没测** | ✅ **采纳 MRR**（§11.4），nDCG 不做（无相关性分级，用不上） |
| **R6** | 索引健康度要有独立监控 | 无 | ⚠️ 部分采纳：不引入 `raghealth` 之类外部工具，但 §11.2 的自检 + §4.5 的 eval 已覆盖核心 |

### 11.2 采纳 R1+R2：加「索引新鲜度」

外部结论很直接：**索引最怕的是"源文件变了而索引没跟上"**。我们的 `build_index` 有
`skipped` 早退逻辑（第 305~306 行：meta 存在就直接 `return {"skipped": True}`）——
**这正是"陈旧索引"的入口**：只要有人忘了加 `--force`，语料更新了索引也不动，
而**大脑看不出区别**。

**改法**：`build_index` 计算并落盘一个**新鲜度指纹** `index_stamp.json`，含：

- 构建时间；
- 本次纳入的**文件清单 + 各自 mtime**（或整体哈希）；
- 各 `root` 的文件数。

`search` 与 CLI 在检索前**比对当前磁盘清单与指纹**：不一致就打印一行醒目的
「**索引陈旧：自 YYYY-MM-DD 起有 N 个文件变动，请重跑 build**」。
**只告警不自动重建**（自动重建会让检索变成分钟级，破坏"毫秒级"这个核心体验）。

配套把 §3.2 的防线③从单向升级为**双向集合相等**：

```
索引里的 source 集合  ==  磁盘上扫描到的文件集合（相对路径）
```

少了 → 有文件没进索引；多了 → **有已删除文件仍留在索引里**（R2 的第一种病）。
两个方向都要断言，不等就失败、不落盘。

### 11.3 采纳 R3：元数据前缀推广到全部语料

§4.6.1 已经给邮箱语料注入来源前缀，实测这个做法有效，**推广到所有根**：

| 语料 | 注入的前缀 |
|---|---|
| `vera_obs_vault/company/*` | 已有：`公司：X。股票代码：Y。所属行业：Z。`（`_extract_file_prefix`，第 231 行） |
| `research/*` | `研究报告：<文件名主题>。日期：YYYY-MM-DD。` |
| `docs/audit/*` | `审计报告。日期：YYYY-MM-DD。` |
| `docs/plan/*` | `实施计划。日期：YYYY-MM-DD。` |
| `docs/research_inbox/*` | `外部研究产物（邮箱）。来源：X。收到：YYYY-MM-DD。` |

日期从文件名前缀 `YYYY-MM-DD_` 正则提取，提不到就不写（**不编日期**）。
好处：检索片段自带出处与类型，大脑引用时能分清"我自己写的计划"和"外部发来的研报"。

### 11.4 采纳 R5：评测加 MRR（这条是刚需）

现在 `eval_search` 只判"在前 5 吗"。**语料扩大 3 倍后，最典型的退化不是"找不到"，
而是"找到了但排到第 4 第 5"**——现指标对此**完全瞎**。

**改法**：在 `eval_search` 返回值里加：

- `mrr`：命中文档排名倒数的平均（排第 1 得 1.0，排第 5 得 0.2，没命中得 0）；
- 命中但排名 > 1 的题目清单（`ranked_low`），便于逐题排查。

这条直接服务 §3.1 的两步走：**第一步**（只收口 source 口径）要求
**hit_rate 与 MRR 双双不变**；**第二步**（扩语料）要求 hit_rate 不降、
**MRR 不显著下降**（新语料会稀释排名，降一点是正常的，但要看得见）。

### 11.5 §11 的验收标准

1. `build_index` 落盘 `index_stamp.json`；`search` 检测到陈旧时打印醒目告警（有测试锁）。
2. 构建后断言**双向集合相等**（索引 source 集合 == 磁盘文件集合），不等则失败不落盘。
3. 五类语料都有对应元数据前缀；日期提不到时不写（有测试锁）。
4. `eval_search` 返回 `mrr` 与 `ranked_low`；§3.1 两步走的判据改为 hit_rate + MRR 双指标。
5. **不引入父子分块、不引入外部 RAG 健康度工具**（§11.1 的 R4/R6）。
