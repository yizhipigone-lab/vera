# Handoff: VERA 架构优化计划执行 (批次 1 收尾 → 批次 2-6)

Next session purpose: 按 `docs/plan/2026-08-01_架构优化计划书.md` 继续执行批次。计划书是真相源, 本文档只补会话上下文。

## Start here

**批次 1 只剩 A3 一项 (~2h)**: save_trade 失败的 reconciler 闭环。改 `trade/reconciler.py::_adopt_manual_trades` —— 当 `book.apply_trade` 因幂等返回 False、但 `trades` 表查无此 traded_id 时, 仍执行 `save_trade` 补写 (QMT 是真相源, sync_reports 是所有回调/落库丢失的统一兜底网, 与 _on_sync_reports 设计哲学一致)。
验收: 新增测试 "首次 save_trade 失败后, 下一轮 sync_reports 主动补写 trades 行"; 跑 `python -X utf8 -m pytest tests/trade/` 全绿。
之后直接合并推进批次 2 (缓存原语, 半天), 省下的时间给批次 3 (parity 快照是最耗时单项)。总估时 ~3.5 天。

## Goal & current state

目标: 完成计划书批次 1-6 (正确性 → 缓存 → 回测一致性 → 瘦身 → 清理 → 债)。

**已完成且经核实** (上一个会话逐项验证过代码):
- P0 六项: hold_days 接线 / 尾盘定价 (沪市禁市价+创业科创贴笼子, 含 688 修复) / `_on_pending_died` 解除 _triggered / EOD 空快照守卫 / webhook 抽环境变量 / 委托价存 order_price
- H5 重连死循环 (连续失败 3 次释放旧 trader + 换 session_id, gateway.py `_note_connect_failure`)
- 翻页 (4 端点 ge=1 + tie 决胜键)
- `tests/__init__.py` (修 site-packages `tests` 抢名导致的 4 个收集错误; 全量 1408 可收集)
- 批次 1 的 A1/A2/A4/A6/A7 (用户另一窗口完成, 本会话 08-01 复核属实):
  A1 monitor.py `_date` 戳跨日重置 high/prev_close; A2 events.py `_CRITICAL_TYPES` + 关键事件 5s timeout;
  A4 store.py 终态守卫 (SELECT status, 53/54/56/57 直接 return); A6 core/limit_ratio.py 单一实现 (engine+executor 统一引用);
  A7 executor.py `_CANCEL_ACK_TIMEOUT_SEC = 5.0`

**测试基线**: trade 283+ 绿 (用户新增测试后更多), 全量 1408 收集无错。跑法: `python -X utf8 -m pytest tests/trade/`。

**未做**: A3 (见 Start here)、A5 parity 快照基线、批次 2/4/5/6 全部。

## Decisions & why (不要再翻案)

- **fill ctx 用 peek 不用 pop** (部成多笔共享原因); 回收点只在 _on_trade 满量后 + _sync_orders —— **绝不能放 _on_order**, 终态委托回报可能先于成交回报到达 (FakeGateway 实证, 踩过)。
- **688 科创板判定只许代码前缀** ("300","301","688"), 禁用 is_sz —— 688 是 .SH。
- **A3 验收以用户 08-01 校准为准**: 不是"kill 首次 save", 是"sync_reports 主动对 book 有/trades 缺的行补写", QMT 为真相源。
- **AutoBuyFeature 提取 (批次 4) 照 html 报告 Card1 做, 但重连状态机不动** (消费者线程纪律, 不为瘦身造正确性风险)。
- **批次 3 顺序硬性**: parity 快照基线先于 engine run/run_cached 合并; 基线与现状有差异时先查因再固化, 不机械对齐。
- 缓存原语放 `utils/parquet_cache.py` 不放 selection/ (matrix_cache 跨层用)。
- 不做清单照计划书 §四 (不动 trade 架构/不换存储/不动成交价口径/不改 git 历史/每批独立可合)。

## Open questions & blockers

- **A3 现状**: 用户说已加了重试兜底 (未核实位置), reconciler 主动补记是闭环 —— 开工先看 _on_trade save_trade 失败处理现状, 别重复造。
- **飞书 webhook 轮换**: 代码侧已清, 但旧 URL 仍需用户手动去飞书后台重置 (只能人做)。
- **EOD 真空仓边缘**: 全卖光的日子也走 eod_skip, 旧快照残留 (C 方有成交净额兜底, 影响小, 知情取舍已接受)。
- **M5 启动窗口单写者** (主线程 engine.start 后仍跑 reconcile/place_ladder, remark 序号理论撞号): 未修, 低危, 批次 4 时可顺带评估。
- architecture-review HTML 在 Temp 目录, 随时可能被清 —— 其有效内容已全部并入计划书。

## Pointers

- 计划 (真相源): `docs/plan/2026-08-01_架构优化计划书.md`
- 审计依据: `docs/audit/2026-07-31_trade_质量审计报告.md` · `docs/audit/2026-08-01_VERA系统全面评估报告.md` · `docs/audit/2026-08-01_VERA系统评估_复核与优先级.md`
- 批次 2 关键文件: `selection/selection_cache.py` / `signal_day_cache.py` / `universe_cache.py` / `backtest/matrix_cache.py` / `core/kline_cache.py:403` (pid+uuid 范式) / `tests/conftest.py`
- 批次 3 关键文件: `backtest/engine.py` (run:363-555 vs run_cached:690-789) / `docs/architecture/loop.md` §7.7 (快照方案)
- 批次 4 关键文件: `trade_main.py` (auto_buy 段 ~660-920 行) / `tests/trade/test_auto_buy.py`

## Suggested skills

- `superpowers-test-driven-development` — 每批次先写行为级测试 (计划书验收列了具体用例)
- `superpowers-verification-before-completion` — 每批次交付前跑全量再报数
- `code-review` — 批次 2/3 (重构类) 合入前过一遍
