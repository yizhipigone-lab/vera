# 研究大脑 DSH 深度思考通道 计划书

> 来源：复刻 IRX《DSH深度思考通道-实施全记录与复刻手册》（2026-09-04，`E:\1target\IRX\docs\guides\`）。
> 读者：执行本计划的工程师/Agent，不需要事先了解 DSH。
> 日期：2026-09-04 · 仓库：VERA

***

## 0. 结论先行（大白话）

**要做什么**：给研究 TAB 的对话大脑卡片加一个「🧠 深度思考」勾选框（**不做独立 TAB、不做全局侧边栏**，决策理由见 D11）。勾选后，问题不再走现在的 claude CLI 大脑，而是发给一个**住在仓库里的独立 DSH 子进程**（DeepSeek 官方 harness，能自己写代码、读文件、联网），答完进程自动退出。不勾选，现有大脑一个标点都不动。

**我们和 IRX 的差距在哪**：IRX 的默认引擎是"一双眼睛没有手"（19 个只读工具），DSH 是它的"手"。**VERA 的现有大脑已经有手**（claude CLI 带着 Bash/Write/Edit 在跑），所以我们复刻的不是"有没有手"，而是 IRX 这套**工程化外壳**：

| IRX 有、VERA 大脑现在没有                | 价值（大白话）                                      |
| -------------------------------- | -------------------------------------------- |
| 便携运行时 `dsh-runtime/`（整个文件夹拷走即迁移） | 现在大脑依赖全局装的 claude CLI，换台电脑就瘫；DSH 住在仓库里，跟着仓库走 |
| 升级制路由（勾选才走 DSH，默认不动）             | 便宜快捷的问题走原路，重活才请外援，成本可控                       |
| 停止按钮（杀进程树）                       | 现在大脑跑飞了只能干等 300 秒超时                          |
| 每问全量留档 + 泄漏关键词扫描                 | 发给外部 LLM 的上下文全部记账+扫描，敏感信息（账户/策略）出网即告警        |
| ADR 决策留痕                         | 为什么开这个口子，白纸黑字                                |

**风险多大**：低。不勾选 = 零改动；勾选 = 新代码路径；`DSH_CHANNEL_ENABLED=False` 一键停用。留档失败时宁可不给答案（IRX 铁律，照抄）。

## 1. 目标与架构

**Goal**：研究 TAB 对话卡片新增「🧠 深度思考」勾选通道，勾选后问题路由到仓内便携 DSH headless 子进程，带停止按钮、每问全量留档与泄漏告警；未勾选路径零改动。

**Architecture**：

```
研究 TAB 对话卡片（web/index.html + web/js/brain_chat.js）
   │ 勾选「🧠 深度思考」→ body.deep=true（+history 多轮打包）
   ▼
POST /api/research/chat/stream（research_api.py 加 deep 分流）
   │ deep 缺省 → 现有 fastpath / ask_brain（claude CLI，零改动）
   │ deep=true → brain/dsh_channel.py（新深模块，接口仅 3 符号：run_dsh/stop_dsh/scan_leak）
   ▼
asyncio 子进程: dsh-runtime/dsh.cmd --profile headless <任务文本>
   │ cwd=dsh-runtime/workspace, env 自注入 DSH_HOME=dsh-runtime/home
   ▼
便携 Node 22 + @deepseek-ai/dsh@0.1.0-rc.6（从 IRX 拷贝，pnpm hoisted 平铺）
   │ 写完 stdout → 进程退出（"摇醒"机制：一次性程序，无常驻无端口）
   ▼
收尾：读 DSH 会话日志 → SHA-256 + 泄漏关键词扫描 → 落 SQLite data/brain_dsh_runs.db
```

**Tech Stack**：Python asyncio 子进程（与 brain/claude\_cli.py 同款）、FastAPI SSE（现有模式）、SQLite（stdlib sqlite3，VERA 无 Postgres）、Node 22 + pnpm（仅部署期）。

**「摇醒」机制（关键概念，照抄 IRX §3）**：DSH headless 是一次性程序而非服务——每次提问摇醒一个新进程，答完即灭。多轮对话的"记忆"靠把近期对话**打包进任务文本**（新进程 = 读完简报的新顾问）。所以 `start_vera.bat` 不需要启动/停止任何 DSH 进程，只加一行体检提示。

## 2. 决策点（对照 IRX 手册逐条 VERA 化）

| #   | IRX 决策                            | VERA 取值                                                                                                                                                                                                                           | 理由                                        |
| --- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| D1  | 升级制路由（勾选才进 DSH）                   | **照抄**：不勾 = 现有 claude 大脑零改动；勾选 = DSH                                                                                                                                                                                              | 手册核心决策，风险隔离                               |
| D2  | 完整信任不裁（DSH 全工具面）                  | **照抄**：与 VERA 现有大脑 `--dangerously-skip-permissions` 同级，安全红线继续靠 prompts 纪律 + 检测型控制                                                                                                                                                 | VERA 大脑早已裸奔（2026-08-01 用户拍板），DSH 通道不扩大风险面 |
| D3  | 检测型控制替代物理隔离                       | **照抄 + 加码**：每问全量留档 + 泄漏扫描；**留档失败 = 显性失败**（宁可不给答案）                                                                                                                                                                                 | 这是底线不是装饰                                  |
| D4  | 便携部署 git 跟踪本体                     | **改为整个 dsh-runtime/ 不入 git**：VERA .gitignore 风格本就保守（`*.json` 都排除），运行时可从 IRX 拷贝或脚本重建                                                                                                                                               | node.exe + 凭据 + 会话日志，没有入 git 的价值          |
| D5  | 一期单轮+等全文；多轮靠历史打包                  | **照抄**：前端把该会话最近 5 轮打进 `history`，DSH 无会话续接                                                                                                                                                                                         | 真流式（ACP）留二期，与 IRX 一致                      |
| D6  | Postgres `assistant_dsh_runs` 留档表 | **改为 SQLite** **`data/brain_dsh_runs.db`**（stdlib，零新依赖）                                                                                                                                                                           | VERA 没有 Postgres，全仓 SQLite                |
| D7  | 同步 Popen + sleep 轮询               | **改为 asyncio 子进程**（复用 claude\_cli 的 `_kill_tree`，Windows 双坑已解决）                                                                                                                                                                   | 与 VERA 现有 SSE 架构同构                        |
| D8  | 无自动超时（用户手动停）                      | **改为 30 分钟安全上限 + 手动停**                                                                                                                                                                                                            | VERA 无人值守场景多（scheduler），防僵尸               |
| D9  | 泄漏关键词 = 名册/证件号                    | **改为 VERA 敏感域**：资金账号/股东账号/交易密码/trade.db + 保留证件号正则                                                                                                                                                                                 | VERA 的机密是账户与策略参数                          |
| D10 | 先写 ADR 再写码，先写红测试再实现               | **照抄**：Task 1 写 ADR-0002，Task 3 TDD                                                                                                                                                                                               | VERA 既有惯例（docs/adr/0001）                  |
| D11 | IRX 用全局侧边栏抽屉承载助手                  | **改为留在研究 TAB 对话卡片内做通道开关**：①深度思考是"问法"不是"另一个工具"，勾选语义刚好；②会话条/消息/localStorage/vault 沉淀/BrainViz 渲染全部绑在现有卡片 DOM 上，独立 TAB = 整套状态复制一份，侧边栏 = 全站布局改造；③IRX 抽屉的价值锚点是 page\_context 注入（在任何页面问都知道你在看啥），VERA 一期没做注入，全局呼出价值打折；④单人本地应用，"随时呼出"非强需求 | 最小 diff 达成同一能力；抽屉+page\_context 列二期候选（§8） |
| D12 | IRX 留档只进 DB 表                     | **加码：DSH 回答同样沉淀 vault 对话归档**（archive\_exchange，2026-07-28 用户拍板"对话=宝贵思考必须沉淀"）；该调用原本藏在 ask\_brain 内部，DSH 分支绕过它，不显式补上就是悄悄破了铁律                                                                                                        | 审查第二轮抓出的真缺口                               |

## 3. 踩坑预案（IRX 实测坑 → 本计划对策）

| IRX 坑                                                                                      | 本计划对策                                                                        |
| ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| npm 上没有 rc.5、依赖树死锁 15 分钟                                                                   | **不碰 npm**：直接从 `E:\1target\IRX\dsh-runtime` 整体拷贝（手册卖点就是"拷走即迁移"）；备选才跑 pnpm 脚本 |
| provider 名 `deepseek` 报 NO\_ADAPTER，真名 `deepseek-official`                                 | settings.yaml 照抄 IRX 已验证配置，原样拷贝                                              |
| 本机全局 dsh.bat 硬编码 cd 到 checkout                                                             | 用 IRX 仓里的干净 shim（`%~dp0` 相对路径），不碰全局 shim                                     |
| 停止按钮时序 bug（channel 先于进程注册）                                                                 | 代码里**先注册** **`_runs[run_id]`** **再发 channel 事件**，并有对应测试                      |
| 测试挂死 120s（留档真连数据库）                                                                         | 测试全部注入 tmp sqlite + fake 子进程，任何路径不碰真 DB/真 DSH                                |
| （新疑点）会话日志实为 `session.jsonl.zstd` 压缩包，IRX 的 `_latest_session_log` 只 glob `*.jsonl`          | Task 2 冒烟时**实测确认日志格式**；代码两种后缀都认，遇 zstd 用 `zstandard` 解压（缺包且真遇到才报错）           |
| （VERA 既有坑，claude\_cli.py 注释在案）uvicorn `--reload` 把事件循环换成 Selector，Windows 上不支持 asyncio 子进程 | server.py 现状已是 Proactor 循环，直接满足；**调试 DSH 流程时不要开** **`--reload`**，否则子进程启动即炸   |
| （本计划自省）凭据随 robocopy 一起拷过来（home/.credentials.yaml 含 API key）                                | 这是"拷走即迁移"的预期行为，但意味着 `dsh-runtime/` **绝不外发/上传**；.gitignore 整目录兜底（Task 6）      |

## 4. 文件结构

| 文件                                           | 动作          | 职责                                                |
| -------------------------------------------- | ----------- | ------------------------------------------------- |
| `docs/adr/0002-dsh-deep-thinking-channel.md` | 新建          | 决策留痕（数据出网例外 + 检测型控制）                              |
| `dsh-runtime/`                               | 新建（拷贝）      | 便携运行时：node/app/home/workspace/dsh.cmd             |
| `scripts/setup_dsh_home.py`                  | 新建（从 IRX 改） | 运行时重建脚本（dry-run/--apply/--smoke），备将来 IRX 不在手边     |
| `brain/dsh_channel.py`                       | 新建          | **深模块本体**：run\_dsh/stop\_dsh/scan\_leak，其余全藏内部    |
| `tests/brain/test_dsh_channel.py`            | 新建          | 行为测试，全离线                                          |
| `research_api.py`                            | 修改          | stream 端点加 deep 分流 + 新增 `/api/research/chat/stop` |
| `tests/test_research_chat_api.py`            | 修改          | deep 路由 + stop 端点测试                               |
| `web/index.html`                             | 修改          | 勾选框 + 停止按钮                                        |
| `web/js/brain_chat.js`                       | 修改          | deep 参数、channel 事件、停止逻辑                           |
| `.gitignore`                                 | 修改          | dsh-runtime/ + 留档 db                              |
| `start_vera.bat`                             | 修改          | DSH 体检行                                           |
| `CHANGELOG.md`                               | 修改          | 变更记录                                              |

***

### Task 1: ADR-0002 决策留痕（先决策后写码）

**Files:**

- Create: `docs/adr/0002-dsh-deep-thinking-channel.md`

- [ ] **Step 1: 写 ADR**（照 docs/adr/0001 格式：状态/决策者/背景/决策/后果）

```markdown
# ADR-0002: 研究大脑 DSH 深度思考通道（数据出网例外 + 检测型控制）

- **状态**: 已接受 (Accepted) — 2026-09-04 用户拍板
- **决策者**: 用户
- **关联**: 复刻 IRX ADR-011（E:\1target\IRX\docs\adr\）

## 背景

研究 TAB 对话大脑（brain/claude_cli.py）已带全工具（Bash/Write/Edit，
--dangerously-skip-permissions，2026-08-01 用户拍板），上下文本就发给外部 LLM
provider。本次新增 DSH 通道（DeepSeek 官方 harness，便携运行时驻留仓内
dsh-runtime/）作为勾选制升级路径，**不扩大出网面**，但补齐检测型控制。

## 决策

1. 升级制路由：未勾选走现有大脑（零改动），勾选「🧠 深度思考」才进 DSH。
2. DSH 全工具面不裁（与现有大脑同级）。
3. 检测型控制为底线：每问全量留档 data/brain_dsh_runs.db（会话日志
   SHA-256 + 泄漏关键词扫描），leak_hits 非空即告警；**留档失败 = 显性失败**。
4. 应急开关 brain/dsh_channel.py 的 DSH_CHANNEL_ENABLED=False 一键停用。
5. dsh-runtime/ 整个不入 git（含 node 二进制/凭据/会话日志）。

## 后果

- 正面：停止按钮、便携部署、出网留痕三件套到位；成本走 deepseek-v4-flash 更低。
- 代价：每轮约 10s 冷启动 + 历史打包 token（摇醒机制，无会话续接）。
- 边界：单用户本地系统，例外成立；若未来多用户化必须重估。
```

- [ ] **Step 2: Commit**

```bash
git add docs/adr/0002-dsh-deep-thinking-channel.md
git commit -m "docs: ADR-0002 DSH 深度思考通道决策留痕"
```

***

### Task 2: 便携运行时部署 + 冒烟（从 IRX 拷贝，不碰 npm）

**Files:**

- Create: `dsh-runtime/`（拷贝自 IRX）

- Create: `scripts/setup_dsh_home.py`

- [ ] **Step 1: 从 IRX 整体拷贝运行时**（手册卖点：拷走即迁移）

```powershell
robocopy E:\1target\IRX\dsh-runtime E:\1target\VERA\dsh-runtime /E
Remove-Item -Recurse -Force E:\1target\VERA\dsh-runtime\home\sessions
```

预期：`dsh-runtime/{node,app,home,workspace}` 四层 + `dsh.cmd` 就位；sessions 清空（IRX 的会话记录不带过来）。

- [ ] **Step 1b: 重建 junction（执行时实测抓出的新坑，必做）**

IRX 的 `home/profiles/node_modules` 下 430 个条目是 junction（指向 app/node\_modules），robocopy 会把它们**实体化成真目录**，DSH 启动即炸（`exists and is not a symlink`）。拷贝后必须整体重建：

```powershell
$srcRoot = "E:\1target\IRX\dsh-runtime"; $dstRoot = "E:\1target\VERA\dsh-runtime"
function Rebuild($srcDir, $dstDir) {
  New-Item -ItemType Directory -Force -Path $dstDir | Out-Null
  foreach ($i in Get-ChildItem $srcDir -Force) {
    $dstPath = Join-Path $dstDir $i.Name
    if ($i.LinkType) {
      $t = ($i.Target | Select-Object -First 1) -replace [regex]::Escape($srcRoot), $dstRoot
      New-Item -ItemType Junction -Path $dstPath -Target $t | Out-Null
    } elseif ($i.PSIsContainer) { Rebuild $i.FullName $dstPath }
    else { Copy-Item $i.FullName $dstPath -Force }
  }
}
Remove-Item -Recurse -Force "$dstRoot\home\profiles\node_modules"
Rebuild "$srcRoot\home\profiles\node_modules" "$dstRoot\home\profiles\node_modules"
```

验收：重建后 junction 430 个、无指向 IRX 的残留（2026-09-04 实测通过）。

- [ ] **Step 2: 改 settings.yaml 注释归属**（内容不动，只改抬头一行）

`dsh-runtime/home/settings.yaml` 首行注释改为 `# 第二套 DSH（VERA 深度思考通道）最小配置`。`provider: deepseek-official` / `model: deepseek-v4-flash` 原样保留（IRX 踩坑 #3：名字写错报 NO\_ADAPTER）。

- [ ] **Step 3: 冒烟**（手册复刻步骤 4）

```powershell
E:\1target\VERA\dsh-runtime\dsh.cmd --profile headless "用一句话回答：1+1等于几？"
```

预期：exit 0、stdout 单行答案、stderr 空。**同时记录**：`dsh-runtime\home\sessions\` 下新生成的日志文件名后缀是 `.jsonl` 还是 `.jsonl.zstd`（决定 Task 3 的解压分支是否会被触发；两种代码都认，此步只为知情）。

- [ ] **Step 3b: 若日志是 .zstd，装解压依赖**

```powershell
python -m pip install zstandard
```

（仅冒烟确认日志为压缩格式时执行；纯 `.jsonl` 则跳过。不写入 pyproject 硬依赖——`_log_text` 按需 import，缺包且真遇到才显性报错。）

- [ ] **Step 4: 拷贝部署脚本备将来重建**

从 `E:\1target\IRX\scripts\setup_dsh_home.py` 拷到 `scripts/setup_dsh_home.py`，docstring 中 "IRX 深度思考通道" 改 "VERA 深度思考通道"，其余不动（脚本内路径全部相对 `REPO_ROOT`，天然便携）。

- [ ] **Step 5: Commit**

```bash
git add scripts/setup_dsh_home.py
git commit -m "chore: DSH 便携运行时部署脚本 (dsh-runtime 本体不入 git)"
```

***

### Task 3: `brain/dsh_channel.py` 深模块（TDD）

**Files:**

- Create: `tests/brain/test_dsh_channel.py`

- Create: `brain/dsh_channel.py`

- [ ] **Step 1: 先写失败测试**

```python
# -*- coding: utf-8 -*-
"""brain/dsh_channel.py 行为测试 — 全离线 (fake 子进程 + tmp sqlite)。

任何路径不碰真 DSH / 真 DB / 真 taskkill 有效目标。
"""
import asyncio
import json
import sys
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parent.parent.parent
sys.path.insert(0, str(ROOT))

import brain.dsh_channel as dsh  # noqa: E402


class _FakeProc:
    """假子进程: wait 阻塞直到 kill; communicate 返固定输出。"""

    def __init__(self, out=b"答案", err=b"", rc=0, hang=False):
        self._out, self._err, self._rc = out, err, rc
        self._hang = hang
        self.returncode = None
        self.pid = 99999
        self.killed = False

    async def wait(self):
        while self.returncode is None:
            if not self._hang:
                self.returncode = self._rc
            await asyncio.sleep(0.005)
        return self.returncode

    async def communicate(self):
        if self.returncode is None:
            self.returncode = self._rc
        return self._out, self._err

    def kill(self):
        self.killed = True
        self.returncode = self._rc if not self._hang else -9


def _patch_spawn(monkeypatch, proc):
    async def fake_spawn(task, env):
        return proc
    monkeypatch.setattr(dsh, "_spawn", fake_spawn)


def _patch_runtime(monkeypatch, tmp_path):
    monkeypatch.setattr(dsh, "DSH_BIN", tmp_path / "dsh.cmd")
    (tmp_path / "dsh.cmd").write_text("@echo off")
    monkeypatch.setattr(dsh, "DSH_HOME", tmp_path / "home")
    (tmp_path / "home").mkdir()
    monkeypatch.setattr(dsh, "DSH_WORKSPACE", tmp_path / "workspace")
    (tmp_path / "workspace").mkdir()


def run(coro):
    return asyncio.run(coro)


# ---- scan_leak 纯函数 ----

def test_scan_leak_keyword_and_idcard():
    assert "资金账号" in dsh.scan_leak("我的资金账号是 123")
    assert "证件号模式" in dsh.scan_leak("11010119900307777X")
    assert dsh.scan_leak("今天大盘怎么样") == []


# ---- _pack_task 历史打包 ----

def test_pack_task_no_history():
    assert dsh._pack_task("问题", None) == "问题"
    assert dsh._pack_task("问题", []) == "问题"


def test_pack_task_packs_recent_rounds_only():
    hist = ([{"role": "user", "content": f"问{i}"},
             {"role": "assistant", "content": f"答{i}"} for i in range(8)]
            + [{"role": "tool", "content": "忽略我"}])
    task = dsh._pack_task("新问题", hist)
    assert "【对话记录】" in task and "【问题】" in task
    assert "问0" not in task            # 超窗丢弃 (DSH_HISTORY_ROUNDS=5)
    assert "问3" in task and "答7" in task
    assert "忽略我" not in task          # 非 user/assistant 轮不打包


# ---- run_dsh 主路径 ----

def test_run_dsh_success_archives(monkeypatch, tmp_path):
    _patch_runtime(monkeypatch, tmp_path)
    _patch_spawn(monkeypatch, _FakeProc(out="答: 42".encode()))
    db = tmp_path / "runs.db"
    r = run(dsh.run_dsh("1+1?", db_path=db, poll_seconds=0.01))
    assert r["success"] is True and r["answer"] == "答: 42"
    import sqlite3
    row = sqlite3.connect(str(db)).execute(
        "SELECT run_id, question, answer, exit_code, stopped FROM brain_dsh_runs"
    ).fetchone()
    assert row[1] == "1+1?" and row[2] == "答: 42"
    assert row[3] == 0 and row[4] == 0


def test_run_dsh_nonzero_exit(monkeypatch, tmp_path):
    _patch_runtime(monkeypatch, tmp_path)
    _patch_spawn(monkeypatch, _FakeProc(out=b"", err="boom".encode(), rc=2))
    r = run(dsh.run_dsh("q", db_path=tmp_path / "r.db", poll_seconds=0.01))
    assert r["success"] is False and "exit 2" in r["answer"] and "boom" in r["answer"]


def test_run_dsh_archive_failure_is_hard_failure(monkeypatch, tmp_path):
    """留档失败 = 显性失败 (宁可不给答案也不出无痕答案)。"""
    _patch_runtime(monkeypatch, tmp_path)
    _patch_spawn(monkeypatch, _FakeProc())
    bad_db = tmp_path / "是个目录"   # connect 到目录必炸
    bad_db.mkdir()
    r = run(dsh.run_dsh("q", db_path=bad_db, poll_seconds=0.01))
    assert r["success"] is False
    assert ("留档" in r["answer"]) or ("异常" in r["answer"])


def test_run_dsh_stop_kills_tree(monkeypatch, tmp_path):
    _patch_runtime(monkeypatch, tmp_path)
    proc = _FakeProc(hang=True)
    _patch_spawn(monkeypatch, proc)
    db = tmp_path / "r.db"

    async def scenario():
        task = asyncio.create_task(
            dsh.run_dsh("q", run_id="run123", db_path=db, poll_seconds=0.01))
        await asyncio.sleep(0.05)
        assert dsh.stop_dsh("run123") is True
        return await task

    r = run(scenario())
    assert proc.killed is True
    assert r["success"] is False and "已停止" in r["answer"]
    import sqlite3
    stopped = sqlite3.connect(str(db)).execute(
        "SELECT stopped FROM brain_dsh_runs WHERE run_id='run123'").fetchone()
    assert stopped[0] == 1


def test_stop_dsh_unknown_run_id():
    assert dsh.stop_dsh("不存在的") is False


def test_run_dsh_disabled_channel(monkeypatch, tmp_path):
    monkeypatch.setattr(dsh, "DSH_CHANNEL_ENABLED", False)
    r = run(dsh.run_dsh("q", db_path=tmp_path / "r.db"))
    assert r["success"] is False and "已停用" in r["answer"]


def test_run_dsh_runtime_missing(monkeypatch, tmp_path):
    monkeypatch.setattr(dsh, "DSH_BIN", tmp_path / "没有.cmd")
    monkeypatch.setattr(dsh, "DSH_HOME", tmp_path / "也没有")
    r = run(dsh.run_dsh("q", db_path=tmp_path / "r.db"))
    assert r["success"] is False and "尚未部署" in r["answer"]


def test_run_dsh_leak_scan_records_hits(monkeypatch, tmp_path):
    """会话日志含敏感词 → leak_hits 落库。"""
    _patch_runtime(monkeypatch, tmp_path)
    sess = dsh.DSH_HOME / "sessions" / "s1"
    sess.mkdir(parents=True)
    (sess / "session.jsonl").write_text("...资金账号 12345...", encoding="utf-8")
    _patch_spawn(monkeypatch, _FakeProc())
    db = tmp_path / "r.db"
    run(dsh.run_dsh("q", db_path=db, poll_seconds=0.01))
    import sqlite3
    hits = sqlite3.connect(str(db)).execute(
        "SELECT leak_hits FROM brain_dsh_runs").fetchone()[0]
    assert "资金账号" in json.loads(hits)


def test_run_dsh_registers_before_events(monkeypatch, tmp_path):
    """IRX 踩坑 #5: on_line 回调触发时进程必须已注册 (停止随时可命中)。"""
    _patch_runtime(monkeypatch, tmp_path)
    _patch_spawn(monkeypatch, _FakeProc(hang=True))

    seen = {}

    async def scenario():
        async def on_line(line):
            seen["registered"] = "run99" in dsh._runs
        t = asyncio.create_task(dsh.run_dsh(
            "q", run_id="run99", on_line=on_line,
            db_path=tmp_path / "r.db", poll_seconds=0.01))
        await asyncio.sleep(0.05)
        dsh.stop_dsh("run99")
        await t

    run(scenario())
    assert seen["registered"] is True


def test_run_dsh_archives_to_vault_when_channel(monkeypatch, tmp_path):
    """D12: 传了 channel 就必须调 archive_exchange (对话沉淀铁律, 2026-07-28)。

    archive_exchange 自身松耦合不抛, 这里 monkeypatch 只验证"被调到、参数对"。
    """
    _patch_runtime(monkeypatch, tmp_path)
    _patch_spawn(monkeypatch, _FakeProc(out=b"a"))
    cap = {}
    monkeypatch.setattr(dsh, "archive_exchange",
                        lambda ch, q, r: cap.update({"ch": ch, "q": q,
                                                     "ans": r["answer"]}))
    run(dsh.run_dsh("问题X", channel="research_tab_c1",
                    db_path=tmp_path / "r.db", poll_seconds=0.01))
    assert cap == {"ch": "research_tab_c1", "q": "问题X", "ans": "a"}

    cap.clear()
    run(dsh.run_dsh("问题Y", db_path=tmp_path / "r.db", poll_seconds=0.01))
    assert cap == {}          # 不传 channel 不归档 (脚本/测试场景不污染 vault)
```

- [ ] **Step 2: 跑测试确认全红**

```powershell
python -m pytest tests/brain/test_dsh_channel.py -q
```

预期：ImportError（模块不存在）。

- [ ] **Step 3: 实现** **`brain/dsh_channel.py`**

```python
"""brain/dsh_channel.py — DSH 深度思考通道 (复刻 IRX ADR-011 架构, VERA 化).

外部接口仅 3 个符号: run_dsh / stop_dsh / scan_leak. research_api 是唯一调用方;
子进程生命周期 / 会话日志取证 / 留档落库全藏在内部.

"摇醒"机制 (IRX 手册 §3): DSH headless 是一次性程序, 每问摇醒一个新进程,
答完即灭; 多轮记忆靠调用方把近期对话打包进任务文本 (headless 无会话续接).

VERA 化差异 (对照 IRX src/irx/assistant/dsh.py):
- asyncio 子进程 (与 brain/claude_cli.py 同款), 不用同步 Popen + sleep 轮询;
  杀进程树复用 claude_cli._kill_tree (Windows 双坑已解决: 孙进程持管道 + taskkill /T).
- 留档落 SQLite data/brain_dsh_runs.db (VERA 无 Postgres); 留档失败 = 显性失败
  (宁可不给答案也不出无痕答案 — 检测型控制是底线不是装饰).
- 30 分钟安全上限防僵尸 (IRX 无超时纯手动停; VERA 有无人值守场景).
- 传 channel 时调用 brain.archive.archive_exchange 沉淀 vault 对话归档
  (2026-07-28 用户拍板铁律; 该调用原本藏在 ask_brain 内部, 本通道绕过它必须自补).
失败一律返 {"success": False, ...}, 不抛 (松耦合: DSH 挂了主大脑零感知).
"""
from __future__ import annotations

import asyncio
import hashlib
import json
import os
import re
import sqlite3
import time
import uuid
from pathlib import Path

from brain.archive import archive_exchange  # 对话沉淀 (松耦合, 自身不抛)
from brain.claude_cli import _kill_tree     # 复用: Windows 进程树双坑已解决
from utils.logger import get_logger
from utils.sysutil import project_root

logger = get_logger(__name__)

_ROOT = project_root()
DSH_RUNTIME = _ROOT / "dsh-runtime"
DSH_BIN = DSH_RUNTIME / "dsh.cmd"
DSH_HOME = DSH_RUNTIME / "home"
DSH_WORKSPACE = DSH_RUNTIME / "workspace"
DSH_DB = _ROOT / "data" / "brain_dsh_runs.db"

DSH_CHANNEL_ENABLED = True   # 应急总开关: False 一键停用, 主大脑零影响
DSH_PROGRESS_SECONDS = 2.0   # 等待动作提示轮询间隔
DSH_MAX_SECONDS = 1800       # 安全上限 (防僵尸; 正常深度思考 1-5 分钟)
DSH_HISTORY_ROUNDS = 5       # 多轮记忆打包轮数 (打进任务文本, headless 无会话续接)

# 泄漏扫描关键词 (单一来源): 命中 = 出网上下文含敏感域特征 → 告警记录
DSH_LEAK_KEYWORDS = ("资金账号", "股东账号", "交易密码", "trade.db")
_ID_CARD_RE = re.compile(r"\b\d{17}[\dXx]\b")

# 会话日志尾部事件 → 等待 UI 动作提示 (IRX 同款人话翻译)
_ACTION_HINTS = (
    ("user/message", "正在思考"),
    ("tool/call", "正在调用工具"),
    ("tool/result", "正在整理工具结果"),
    ("assistant/chunk", "正在撰写答复"),
    ("assistant/message", "正在撰写答复"),
    ("step/start", "正在推进"),
)

_runs: dict[str, asyncio.subprocess.Process] = {}
_stopped: set[str] = set()

_DDL = """
CREATE TABLE IF NOT EXISTS brain_dsh_runs (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    run_id           TEXT NOT NULL,
    question         TEXT NOT NULL,
    answer           TEXT,
    exit_code        INTEGER,
    stopped          INTEGER NOT NULL DEFAULT 0,
    elapsed_ms       INTEGER,
    session_log_path TEXT,
    context_sha256   TEXT,
    context_chars    INTEGER,
    leak_hits        TEXT,
    created_at       TEXT NOT NULL DEFAULT (datetime('now', 'localtime'))
);
CREATE INDEX IF NOT EXISTS idx_brain_dsh_runs_ts ON brain_dsh_runs (created_at DESC);
"""


# ---------------------------------------------------------------- 公开接口

def scan_leak(content: str) -> list[str]:
    """纯函数: 扫描文本, 返回命中的敏感域特征列表 (关键词 + 证件号模式)。"""
    hits = [kw for kw in DSH_LEAK_KEYWORDS if kw.lower() in content.lower()]
    if _ID_CARD_RE.search(content):
        hits.append("证件号模式")
    return hits


def stop_dsh(run_id: str) -> bool:
    """终止一次运行 (杀进程树)。返回是否找到了该运行。"""
    proc = _runs.get(run_id)
    if proc is None:
        return False
    _stopped.add(run_id)
    try:
        proc.kill()
    except ProcessLookupError:
        pass
    return True


async def run_dsh(question: str, history: list[dict] | None = None,
                  run_id: str | None = None, on_line=None,
                  channel: str | None = None,
                  db_path: Path | None = None,
                  poll_seconds: float = DSH_PROGRESS_SECONDS,
                  max_seconds: int = DSH_MAX_SECONDS) -> dict:
    """摇醒一次 DSH 深度思考: 子进程执行 → 周期动作提示 → 收尾留档。

    返回与 brain.claude_cli.ask_brain 同款契约
    {answer, success, low_confidence, warnings, session_id}, 路由层/前端零适配。
    history 为 [{role, content}] (仅 user/assistant 轮被打包进任务文本)。
    on_line: async callable(line:str), SSE 进度回调; None=静默。
    channel: 传了就把问答沉淀进 vault 对话归档 (archive_exchange); None=不归档。
    """
    result = {"answer": "", "success": False, "low_confidence": False,
              "warnings": [], "session_id": None}
    if not DSH_CHANNEL_ENABLED:
        result["answer"] = "深度思考通道已停用 (DSH_CHANNEL_ENABLED=False)。取消勾选可用普通模式提问。"
        return result
    if not DSH_BIN.exists() or not DSH_HOME.is_dir():
        result["answer"] = ("深度思考通道尚未部署: 缺 dsh-runtime/ 便携运行时。"
                            "部署见 docs/plan/2026-09-04_研究大脑DSH深度思考通道_计划书.md Task 2。")
        return result

    run_id = run_id or uuid.uuid4().hex[:12]
    task = _pack_task(question, history)
    env = {**os.environ, "DSH_HOME": str(DSH_HOME)}
    t0 = time.monotonic()
    try:
        proc = await _spawn(task, env)
    except Exception as e:
        result["answer"] = f"深度思考通道启动失败: {type(e).__name__}: {e}"
        return result

    # 先注册进程再发任何事件 (IRX 踩坑 #5: 保证任意时刻停止都能命中)
    _runs[run_id] = proc
    stopped = False
    try:
        if on_line:
            await on_line(f"[DSH] 已摇醒深度思考进程 (run_id={run_id})，答完自动退出…")
        while True:
            if run_id in _stopped:
                stopped = True
                break
            try:
                # 不加 shield: wait_for 超时只取消"等待协程", 进程照跑;
                # 加 shield 会让每个轮询周期堆积一个挂起 task (审查第一轮抓出)
                await asyncio.wait_for(proc.wait(), timeout=poll_seconds)
                break  # 进程自然结束
            except TimeoutError:
                pass
            if time.monotonic() - t0 > max_seconds:
                await _kill_tree(proc)
                _archive(run_id, question, "", None, True, t0, db_path)
                result["answer"] = f"深度思考超时 (> {max_seconds}s)，已终止"
                return result
            if on_line:
                await on_line(f"[DSH] {_tail_action()} · {int(time.monotonic() - t0)}s")

        out, err = await proc.communicate()
        answer = (out or b"").decode("utf-8", "replace").strip()
        rc = proc.returncode
        hits = _archive(run_id, question, answer, rc, stopped, t0, db_path)  # 留档失败=抛 → 显性失败
        if hits:
            result["warnings"].append(f"泄漏扫描命中 {hits}（已告警留档）")
        if stopped:
            result["answer"] = "已停止: 本次深度思考被手动终止。"
            return result
        if rc == 0 and answer:
            result.update({"answer": answer, "success": True})
        else:
            lines = (err or b"").decode("utf-8", "replace").strip().splitlines()
            hint = lines[0][:120] if lines else "无 stderr 输出"
            result["answer"] = (f"深度思考未能完成 (exit {rc}): {hint}\n"
                                "可稍后重试, 或取消勾选走普通模式。")
        return result
    except Exception as e:
        # 留档失败等异常 → 显性失败 (不出无痕答案), 仍松耦合不炸 server
        logger.warning(f"DSH 通道异常: {e}", exc_info=True)
        result["answer"] = f"深度思考异常 (含留档失败): {type(e).__name__}: {e}"
        return result
    finally:
        if channel:
            # D12: vault 对话沉淀 (铁律 2026-07-28)。archive_exchange 松耦合不抛。
            # 停止/失败也归档 —— archive.py 惯例: 失败标 [失败], 试错也是思考
            archive_exchange(channel, question, result)
        _runs.pop(run_id, None)
        _stopped.discard(run_id)


# ---------------------------------------------------------------- 内部实现

async def _spawn(task: str, env: dict):
    """起 DSH headless 子进程 (独立函数方便测试 monkeypatch)。

    .cmd 经 create_subprocess_exec 可跑 (VERA 既有证据: claude CLI 在
    Windows 同为 .cmd, claude_cli.py 生产在跑)。
    """
    return await asyncio.create_subprocess_exec(
        str(DSH_BIN), "--profile", "headless", task,
        stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.PIPE,
        cwd=str(DSH_WORKSPACE), env=env)


def _pack_task(question: str, history: list[dict] | None) -> str:
    """多轮记忆: 近期对话 (仅 user/assistant 轮) 打包进任务文本。"""
    if not history:
        return question
    lines = []
    for m in history[-(DSH_HISTORY_ROUNDS * 2):]:
        role = m.get("role")
        if role in ("user", "assistant"):
            label = "用户" if role == "user" else "助手"
            lines.append(f"{label}: {m.get('content') or ''}")
    if not lines:
        return question
    return "【对话记录】\n" + "\n".join(lines) + f"\n\n【问题】\n{question}"


def _latest_session_log() -> Path | None:
    """定位 DSH home 下最新会话日志 (= 本次运行的出网上下文全集)。

    .jsonl 与 .jsonl.zstd 都认 (IRX 仓实测为 zstd 压缩, 其 glob 只认
    .jsonl —— 本实现修正该疑点)。
    """
    d = DSH_HOME / "sessions"
    if not d.is_dir():
        return None
    logs = [p for p in d.rglob("*")
            if p.is_file() and (p.name.endswith(".jsonl")
                                or p.name.endswith(".jsonl.zstd"))]
    return max(logs, key=lambda p: p.stat().st_mtime) if logs else None


def _log_text(raw: bytes, path: Path) -> str:
    """会话日志 bytes → 文本 (zstd 压缩则解压; 缺 zstandard 包且真遇到才报错)。"""
    if path.name.endswith(".zstd"):
        import zstandard  # 可选依赖: 仅压缩日志需要
        raw = zstandard.ZstdDecompressor().stream_reader(raw).read()
    return raw.decode("utf-8", errors="ignore")


def _tail_action() -> str:
    """读会话日志尾部, 把最近事件翻译成等待 UI 动作提示 (zstd 压缩时退化为"工作中")。"""
    log = _latest_session_log()
    if log is None:
        return "启动中"
    try:
        with log.open("rb") as f:
            f.seek(0, os.SEEK_END)
            size = f.tell()
            f.seek(max(0, size - 4096))
            tail = f.read().decode("utf-8", errors="ignore")
    except OSError:
        return "工作中"
    for marker, hint in _ACTION_HINTS:
        if marker in tail:
            return hint
    return "工作中"


def _archive(run_id: str, question: str, answer: str, exit_code: int | None,
             stopped: bool, t0: float, db_path: Path | None = None) -> list:
    """每问全量留档: 会话日志哈希 + 泄漏扫描 + 落 SQLite。

    失败直接抛 (显性失败)。返回泄漏命中列表 (供调用方写进 warnings 让前端可见)。"""
    db = db_path or DSH_DB
    log = _latest_session_log()
    ctx_hash, ctx_chars, hits = None, None, []
    if log is not None:
        raw = log.read_bytes()
        ctx_hash = hashlib.sha256(raw).hexdigest()
        ctx_chars = len(raw)
        hits = scan_leak(_log_text(raw, log))
    db.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(db))
    try:
        conn.executescript(_DDL)
        conn.execute(
            "INSERT INTO brain_dsh_runs (run_id, question, answer, exit_code,"
            " stopped, elapsed_ms, session_log_path, context_sha256,"
            " context_chars, leak_hits) VALUES (?,?,?,?,?,?,?,?,?,?)",
            (run_id, question[:500], answer[:20000], exit_code, int(stopped),
             int((time.monotonic() - t0) * 1000),
             str(log) if log else None, ctx_hash, ctx_chars,
             json.dumps(hits, ensure_ascii=False) if hits else None))
        conn.commit()
    finally:
        conn.close()
    if hits:
        logger.warning(f"DSH 泄漏告警 run={run_id}: 命中 {hits}")
    return hits
```

- [ ] **Step 4: 跑测试确认全绿**

```powershell
python -m pytest tests/brain/test_dsh_channel.py -q
```

预期：12 passed。

- [ ] **Step 5: Commit**

```bash
git add brain/dsh_channel.py tests/brain/test_dsh_channel.py
git commit -m "feat(brain): DSH 深度思考通道深模块 (run_dsh/stop_dsh/scan_leak + 留档)"
```

***

### Task 4: 路由分流 + 停止端点（research\_api.py）

**Files:**

- Modify: `research_api.py`（stream 端点加 deep 分支 + 新 stop 端点）

- Modify: `tests/test_research_chat_api.py`

- [ ] **Step 0: 先修现有测试 helper（审查第一轮抓出的前置 bug）**

现有 `_mock_brain` 的 `fake_ask` 签名是 `(question, session_id=None, timeout=120, max_turns=8, channel="default")`，**没有** **`on_line`** **参数**；而 stream 端点调用 `ask_brain(..., on_line=on_line)` —— 不补这个参数，任何 stream 端点测试都会 TypeError（失败原因跑偏）。把 `tests/test_research_chat_api.py` 的 helper 改为：

```python
def _mock_brain(monkeypatch, resp=None, capture=None):
    async def fake_ask(question, session_id=None, timeout=120, max_turns=8,
                       channel="default", on_line=None):
        if capture is not None:
            capture.update({"question": question, "channel": channel,
                            "timeout": timeout, "max_turns": max_turns})
        return resp or {"answer": "答: 据 `kg/graph.db` 共 3 条边",
                        "success": True, "low_confidence": False,
                        "warnings": [], "session_id": "s-1"}
    monkeypatch.setattr(brain_cli, "ask_brain", fake_ask)
```

- [ ] **Step 1: 先写失败测试**（追加到 tests/test\_research\_chat\_api.py）

```python
def test_chat_stream_deep_routes_to_dsh(client, monkeypatch):
    import re as _re

    import brain.dsh_channel as dsh
    cap = {}

    async def fake_run(question, history=None, run_id=None, on_line=None,
                       channel=None, **kw):
        cap.update({"question": question, "history": history,
                    "run_id": run_id, "channel": channel})
        return {"answer": "DSH 答", "success": True, "low_confidence": False,
                "warnings": [], "session_id": None}

    monkeypatch.setattr(dsh, "run_dsh", fake_run)
    r = client.post("/api/research/chat/stream", json={
        "question": "深度问题", "conv": "c1", "deep": True,
        "history": [{"role": "user", "content": "上一问"}]})
    assert r.status_code == 200
    assert cap["history"] == [{"role": "user", "content": "上一问"}]
    assert cap["channel"] == "research_tab_c1"      # D12: vault 沉淀要用
    assert _re.fullmatch(r"[0-9a-f]{12}", cap["run_id"])  # 服务端生成 12 位 hex
    assert f'"run_id": "{cap["run_id"]}"' in r.text       # channel 事件带同一 run_id
    assert "DSH 答" in r.text


def test_chat_stream_no_deep_ignores_dsh(client, monkeypatch):
    """不勾选 = 现有路径零改动 (fastpath/ask_brain), DSH 不被触碰。"""
    import brain.dsh_channel as dsh

    async def boom(*a, **k):
        raise AssertionError("不该调 DSH")

    monkeypatch.setattr(dsh, "run_dsh", boom)
    _mock_brain(monkeypatch)
    import brain.fastpath as fp

    async def no_fast(*a, **k):
        return None

    monkeypatch.setattr(fp, "try_stock_diagnosis", no_fast)
    monkeypatch.setattr(fp, "try_market_brief", no_fast)
    r = client.post("/api/research/chat/stream",
                    json={"question": "普通问题", "conv": "c1"})
    assert r.status_code == 200 and "答:" in r.text


def test_chat_stop_calls_dsh(client, monkeypatch):
    import brain.dsh_channel as dsh
    called = {}

    def fake_stop(rid):          # 审查第一轮: lambda 里 setdefault 返回的是值不是 True
        called["rid"] = rid
        return True

    monkeypatch.setattr(dsh, "stop_dsh", fake_stop)
    r = client.post("/api/research/chat/stop", json={"run_id": "abc123"})
    assert r.json()["success"] is True and called["rid"] == "abc123"
```

- [ ] **Step 2: 跑测试确认红**

```powershell
python -m pytest tests/test_research_chat_api.py -q
```

预期：3 个新测试失败（路由未实现）。

- [ ] **Step 3: 改 research\_api.py**

文件头部 import 区加 `import uuid`。

`research_chat_stream` 内，`question, err = _extract_question(body)` 之后加：

```python
    deep = bool(body.get("deep"))
    run_id = uuid.uuid4().hex[:12] if deep else None
    history = body.get("history") if deep else None
```

`generate()` 开头（queue 定义之后）加 channel 事件：

```python
    async def generate():
        queue: _aio.Queue = _aio.Queue()
        async def on_line(line: str):
            await queue.put(line)

        if run_id:
            # 先发 channel 事件 (前端拿 run_id 才能点停止);
            # 此刻 dsh_channel.run_dsh 内部已先注册进程 (IRX 踩坑 #5)
            yield f"data: {_json.dumps({'type': 'channel', 'channel': 'dsh', 'run_id': run_id}, ensure_ascii=False)}\n\n"
```

`_run()` 开头加 deep 分流（在 fastpath 之前，勾选即跳过所有快路径——升级制路由）：

```python
        async def _run():
            if deep:
                # 深度思考通道: 勾选即走 DSH, 跳过 fastpath/claude 大脑;
                # channel 照传 (D12: run_dsh 内部据此沉淀 vault 对话归档)
                from brain import dsh_channel
                return await dsh_channel.run_dsh(
                    question, history=history, run_id=run_id, on_line=on_line,
                    channel=_channel_of(body))
            # 2026-08-13 提速: ...（原有 fastpath 代码不动）
```

文件末尾加停止端点：

```python
@router.post("/api/research/chat/stop")
async def research_chat_stop(body: dict):
    """停止一次深度思考 (杀进程树)。run_id 非法/不存在 → success=False。"""
    from brain import dsh_channel
    run_id = (body.get("run_id") or "")[:12]
    return {"success": bool(run_id) and dsh_channel.stop_dsh(run_id)}
```

注：`/api/research/chat`（非流式旧端点）一期不加分流——前端 SSE 失败降级时 deep 问题自然回落 claude 大脑，属于可接受降级。

- [ ] **Step 4: 跑测试确认绿**

```powershell
python -m pytest tests/test_research_chat_api.py -q
```

预期：全部通过（旧 5 个 + 新 3 个）。

- [ ] **Step 5: Commit**

```bash
git add research_api.py tests/test_research_chat_api.py
git commit -m "feat(research): 深度思考勾选分流 + /api/research/chat/stop 停止端点"
```

***

### Task 5: 前端勾选框 + 停止按钮

**Files:**

- Modify: `web/index.html`（约 L1012-1016 输入行）

- Modify: `web/js/brain_chat.js`

- [ ] **Step 1: index.html 输入行加勾选框与停止按钮**

在 `brainSendBtn` 按钮前插入：

```html
        <label style="font-size:12px;color:var(--text2);display:flex;align-items:center;gap:4px;white-space:nowrap" title="走独立 DSH 深度思考进程 (deepseek-v4-flash): 更慢, 会自己写代码/读文件/联网; 每问全量留档">
          <input type="checkbox" id="brainDeepChk">🧠 深度思考
        </label>
```

在 `brainResetBtn` 后插入：

```html
        <button id="brainStopBtn" class="btn" style="padding:8px 12px;font-size:11px;display:none" title="终止本次深度思考 (杀进程)">停止</button>
```

- [ ] **Step 2: brain\_chat.js 接入**

`send()` 函数内 `var q = input.value.trim();` 之后加：

```js
    var deepChk = $('brainDeepChk');
    var deep = !!(deepChk && deepChk.checked);
```

fetch body 改为携带 deep 与 history（勾选时把该会话最近 10 条消息打包——DSH 摇醒机制无会话记忆）：

```js
    var bodyObj = { question: q, conv: convId };
    if (deep) {
      bodyObj.deep = true;
      bodyObj.history = msgs.filter(function (m) { return m.role === 'user' || m.role === 'brain'; })
        .slice(-10)
        .map(function (m) { return { role: m.role === 'user' ? 'user' : 'assistant', content: m.text }; });
    }
```

并把 `body: JSON.stringify({ question: q, conv: convId })` 改为 `body: JSON.stringify(bodyObj)`。

SSE 事件循环里 `if (d.type === 'line')` 分支前加 channel 分支：

```js
            if (d.type === 'channel') {
              var p0 = pending[convId];
              if (p0) { p0.runId = d.run_id; renderStatus(); }
              continue;
            }
```

`renderStatus()` 里 `var p = pending[activeId];` 之后加停止按钮显隐（注意：`p` 在这一行才定义，插早了会引用未定义变量——审查第一轮抓出）：

```js
    var stopBtn = $('brainStopBtn');
    if (stopBtn) stopBtn.style.display = (p && p.runId) ? '' : 'none';
```

`init()` 里 `$('brainResetBtn').addEventListener('click', resetConv);` 之后加停止按钮绑定（点停止时**先把气泡写成"已停止"再 cancel reader**——否则气泡会残留一屏 `[DSH] …` 进度行，审查第一轮抓出的 UX 坑）：

```js
    var stopBtn = $('brainStopBtn');
    if (stopBtn) stopBtn.addEventListener('click', function () {
      var p = pending[activeId];
      if (!p || !p.runId) return;
      var m = loadMsgs(activeId);
      var last = m[m.length - 1];
      if (last && last.streaming) {
        last.text = '已停止: 本次深度思考被手动终止。';
        delete last.streaming;
        saveMsgs(activeId, m);
        renderMessages();
      }
      fetch('/api/research/chat/stop', {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ run_id: p.runId }),
      }).catch(function () { /* 松耦合 */ });
      if (p.reader) p.reader.cancel().catch(function () {});
    });
```

`send()` 的 fetch reader 存入 pending（供停止取消）——`var reader = resp.body.getReader();` 之后加：

```js
      var p3 = pending[convId];
      if (p3) p3.reader = reader;
```

- [ ] **Step 2b: 通道徽标（让人知道这条答案是哪条通道产的，D11 发现性补强）**

`send()` 里 streaming 占位 push 行改为带 `via` 字段：

```js
    msgs.push({ role: 'brain', text: '思考中…', lowConf: false, warnings: [], streaming: true, via: deep ? 'dsh' : 'claude' });
```

`bubbleHtml()` 里 `'大脑' + badge` 改为：

```js
      '<div style="font-size:12px;color:var(--text2);margin-bottom:4px">' + (m.via === 'dsh' ? '大脑 · 🧠DSH' : '大脑') + badge + '</div>' +
```

- [ ] **Step 3: 浏览器人工验收**（无 JS 测试基建覆盖此文件，照 VERA 惯例人工验证）

1. `python server.py` 启动，打开研究 TAB。
2. 不勾选提问 → 走原大脑（行为与之前一致）。
3. 勾选提问 → 状态栏出现 `[DSH] 已摇醒…` + 动作提示轮转；回答带完整全文。
4. 勾选提问后点「停止」→ 数秒内气泡显示"已停止"。
5. 追问 → 第二轮回答能看出记得第一轮内容（历史打包生效）。

- [ ] **Step 4: Commit**

```bash
git add web/index.html web/js/brain_chat.js
git commit -m "feat(web): 研究 TAB 深度思考勾选框 + 停止按钮"
```

***

### Task 6: 收尾（gitignore / 启动体检 / 变更记录 / 全量回归）

**Files:**

- Modify: `.gitignore`

- Modify: `start_vera.bat`

- Modify: `CHANGELOG.md`

- [ ] **Step 1: .gitignore 追加**

```gitignore
# DSH 深度思考便携运行时 (含 node 二进制/凭据/会话日志, 不入 git;
# 重建: 从 IRX 拷贝或 python scripts/setup_dsh_home.py --apply) — 2026-09-04
dsh-runtime/
data/brain_dsh_runs.db
```

- [ ] **Step 2: start\_vera.bat 体检行**（echo 三进程之后、pause 之前插入）

```bat
if exist "%~dp0dsh-runtime\dsh.cmd" (echo [体检] DSH 深度思考通道: 已部署) else (echo [体检] DSH 深度思考通道: 未部署, 研究 TAB 勾选框不可用)
```

注意：只提示不启动任何进程（摇醒机制，无常驻）。

- [ ] **Step 3: CHANGELOG.md 追加条目**（照仓库实际格式——`## 日期 — 主题` 节 + 表格，不是散 bullet，审查第一轮抓出）

```markdown
## 2026-09-04 — 研究大脑 DSH 深度思考通道

**决策入口**: [docs/adr/0002-dsh-deep-thinking-channel.md](docs/adr/0002-dsh-deep-thinking-channel.md)
**计划书**: [docs/plan/2026-09-04_研究大脑DSH深度思考通道_计划书.md](docs/plan/2026-09-04_研究大脑DSH深度思考通道_计划书.md)

### 关键改动一览

| 主题 | 关键改动 | 影响 |
|---|---|---|
| DSH 深度思考通道 | 新建 `brain/dsh_channel.py` (run_dsh/stop_dsh/scan_leak) + 便携运行时 `dsh-runtime/` (不入 git) | 研究 TAB 勾选「🧠 深度思考」走独立 DSH 子进程 (deepseek-v4-flash, 全工具面) |
| 路由 + 停止 | `research_api.py` stream 端点 deep 分流 + `/api/research/chat/stop` | 未勾选路径零改动; 跑飞了可手动杀进程树 |
| 检测型控制 | 每问全量留档 `data/brain_dsh_runs.db` (会话日志 SHA-256 + 泄漏关键词扫描), 留档失败=显性失败 | 出网上下文全部记账, 敏感信息出网即告警 |
| 对话沉淀 | run_dsh 传 channel 时调 archive_exchange | DSH 回答同样进 vault, 不破 2026-07-28 铁律 |
| 测试 | +16 (tests/brain/test_dsh_channel.py 13 + tests/test_research_chat_api.py 3) | 全离线, 不碰真 DSH/真 DB |
```

- [ ] **Step 4: 全量回归**

```powershell
python -m pytest -q
```

预期：既有全部通过 + 新增 15 个（12 通道 + 3 路由），0 失败。

- [ ] **Step 5: lint**

```powershell
python -m ruff check brain/dsh_channel.py research_api.py tests/brain/test_dsh_channel.py tests/test_research_chat_api.py
```

预期：无输出（通过）。

- [ ] **Step 6: Commit**

```bash
git add .gitignore start_vera.bat CHANGELOG.md
git commit -m "chore: DSH 通道收尾 (gitignore/启动体检/CHANGELOG)"
```

***

## 5. 验收标准（对照 IRX §8）

- [ ] 红绿证据：Task 3/4 先红（ImportError/断言失败）后绿

- [ ] 全量回归：既有测试 0 失败

- [ ] 冒烟实测：便携运行时 headless 单问 exit 0、答案正确、耗时记录

- [ ] 留档证据：`data/brain_dsh_runs.db` 每次勾选提问都有一行，含 context\_sha256

- [ ] 沉淀证据：勾选提问后 `vera_obs_vault/对话沉淀/` 对应会话 md 追加该问答（与普通提问同一文件）

- [ ] 停止证据：深度思考进行中点停止，进程树被杀、前端数秒内复位

- [ ] 降级证据：`DSH_CHANNEL_ENABLED=False` 或删掉 `dsh-runtime/` 时，勾选提问得到可读错误而非崩溃，不勾选路径完全无感

## 6. 自检记录（写计划时自查）

- 规格覆盖：IRX 手册 §4 实施清单 13 项 —— dsh.py→Task 3 ✓、config 参数区→模块顶部常量（VERA 无中央 config.py，照 claude\_cli.py 惯例）✓、路由→Task 4 ✓、前端→Task 5 ✓、留档表→Task 3 `_DDL`（Postgres→SQLite 适配）✓、部署脚本→Task 2 ✓、测试→Task 3/4 ✓、ADR→Task 1 ✓、gitignore→Task 6 ✓、START.BAT 体检→Task 6 ✓。数据资产清单登记 VERA 无对应物（docs/p0 是 IRX 的），由 ADR-0002 承担。

- 占位符扫描：无 TBD/TODO；所有代码步骤含完整代码。

- 类型一致：`run_dsh` 返回契约与 `ask_brain` 对齐（answer/success/low\_confidence/warnings/session\_id）；`stop_dsh(run_id)` 与路由 `body.run_id` 一致；SSE 事件 type 取值 channel/line/done 与前端分支一一对应；`archive_exchange(channel, question, result)` 签名与 brain/archive.py:137 一致。

## 7. What already exists（复用清单，不重造）

| 已有                                                       | 本计划怎么用                                    |
| -------------------------------------------------------- | ----------------------------------------- |
| `brain/claude_cli.py:_kill_tree`（Windows 进程树双坑已解决）       | 直接 import 复用，停止/超时杀树不写第二份                 |
| `research_api.py` SSE 桥（asyncio.Queue + on\_line，v2 H-1） | deep 分支复用同一桥，channel 事件走同一 yield 通道       |
| `brain/archive.py:archive_exchange`（松耦合不抛）               | run\_dsh 传 channel 时调用，DSH 回答进同一 vault 文件 |
| `brain/fastpath.py` 快路径                                  | 不动；勾选 deep 显式跳过（升级制路由的本义）                 |
| `_channel_of(body)`（conv→channel 映射）                     | deep 分支照传，沉淀/会话归属口径一致                     |
| IRX `dsh-runtime/` 整个目录                                  | robocopy 直接拷，不碰 npm/pnpm                  |
| IRX `scripts/setup_dsh_home.py`                          | 原样拷贝改 docstring，路径全相对 REPO\_ROOT 天然便携     |

## 8. NOT in scope（明确不做，附理由）

- **全局侧边栏抽屉 / 独立 TAB**：D11 已论证。二期若出现"看回测结果时随手问"高频场景再做，前置条件是 brain\_chat.js 模块化 + page\_context 注入。

- **page\_context 注入**（IRX run() 有此参数）：没有全局呼出就没有注入对象；且 VERA 页面的"上下文"是回测结果/持仓等结构化数据，注入口径需要专门设计，单独立项。

- **真流式（ACP 协议）**：与 IRX 一期口径一致——DSH headless stdout 结束才出全文，动作提示轮询已够告知"活着"；ACP 是 DSH 官方协议升级，等它稳定。

- **非流式端点** **`/api/research/chat`** **的 deep 分流**：SSE 失败降级时 deep 问题回落 claude 大脑（可读答案，非崩溃），属可接受降级；若后续要补齐，只需在旧端点加同款 if。

- **zstandard 写入 pyproject 硬依赖**：按需 import，缺包且真遇到压缩日志才显性报错；不为可选路径加重依赖面。

- **DSH 通道的语义索引/双链以外的 vault 增强**：archive\_exchange 自带 \_refresh\_links + 索引同步，白拿不谢。

## 9. 审阅记录（2026-09-04 两轮自审，逐行验证）

**第一轮（代码级，对照 VERA 真实源码逐行核）——10 个发现全部已修入计划：**

1. \[P1] `tests/test_research_chat_api.py:_mock_brain` 的 fake\_ask 无 `on_line` 参数，stream 端点调用必 TypeError → Task 4 新增 Step 0 先修 helper。
2. \[P1] vault 沉淀缺口：`archive_exchange` 只在 `ask_brain` 内部调用（claude\_cli.py:133），DSH 分支绕过它 → 破 2026-07-28 铁律。新增 D12 + run\_dsh `channel` 参数 + finally 归档 + 测试。
3. \[P2] `test_chat_stop_calls_dsh` 的 lambda `setdefault(...) or True` 实际返回字符串 "abc123"，`is True` 断言必挂 → 改为 def。
4. \[P2] 留档失败测试断言 `(A and B) or C` 运算符优先级错误 → 加括号。
5. \[P2] 轮询循环 `asyncio.shield(proc.wait())` 每个周期堆积一个挂起 task → 去 shield（wait\_for 取消等待协程是安全的，进程照跑）。
6. \[P3] 测试计数错误（10 → 实际 11，加 vault 测试后 12；Task 6 总计 13 → 15）。
7. \[P3] deep 路由测试断言过弱（`"run123" not in cap` 永真）→ 改为 12 位 hex fullmatch + SSE 体含同一 run\_id + channel 断言。
8. \[P3] renderStatus 插入点写错（`var st` 之后引用未定义的 `p`）→ 改为 `var p = ...` 之后。
9. \[P3] 停止按钮只 cancel reader，气泡残留 `[DSH] …` 进度行 → 先写"已停止"再 cancel。
10. \[P3] CHANGELOG 条目格式与仓库实际（`## 日期 — 主题` 节+表格）不符 → 重写。

**第二轮（一致性/产品形态）——5 个发现全部已修入计划：**

1. \[P1] UI 形态未决策：独立 TAB vs 侧边栏 vs 留卡片 → 新增 D11，**留卡片做通道开关**（理由四条见决策表），抽屉+page\_context 列 §8 二期候选。
2. \[P2] 踩坑预案漏 VERA 既有坑：uvicorn `--reload` 的 Selector 循环不支持 Windows asyncio 子进程（claude\_cli.py 注释在案）→ 已补行。
3. \[P2] 凭据随 robocopy 拷贝的安全提示缺失 → 已补行（绝不外发 + gitignore 兜底）。
4. \[P3] zstandard 只在冒烟确认压缩格式后才需要装 → Task 2 补 Step 3b 条件安装。
5. \[P3] 深度思考的回答与普通回答气泡无区分 → Step 2b 通道徽标（大脑 · 🧠DSH）。

