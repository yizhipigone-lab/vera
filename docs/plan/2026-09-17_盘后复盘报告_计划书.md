# 2026-09-17 盘后复盘报告 计划书

> 入口：用户 2026-09-17 拍板「做复盘报告（按『页面合、模块分』那套）」。
> 一句话目标：把**市场盘面**接进已有的盘后日报，补上「需要你留意的清单」，
> 多开两路投递，并让大盘位置页签能看到复盘全文。

---

## 1. 【更正】我上次的判断是错的

我在 2026-09-17 的系统优化评估里说过：

> 「brain/sentiment_pipeline.py 和 trade/asset_gapfill.py 的数据都是现成的，
> 缺的是一个定时串起来的脚本。」

**这句话不准确，方向也偏了。** 实际去看代码之后：

**盘后日报早就存在，而且在跑。** `trade_main.py` 第 970~1069 行的 `TradeApp._notify_daily`，
搭 15:05 EOD 归档的车，已经推飞书并落库：

| 已有内容 | 证据 |
|---|---|
| 总资产 / 市值 / 现金 / 持仓只数 | `trade_main.py:1006~1011` |
| 当日盈亏（金额 + 比例，基准=昨日日终资产） | `trade_main.py:990~1003`（含 2026-08-13 基准 bug 修复注释） |
| 当日交易摘要（买卖笔数 / 成交额 / 已实现盈亏 / 胜率） | `trade/daily_report.py::build_trade_summary` |
| 卖出明细 + **卖飞信号**（盘中最高涨幅 vs 卖出时点涨幅） | `trade/daily_report.py::enrich_sell_quotes` |
| 交易明细混排（超 12 笔折叠） | 同上 |
| 浮盈（市值 − 持仓成本） | `trade_main.py:1031~1034` |
| 仓位变动（新进/清仓/加仓/减仓） | `trade/daily_report.py::diff_positions` |
| **ETF 轮动信号**（逐份目标腿/动量/entry_high） | `trade_main.py:1042~1057`（2026-09-16 三份错峰已支持） |
| 推飞书 + 落 `daily_report` 表（web 回看同源） | `trade/notifier.py::_build_daily_card` + `store.daily_report.save` |
| LLM 人话复盘（3~5 句，快速档） | `trade/llm_review.py`（2026-08-15，含卖飞识别） |
| 详尽档可配（full / summary）、AI 复盘可开关 | `trade/config.py::FeishuConfig`（`daily_report_level` / `ai_review`） |

**所以：账户那一半（持仓表现/盈亏/成交）不需要新建，重复造就是「第三份」。**
本计划的任务是**补缺口**，不是重写日报。

## 2. 真正的缺口（实测确认，四条）

| # | 缺口 | 现状证据 |
|---|---|---|
| **G1** | **市场那一半完全没有**。日报只有"我的账户"，没有"大盘在什么位置" | `_notify_daily` 组装的 payload 字段里没有任何市场指标；今天刚建好的大盘位置子系统（`core/market_position_runner.py`）没接进去 |
| **G2** | **只推飞书一路**。邮箱那两路没接 | `_notify_daily` 只调 `self._notifier.notify_daily`；`tools/send_email.py`（SMTP 已配 `SMTP_USER`/`SMTP_PASSWORD`）与 `tools/send_report_workbuddy.py` 都没被调用 |
| **G3** | **PC 端没有"今日复盘"入口**。payload 落库了但大盘位置页签看不到 | `daily_report` 表有数据，但第 10 个页签「大盘位置」只有体温表，没有当日复盘 |
| **G4** | **"最该提醒的"没提醒**：持仓中**距止损线只剩多少**没有 | payload 有仓位变动和轮动信号，但没有"这只票离止损线只有 2%"这类条目 |

## 3. 用户已拍板的架构裁决（沿用计划书 §12，不重议）

**页面上合，代码上分，依赖单向朝下。**

```
notes_gen/daily.py（新增：复盘编排器）
      ├─→ core.market_position_runner.thermometer_md()   ← 盘面段（复用，不写第二份）
      └─→ trade 域数据（daily_report 表 + 持仓 + 止损线）  ← 持仓段 + 留意清单

core/market_position_runner.py   大盘位置（绝不 import trade，已有 AST 守护）
```

**为什么编排器放在 `notes_gen/`**：那里已有 `notes_gen/monthly.py`（月度笔记），
语义完全一致（生成笔记/报告），**不新增顶层包**（铁律 8「如无必要勿增实体」）。
不放进 `trade/`：`trade/` 有严格的模块纪律（文档记的是 9 模块），且复盘是"报告"
不是"交易"，放进交易域会让交易包继续膨胀。

**注意**：`core/market_position_runner.py` 绝不 import trade 这条守护**必须保持不变**
（`tests/test_market_position.py::test_ast_no_trade_import`）。隔离是靠**编排器在更外层
同时 import 两边**实现的，不是靠放松里层的守护。

## 4. 触发方式：独立 job，15:55（不碰实盘进程）

有两个选项，我推荐第二个：

| 选项 | 做法 | 评价 |
|---|---|---|
| A | 接在 `_notify_daily` 里一起发 | **顺序冲突**：EOD 在 15:05，而大盘位置 15:50 才采集完，15:05 取不到当天盘面。且要改 `trade_main.py` 实盘代码——**脆弱期少动为妙** |
| **B（推荐）** | **新增独立 scheduler job，每交易日 15:55**，纯读 | ①顺序天然正确；②`trade_main.py` 一行不改；③报告生成失败绝不影响交易链 |

时间链（顺序不能错）：

| 时刻 | 步骤 | 状态 |
|---|---|---|
| 15:05 | 交易端 EOD 归档（含 `daily_report` 落库） | 现有 |
| 15:45 | K 线缓存补尾段 | 现有 |
| 15:50 | 大盘位置采集 | 2026-09-17 已建 |
| **15:55** | **复盘报告生成 + 推送** | **本计划新增** |
| 09:05（次日） | 大盘体温表推飞书 | 2026-09-17 已建 |

**代价（如实说）**：15:55 读到的账户数据是 15:05 落的快照。但那时已收盘，
资产数字不会再变，可接受。**唯一的坏情况**是 15:05 那次 EOD 因关机没跑——
此时 `daily_report` 表缺当日行，复盘报告的持仓段要**明确写"当日无交易归档"**，
**不能用昨天的数字冒充今天**。

## 5. 「需要你留意的清单」（原「明日关注」改名）

**改名理由**：「明日关注」承诺了预测，与业务铁律 1（宏观/研判只报告、仓位人工把关）
冲突——推到手机上的报告一旦像建议，就会被当建议读。改叫「需要你留意的清单」，
只陈述事实。

**v1 只做规则事实，不做主观预测，不调 LLM。**

| 条目 | 数据来源 | 把握度 |
|---|---|---|
| ① 持仓中距止损线最近的 N 只 | 持仓 `avg_cost` + 当日收盘价（本地日线缓存，离线可得）+ `trade/config.py::StopConfig` 的硬止损阈值 | **待核实**：止损阈值在多条件（硬止损/移动止盈/阶梯止盈/时间止损）下"当前生效的那条"如何判定，需先读透 `StopConfig` 语义再定 |
| ② ETF 轮动当前目标腿 + 下次锚定日 | payload 里已有 `rotation`（`trade_main.py:1042~1057`） | 高（现成字段） |
| ③ 大盘位置侧状态提示 | `market_position_runner.latest()` 的 `stale` / 宽度阈值 | 高（现成字段） |
| ④ 当日公式出票 | **待核实**：`trade/store.py` 里只有 `RotationSignalStore`，没找到 auto_buy 信号的专用表（可能在 audit 事件里） | **待核实**，查清后决定是否纳入 v1 |

**LLM 只允许做一句话归纳，且默认关闭**（选项同 `trade/config.py::FeishuConfig.ai_review`
的既有惯例：默认 `False`）。

## 6. 模块划分（开工前五问）

| 问 | 答 |
|---|---|
| **放哪层?** | 编排与组装在**新增** `notes_gen/daily.py`（深模块，含取数+组装+投递决策）；纯格式化（payload → Markdown）下沉为**纯函数**放在同一模块内（照 `trade/daily_report.py` 的"纯计算域"范式）；推送复用 `tools/send_report_feishu.py` / `tools/send_email.py`，**不新写推送器**。 |
| **接口多大?** | `notes_gen/daily.py` 公开 **≤4 个**：`build_review()`（组装 payload）、`review_md()`（纯函数：payload → Markdown）、`push_review()`（投递）、`run_daily_review()`（编排入口，供 scheduler 调）。 |
| **有没有第三份?** | 盘面文案复用 `thermometer_md()`；账户数字复用 `daily_report` 表的既有 payload（**不重算**，重算必然与飞书日报漂移）；推送复用两个既有 tools；止损阈值复用 `trade/config.py::StopConfig`（**不新增参数定义**）。 |
| **删除测试** | 删掉 `notes_gen/daily.py`，"把盘面+账户+留意清单拼成一份报告并投递"这件事散回调度器与前端两处 → 该留。 |
| **改完登记** | CLAUDE.md 架构骨架一行 + CHANGELOG 条目 + commit。 |

## 7. 风险与边界

| 风险 | 处置 |
|---|---|
| **碰实盘代码** | 选项 B：`trade_main.py` **一行不改**。复盘全程只读 trade.db |
| **数字与飞书日报不一致** | 账户段**直接复用 `daily_report` 表里已有的 payload**，不重新算一遍 |
| 破坏了「大盘位置不 import trade」的守护 | 编排器在更外层同时 import 两边；里层 `core/market_position*.py` 的 AST 守护保持不变并有测试锁 |
| 停机导致当日无 EOD 归档 | 持仓段明写"当日无交易归档"，**不拿昨日数字冒充**（照 `trade/asset_gapfill.py` 的 fail-closed 纪律） |
| 邮件投递凭据 | `tools/send_email.py` 读 `.env` 的 `SMTP_USER`/`SMTP_PASSWORD`（实测两键都在）；**密钥只打码，绝不写进报告或日志** |
| 报告太长被飞书截断 | 飞书自定义 bot 卡片单卡 ≤8KB，`chunk_sections` 会自动拆卡；复盘报告预计与体温表同量级（体温表实测 2032 字符 / 3580 字节 = **1 张卡**） |
| 与已有「舆情日报」重复 | `brain/sentiment_pipeline.run_daily_report` 是**舆情**日报（15:05 推），本报告是**盘面+账户**复盘，内容不重叠。**不做合并**，避免把两个独立失败域耦合成一个 |

## 8. 不做（划界）

- **不重写已有的盘后日报**（总资产/盈亏/成交/卖飞/仓位变动全部保留原样）。
- **不给"明日关注"做预测**：只陈述规则事实，不改铁律 1。
- **不做盘中实时版**：只在盘后生成。盘中提醒属于另一个失败域（应该是 `brain/alert_rules.py`）。
- **不合并舆情日报**：见 §7 最后一行。
- **不自动联动任何仓位**：报告里出现的"距止损线还有 2%"只是提示，
  **绝不触发减仓**——止损由 `trade/` 既有的自动规则独立负责，报告线不介入。

## 9. 验收标准

1. `python -m notes_gen.daily`（或等价 CLI）能生成一份含**三段**的 Markdown：
   市场盘面 / 我的账户 / 需要你留意的清单。
2. 账户段的数字与同日飞书日报**逐项一致**（因为复用同一份 payload，不一致即为 bug）。
3. 停机场景单测：`daily_report` 表无当日行时，持仓段写明"当日无交易归档"，
   且**不出现**任何昨日数字冒充今日。
4. AST 守护仍然通过：`core/market_position*.py` 无 `import trade`。
5. 投递：飞书卡片 + 邮件两路各实测成功一次（邮件用 `tools/send_email.py` 的既有能力）。
6. 页签：大盘位置页签出现「今日复盘」折叠块，内容与推送一致。
7. `pytest tests/` 全绿。

## 10. 开工前待核实项（诚实标注）

1. **"当前生效的止损线"怎么算**：`trade/config.py` 有 `CostStopConfig` / `TrailingStopConfig` /
   `LadderTpConfig` / `TimeStopConfig` / `CondTimeStopConfig` 五段，多条件共存时哪条生效、
   以什么价（成本价还是最高价）为基准，**我还没读透**。这一项不查清就不做「距止损线」条目，
   宁可 v1 少一条也不出错数。
2. **auto_buy 当日出票落在哪**：`trade/store.py` 只找到 `RotationSignalStore`，
   没找到 auto_buy 的专用信号表（`tests/trade/test_auto_buy.py` 提到"error 事件 → audit"，
   怀疑在 audit 事件流里）。查清后再决定是否纳入 v1 的留意清单。
3. **邮箱那两路到底用哪个**：`tools/send_email.py`（任意邮箱，SMTP 已配）与
   `tools/send_report_workbuddy.py`（本机 WorkBuddy 信箱，需 WorkBuddy 在运行）。
   我倾向 **v1 只接 `send_email.py`**（不依赖第三方进程常驻），WorkBuddy 那路留待用户指定。
