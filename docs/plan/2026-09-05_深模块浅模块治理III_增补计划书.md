# VERA 深模块浅模块治理 III — 增补计划书

**日期**: 2026-09-05
**性质**: 增补（不是重写）——Wave 2/3 直接引用治理 II 任务卡，本计划书只新增 4 张卡 + 重排波次
**前置文档**:
- 评估报告（本轮）: `%TEMP%/architecture-review-20260905-210021.html`（记分卡 + 8 候选卡 + 四波顺序）
- 治理 II 计划: [2026-08-28_VERA架构深化治理II_项目计划书.md](2026-08-28_VERA架构深化治理II_项目计划书.md)（P0/P1/P2 任务卡 + 纪律 + 验收标准，全部沿用）
- 治理 II 分析报告: [../audit/2026-08-28_VERA架构_深模块浅模块分析报告.md](../audit/2026-08-28_VERA架构_深模块浅模块分析报告.md)

> **For agentic workers:** 按 Task 逐个执行，先红后绿，每 Task 独立提交。
> **工作区警告（2026-09-05 实测）**: 工作区混有大量未提交改动（09-04 市场体检+雷达批次：brain/data_tools.py +77 行、scheduler/__main__.py +33 行、sgpjbg_radar.py 未跟踪，及 trade_main/rotation/reconciler 等）。每 Task `git add` 只挑计划内文件；W1-b 提交 data_tools.py 时会连市场体检本体一起入库（已在提交信息注明），其余脏文件一律不碰。

---

## 一、背景（为什么是增补）

2026-09-05 逐项核实：治理 II 13 项中 **12 项未动**（trade_main 仍 1314 行、rotation 仍 811 行，与 08-28 基线一字不差），仅 P1-1 借 09-02 热加载需求以变体完成（trade/view_calc.py）。同期新增 5 个功能模块（DSH 通道、市场体检、研究雷达、秒回通道、TQ 薄封装），并产生两笔新债：

1. **牛熊口径双实现已分叉**（09-04 产生）: brain/data_tools.py:257（min_periods=250）vs research/index_regime.py:23（min_periods=60），注释宣称"复用"实为复制。
2. **交易日历接口债已出真实事故**（09-04）: `get_trading_days`（失败即崩）vs `get_trading_dates`（降级空表）反义双胞胎 → server.py:460-462 被迫绕行 exchange_calendars。原 P1-4 升格。

**结论**: 08-28 的诊断与药方依然成立，不重写；按新证据重排波次，第一波小到当晚可完成。

## 二、用户拍板记录（2026-09-05）

- 四波全部做完，先计划书后开工。
- **牛熊口径历史数据门槛 = 200 天**（不是 250 也不是 60）：MA250 线不变（与"MA200 交易闸门"保持区分），rolling `min_periods=200`——不满 200 根不算，短样本不冒充。

## 三、四波总表

| 波 | 内容 | 任务 | 状态 |
|---|---|---|---|
| **Wave 1 止血包**（当晚 ~3h） | 新债收口 + 清理 + 常量 | W1-a 清理闸 · W1-b 牛熊口径 · W1-c 常量/舍入 | 本计划书详卡 |
| **Wave 2 实盘正确性**（周末 ~8h） | 治理 II P0 原班 | ④=旧P0-2 rotation 迁出 → ①=旧P0-3 资金口径下沉 → ⑤=旧P0-4 卖单登记收口 | 引用旧卡 |
| **Wave 3 接口债**（~7h） | 治理 II P1 + 升格 | ③=旧P1-4 双胞胎（升格领头）→ P1-3 → P1-5 → P1-6 | 引用旧卡 |
| **Wave 4 结构**（条件触发 ~5h） | 新增 + 旧 P2 | ⑥ data_tools 分家（与 W1-b 同车最佳，本轮先只收口径不搬家）· ⑦ api.py 拆分 · ⑧c api.js · P2-3/4/5 | 本计划书简卡 |

## 四、Wave 1 任务详卡

### W1-a [0.5h] 临时脚本/日志清理闸（= 旧 P0-1，按实测清单执行）

**目标**: 根目录与 tools/ 的用完即弃垃圾清走，加防堆积机制。

**实测清单（2026-09-05 glob 核实）**:
- 删（纯垃圾，均已 gitignore 不涉及 git）: 17 个 `*.log`（out/err/eval_run×4/test5m×4/testpools/test5m2/bench_×2/full_pytest2/_turnover×2/_cap1e8）+ `_tmp_console.txt`
- 移 `scratch/`（有残留价值，已 gitignore）: 3 份 `optimization_results*.json`（07-06/07-09 QUANTQQ 寻优产物，审计报告已引用）+ `_feishu_preview.txt` + `tools/_tmp_sentiment_300687.py` + `tools/_gen_gs2_batch.py`
- 移 `tools/`（实用 PS1 助手，入库）: `_tmp_kill_helper.ps1`（v2rayN 进程击杀）、`_tmp_tun_helper.ps1`（代理修复）

**Steps**:
- [ ] 新建 scratch/；物理移动/删除上述文件
- [ ] `.gitignore` 追加: `/_tmp_*`（根目录锚定，不影响 tools/ 下的助手）、`scratch/`
- [ ] `git add .gitignore tools/_tmp_kill_helper.ps1 tools/_tmp_tun_helper.ps1` 提交

**验收**: 根目录无 `*.log`/`_tmp_*`/`optimization_results*`；`git status` 无新增未跟踪垃圾。

### W1-b [1.5h] 牛熊口径收口 core/index_regime.py（新债，2026-09-05 门槛=200 拍板）

**目标**: 「牛/熊/震荡」判定收单一真相源；消灭 data_tools(250) vs research(60) 分叉。

**Files**: 新建 `core/index_regime.py` + `tests/test_index_regime.py`；修改 `brain/data_tools.py`（market_health 内联数学换真调用，:230 注释与 :271-274 脚注改口）、`research/index_regime.py`（build() 数学委托 core，保留 CLI/parquet 行为）。

**接口设计（公开面 ≤6 符号）**:
```
MA_WINDOW = 250      # 牛熊线仍 MA250（区别于 MA200 交易闸门）
MIN_PERIODS = 200    # 2026-09-05 用户拍板；不满 200 根不算
SLOPE_SPAN = 20
BULL / BEAR / RANGE = "bull" / "bear" / "range"
classify(closes)   -> (state|None, ma_now, slope)   # 单点判定；None=样本不足门槛
regime_series(closes) -> pd.Series                   # 全序列（research 回填用）
```
判定规则不变: 价>MA250 且 MA250 20日斜率>0 为牛；价<MA250 且斜率<0 为熊；其余震荡。

**Steps**:
- [ ] 先红: `tests/test_index_regime.py` —— 常量锚点（200/250/20）、牛市/熊市/震荡构造序列、199 根→None / 200 根→有判定、`regime_series` 末点与 `classify` 一致
- [ ] 实现 `core/index_regime.py` → 单测绿
- [ ] data_tools.market_health 内联 :257-267 换 `classify()`；None→【缺】；docstring/脚注改「复用 core/index_regime.py（min_periods=200）」
- [ ] research/index_regime.py 换 `regime_series()`（min_periods 60→200: 2019 年头 ~10 个月标签从有值变 range，历史研究口径统一，提交信息注明）
- [ ] 回归: `pytest tests/test_index_regime.py tests/brain/ -q` + `python -m py_compile research/index_regime.py`

**验收**: 全库 grep 牛熊实现只剩 core/index_regime.py 一处；data_tools 注释与代码一致；门槛常量有测试锚定。

**提交说明**: brain/data_tools.py 含 09-04 市场体检表本体（用户未提交的 +77 行），随本卡一并入库，提交信息注明「含市场体检表本体补登记」。

### W1-c [1h] 常量/舍入收口（= 旧 P1-2 裁剪版）

**目标**: DIRECTION_BUY 三处定义收 book.py 唯一真相源；round_price 迁 book.py 与 ETF 版同居。

**范围调整（2026-09-05）**: round_price_etf 的迁移碰脏文件 rotation.py（含用户未提交改动），**折进 Wave 2 ④ rotation 动刀时顺手做**；本卡只碰干净文件。

**实测**: book.py:42 已是真相源（analysis/view_calc/store/risk/reconciler/trade_main/rotation 全部 import 它）；仅两处自带副本，且"防环"注释理由不成立——book.py 只依赖标准库+utils.logger，notifier/llm_review import book 不成环。

**Files**（全部干净文件）: `trade/notifier.py:33`（删本地定义改 import）、`trade/llm_review.py:21`（同）、`trade/book.py`（增 round_price，0.01 档注释合并）、`trade/executor.py:78-81`（def 删，改 `from trade.book import round_price`——auto_buy 的 `from trade.executor import round_price` 经模块属性继续生效，不改调用方）。

**Steps**:
- [ ] notifier/llm_review 删本地常量改 import（llm_review 用别名 `_DIRECTION_BUY` 最小化 diff）
- [ ] round_price 迁 book.py；executor 改 import
- [ ] 回归: `pytest tests/trade/test_notifier.py tests/trade/test_llm_review.py tests/trade/test_executor.py tests/trade/test_auto_buy.py -q`

**验收**: grep `DIRECTION_BUY = 23` 全库只剩 book.py 一处；`def round_price` 只剩 book.py 一处。

## 五、Wave 2~4 引用与简卡

- **Wave 2**: 按 08-28 计划 §六 P0-2 → P0-3 → P0-4 原卡执行（行号经 2026-09-05 复核基本未漂：trade_main 三件套 :1120/:1138/:1188、_apply_config :605-609、rotation._place_order :775-811、trade_main 手拼 :300-301）。④ 迁出时顺手: rotation 的 round_price_etf 改 import book（W1-c 遗留半）。
- **Wave 3**: ③ 按旧 P1-4 卡（升格理由: 09-04 交易日历事故 server.py:460-462 绕行补丁，收口后评估回撤）→ P1-3 BatchResult → P1-5 get_or → P1-6 schema 上移，均按 08-28 §七原卡。
- **Wave 4 简卡**:
  - ⑥ data_tools 分家: 市场面（market_health/market_snapshot/zt_pool）抽 brain/market_panel.py，照 dsh_channel 范式小接口厚实现；与 W1-b 同车做最省（本轮 W1-b 先只收口径不搬家，避免一次动太多）。
  - ⑦ trade/api.py（729 行）分析域 5 端点抽 trade/analysis_api.py；view_calc.py 数学已收口，剩纯路由搬家。
  - ⑧c api.js 补研究/交易域 client，页面裸 fetch（5 文件 21 处）收敛。
  - 旧 P2-3/4/5 按 08-28 §八。悬空模块 tdx_tq.py: 明确标注"预留"或让市场面消费，随 ⑥ 一并定夺。

## 六、纪律（沿用治理 II §三，全文有效）

纯移动不改行为 / 先红后绿 / 每 Task 独立提交（只 add 计划内文件）/ 改核心函数前 grep 调用方 / 行号引用机器校验 / 公开面 ≤6 符号 / 实盘改动收盘后做 + tests/trade 全绿才合入。

## 七、Wave 1 验收总表

| # | 验收项 | 判定 |
|---|---|---|
| 1 | 根目录无 *.log/_tmp_*/optimization_results* | glob 空 |
| 2 | .gitignore 含 /_tmp_* 与 scratch/ | 读文件 |
| 3 | 牛熊实现单一真相源 | grep `min_periods` 只在 core/index_regime.py |
| 4 | 门槛=200 有测试锚定 | pytest tests/test_index_regime.py 绿 |
| 5 | `DIRECTION_BUY = 23` 只剩 book.py | grep |
| 6 | `def round_price` 只剩 book.py（rotation_etf 版随 W2） | grep |
| 7 | 回归绿 | tests/brain/ + 相关 tests/trade/ 全绿 |
| 8 | CHANGELOG 登记 Wave 1 | 读条目 |

## Self-Review

1. **范围**: 本轮评估 8 候选 → W1 三卡全详 + W2/3 引用旧卡 + W4 简卡；治理 II 剩余 12 项全部有归属（W2×3 / W3×4 / W4×5）。✅
2. **新证据落位**: 牛熊门槛 200 用户拍板入卡；P1-4 升格入 W3 领头；round_price_etf 范围调整已注明理由（脏文件避让）。✅
3. **依赖**: W1 三卡互相独立；W1-b 提交含 data_tools 用户未提交改动已显式声明。✅
