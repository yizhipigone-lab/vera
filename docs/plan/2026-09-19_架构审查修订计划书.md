# 2026-09-19 代码库架构审查修订计划书

> 依据：`docs/audit/2026-09-19_代码库架构审查报告.md`（4 子代理分片深读 + AST 全仓扫描，19 条风险全部带文件:行号证据）。
> 本文是**修订计划**，不是实施记录。每个批次独立成项、独立拍板、独立验收；批次间无强依赖，可按序也可跳序，但批次 4（实盘链）任何一项上线都必须遵守既有重启纪律。
> 大白话原则：审查报告是"体检单"，本文是"治疗方案"——先吃立竿见影的药（批次 1），再做防护（批次 2），最后动手术（批次 3/4/5）。

## 〇、范围与不做清单

**做**：审查报告 P0 六条、P1 九条、P2 四条，共 19 条，全部分配到批次 1~5。

**不做（本次明确排除，防范围蔓延）**：
1. 不重写前端框架（不引入 React/Vue 构建链）—— 并存问题用"收编到既有统一封装"解决，不换新底座。
2. 不把 mobile.html 与 PC 前端合并为单代码库 —— 成本远超收益，改用契约测试防漂移（批次 2）。
3. 不一刀切删 tools/ 一次性脚本 —— 它们被研究报告引用，是研究证据链的一部分；只建立定期清理机制（批次 3）。
4. 不动 QMT/xtquant 通道选型、不动 EventEngine 单写者架构 —— 审查确认地基健康。
5. 不做事件驱动的仪表盘即时刷新 —— 2026-09-19 已拍板用"扫描收尾顺手刷新 + 周六兜底 job"替代。

## 一、批次 1：立竿见影（纯卫生，零行为变化，可一次做完）

> **实施记录（2026-09-19 完成）**：1.1 探针 `tools/qmt_ready_check.py` 真机实测
> READY/exit=0，bat 块经 GBK+CRLF 逐字节校验；1.2 搬家完成，15 处引用带计数断言
> 替换、grep 零残留、调度器+网关测试全绿，shim 留一版本；1.3 轮转函数+启动钩子+
> 8 例测试全绿，21MB/20 万行实测 1.23 秒，752MB 历史切分（约 45 秒）随 trade_main
> 下次重启自动完成；1.4 积压 61 项分 6 提交清零，gitignore 行内注释坑已修。
> 测试：全量 pytest（除 parity）exit=0；前端 15 套件 14 绿（`test_brain_viz.mjs`
> 1 红为历史遗留，HEAD 复跑同红，与本次无关）。

**目标**：消掉"必复发"的启动事故和一个明显的层级错位，占用磁盘止损。

### 1.1 start_vera.bat 加 QMT 就绪等待（审查 P0-3）

- **现状**：`start_vera.bat` 行 36/41/45 同时拉起三进程，仅 rem 注释提醒"交易进程需要 miniQMT 已登录运行"。2026-09-02 冷启动 rc=-1 事故根因未被脚本消化，必复发。
- **改法**：启动 trade_main 之前，循环跑独立连接测试（9 月 2 日已验证"同路径独立连接测试 rc=0"可行）：rc≠0 则等 20 秒重试，最多 10 次（3 分半），仍不通则**跳过启动 trade_main 并红字提示**（fail-closed：server/scheduler 照起，交易不起）。
- **注意 .bat 三坑（CLAUDE.md 项目定位节）**：必须 CRLF 无 BOM；含中文必须 GBK 编码，不能 UTF-8+chcp 65001；Python 用全路径 `D:\Program Files\Python313\python.exe`。
- **验收**：模拟 QMT 未登录双击 start_vera.bat → trade_main 不启动且窗口明确提示；QMT 就绪后重试 → 三进程全起。

### 1.2 trading_calendar 搬家（审查 P1-11）

- **现状**：`scheduler/trading_calendar.py` 是纯日历工具（零 scheduler 内依赖），却被 3 层 9 个文件向上 import（core 3 处、trade 6 处）。
- **改法**：纯搬运 —— 模块移到 `utils/trading_calendar.py`；原位置留 10 行兼容 re-export（`from utils.trading_calendar import *` + 显式名）；9 个消费者文件改 import 路径；一个版本后删 shim。
- **验收**：`pytest tests/` 全绿；grep 确认无 `import scheduler.trading_calendar` 残留（除 shim 自身）；scheduler/server/trade_main 三进程冒烟启动。

### 1.3 raw_reports.jsonl 按月轮转（审查 P2-17）

- **现状**：`data/trade/raw_reports.jsonl` 实测 752MB，只增不删。它是铁律 5"原始回报先落盘"的审计底账，**不能删**，只能归档。
- **改法**：写入侧按 `raw_reports_YYYYMM.jsonl` 分月落盘（store.py 写路径按月拼文件名，单写者改一处）；存量 752MB 一次性按行内时间戳切分到历史月份文件；读侧（对账回放）改读"当月 + 上月"两个文件。旧文件永不改，只新增。
- **验收**：切分前后全量行数一致（wc -l 对比）；对账回放冒烟；新回报落入当月文件。

### 1.4 仓库卫生（审查 P2-19）

- 删 `web/index.html.bak_20260919` 与 `web/index.html.bak_20260919_uiux`（git 历史已有，不留垃圾）；51 个未提交改动按功能分批提交。

**批次 1 测试**：受影响域全绿 + 三进程冒烟。**重启要求**：批次 1 全部内容只需重启对应进程一次，无时刻约束（不动交易链）。

## 二、批次 2：防护补全（前端收口 + 8081 鉴权）

> **实施记录（2026-09-19 完成，2.4 除外）**：2.1 四处 8081 硬编码全部收编到
> `tradeApiBase`（mobile.html 走 module 桥 + DOMContentLoaded 时序，`?trade=`
> 覆盖保留），接线守卫 `test_trade_base_wiring.mjs` 13 例；esc() 收编经勘察放弃
> （charts.js 版依赖 DOM，与 node 可测文件 regex 版不同源，合并不安全）。2.2 两台
> 服务各加全局 exception handler 统一 `{"detail"}`，契约成文在 handler 注释；
> 坑：ServerErrorMiddleware 发完 500 会再 raise，TestClient 须
> `raise_server_exceptions=False`。2.3 手机版 9 端点契约快照落地
> （`test_mobile_contract.py` + `mobile_contract_snapshot.json`，更新走
> `VERA_UPDATE_MOBILE_SNAPSHOT=1`）。trade 域 981 例全绿。2.4 用户拍板不做。

**目标**：把 9 月 19 日两次事故（404 误报、幽灵行）的同源温床拆掉，补上两块最值钱的前置防护。

### 2.1 前端统一 8081 取数（审查 P1-13 前半）

- **现状**：8081 base URL 五处各自硬编码（`trade.js:19`、`analysis.js:8-9` 两处、`decision.js:42`、`decision_util.mjs:268`、`mobile.html:215`），而 `api.js:121` 的 `createTradeClient` 工厂闲置。
- **改法**：五处全部改为从唯一出口取 base（`tradeApiBase` 收编进 `api.js`，classic 脚本经 `window.tradeApiBase` 暴露，module 脚本走 import）；`esc()` 七处重复定义收编到一份。
- **验收**：`tests/web/test_decision_wiring.mjs` 式契约测试扩到五处取数点（断言请求 URL host:port 正确）；404 与"连不上"的报错文案区分保持 `describeTradeError` 单一口径。

### 2.2 后端错误响应统一 envelope（审查 P1-13 后半）

- **现状**：四种错误形状并存（`success:False` dict / `JSONResponse(400)` / `HTTPException` / 混用），无全局 handler，前端被迫写三套错误解析。
- **改法**：server.py 加一个全局 exception handler，统一 `{success:false, error, detail?}`；各 *_api.py 的 `HTTPException` 保留（handler 统一转形状），前端 fetch 封装只认一种。**trade/api.py（8081）同口径**。
- **验收**：前端三套错误解析减为一套；既有契约测试不红；故意触发各域错误路径各一次，响应形状一致。

### 2.3 mobile 消费端点 schema 快照测试（审查 P1-12）

- **现状**：mobile.html 消费 7 个端点 3 页签，零契约测试；`_month` 幽灵行事故的机制（后端加字段→PC 自动有/手机漂移）原样保留。
- **改法**：对 mobile 消费的 7 个端点写 schema 快照测试（字段清单 + 类型），后端响应字段增删即红。快照文件存 `tests/web/mobile_contract_snapshot.json`。
- **验收**：人为在 mock 响应里加/删字段 → 测试红；当前真实响应 → 全绿。

### 2.4 8081 写端点鉴权（审查 P0-1）—— **用户拍板不做（2026-09-19）**

用户原话："为什么要密钥，都是我自己用，无需密钥。这个不要修。" 局域网联机场景，
威胁面可接受。此项从批次 2 移除，移入不做清单，留档备查（若将来暴露面变化
—— 如端口映射到公网 —— 再重启本项）。

**批次 2 测试**：tests/web + tests/js 全绿 + 新增契约/快照测试。**重启要求**：server.py 必须重启（动 core/ 关联与 API 形状）；trade_main 如动 8081 处理链则必须 ≥15:05 重启。

## 三、批次 3：引擎与缓存收口（回测正确性防护）

> **实施记录（2026-09-19 完成）**：3.1 接缝 `engine.prepare_matrices()` 落地，
> 4 个 sweep 复刻段全删改调接缝（涨停预过滤保留在接缝之后），`attr_gp1014` 的
> monkeypatch 改显式配置 `filter_limit_up: False`；**勘误**：计划书原写"返回
> PreparedMatrix"，实际返回既有 dict 契约 —— `PreparedMatrix` 是 run_cached 的
> 前门形状，准备段 dict 是超集（含 idx/cols/degraded_np/degrade_res/turnover_day），
> 换类型要连 matrix_cache 一起改，收益为零，故保持 dict；sweep 缓存 meta 加
> `prep_seam` 标记（接缝口径含 end_time 截断，旧缓存加载告警）。parity 由
> `tests/test_prepare_matrices_parity.py` 用合成数据对拍旧配方（3 例）。
> 3.2 校验下沉 `engine._validate_caliber`（run 新增 `selection_caliber`；直调不传
> 打 `caliber_unverified` WARNING），pipeline 删掉自己那份重复校验。3.3 缺省 True
> 键声明搬到 selector + AST 防漂移锁；`_normalize_universe` → 公开
> `normalize_universe`。3.4 `tools/tools_inventory.py` 三级分类（首跑：128 脚本 =
> 生产 57 / 仅文档 51 / 零引用 20），**不删只分类**。

**目标**：把"同一口径不写第二份"的最后几个破洞堵死。不改任何回测数值结果（快照 parity 锁做验收尺）。

### 3.1 sweep 工具私有复刻收口（审查 P1-7，本批核心）

- **现状**：`gs_5m_sweep.py:161-187`、`quantqq_1m_sweep.py:177-193`、`quantqq_5m_sweep.py:188-206`、`quantqq_5m_sweep_2010.py:256-272` 四个工具把 engine 的 `_prepare_run_matrices` 私有配方各写一份；`attr_gp1014.py:93-95` 运行时 monkeypatch `_filter_limit_up`。
- **改法**：把 `_prepare_run_matrices` 提为 engine 公开接缝（返回 PreparedMatrix 数据类）；四个 sweep 改调它并删除各自的复刻段；monkeypatch 改为显式参数透传。
- **风险**：提公开接缝后它即成契约，未来改准备段要考虑工具消费者 —— 这正是目的。
- **验收**：四个 sweep 各跑一个小区间，结果与改造前同一输入的输出**逐字段一致**（改造前先跑一次留底）；`tests/test_snapshot_parity.py` 全绿。

### 3.2 engine.run 校验下沉（审查 P1-14）

- **现状**：直调 `engine.run` 绕过 pipeline 的复权一致性（`pipeline.py:146`）与 period 校验（`:154`），`engine.py:519-522` 注释自己承认。
- **改法**：两项校验下沉到 engine 构造期/run 入口；pipeline 改为幂等复用（已校验不重复跑）。
- **验收**：直调 engine.run 传不一致复权参数 → 立刻报错而非静默跑完；pipeline 路径行为不变（parity 锁）。

### 3.3 缓存缺省键声明式导出（审查 P1-15）

- **现状**：`_DEFAULT_TRUE_KEYS={"exclude_quit"}`（`selection_cache.py:53`）是缓存层手工维护名单；selector 加同类键 → 7 月 27 日投毒事故的 P0-1 复发。另有 `universe_cache.py:53` 走私私有名 `_normalize_universe`、L0/L2 口径不对称。
- **改法**：缺省 True 键由 selector 声明式导出（selector 定义、缓存层 import），`_normalize_universe` 提为公开名；L0/L2 不对称在文档写明取舍理由（L0 不含池哈希是 17 秒权衡，非 bug），不加代码。
- **验收**：人为往 selector 声明里加一个缺省 True 键 → 缓存 key 自动包含它（测试断言）。

### 3.4 tools/ 淤积治理机制（审查 P2-16）

- **现状**：130 个脚本 82 个零引用（63%）。多数是研究报告证据链，不能一刀切删。
- **改法**：建一个 5 分钟能跑完的清单工具 —— 扫描 tools/ 每个脚本的"被引用处"（源码 + docs/ + research/ + .bat），输出三级清单：被生产引用（白名单）/ 只被文档引用（研究证据，保留）/ 零引用（候选删除）。每季度跑一次，零引用的经人工过目后删。
- **验收**：工具跑出的三级清单与抽查 10 个脚本的人工判断一致。

**批次 3 测试**：`pytest tests/` 全绿（重点 parity 与缓存隔离套件）。**重启要求**：server.py 重启（engine/selection 改动）；trade_main 不动。

## 四、批次 4：实盘交易链手术（最高谨慎，逐项独立上线）

> **实施记录（2026-09-19，前半完成：4.1 / 4.3 / 4.4）**：
> 4.1 新增 `EVENT_READ_QUERY` + `TradeApp.read_via_consumer/read_asset`，
> 两个端点的资产查询改走消费者线程；专项测试用线程名探针证明修复生效。
> 4.3 回填 CLI 加 TCP 探活守卫（活着拒写，`--force` 绕过，`--dry-run` 不拦）；
> 坑：conftest 焊死 urllib，探活必须走 socket。4.4 `_upsert` 加跨进程文件锁
> （msvcrt/fcntl，超时 30s，collect 转人话错误）。测试：trade 全域 969 例 +
> 大盘域 123 例全绿。**待办：4.2（消费者线程墙钟阻塞 → 事件驱动续跑，需
> FakeGateway 行为序列回归 + 实盘影子观察）与 4.5（rotation.py 拆取数链/在途
> 台账两块）—— 这两项动实盘状态机与 89KB 主文件，单独一轮做。**
> 上线约束：trade_main 重启 ≥15:05；scheduler 也需重启才用上新写锁。

**目标**：消掉四条指向实盘的事故路径。**本批每项单独验收、单独上线，上线硬约束：trade_main 重启必须 ≥15:05 且不在 14:54–15:00 窗口。**

### 4.1 HTTP 线程直打 QMT（审查 P0-2）

- **现状**：`trade/api.py:136`、`analysis_api.py:240` 的查询在 HTTP 线程直调 gateway 同步接口，与消费者线程并发打 xtquant（官方死锁坑擦边）。
- **改法**：资产/持仓类只读查询改走 `store.open_readonly` 快照（落库数据已够页面用）；必须实时的查询（如可撤单状态）改"入队到消费者线程 + Future 等待（带超时）"，HTTP 线程零直接接触 gateway。
- **验收**：压测页面连续刷新 5 分钟无 xtquant 异常；并发日志无交叉调用栈；既有 tests/trade 全绿。

### 4.2 消费者线程墙钟阻塞（审查 P0-4）

- **现状**：`rotation._wait_fills:1602-1623` 消费者线程内 sleep 轮询最长 3 秒/笔；`executor._wait_terminal:679` 同类。阻塞期间 tick/对账事件排队。
- **改法**：改终态回报事件驱动 —— 挂出"等待成交"登记，终态回报事件到达时续跑后续逻辑；超时由既有定时器路径兜底。**这是状态机改造，必须配 parity 式回归：用 FakeGateway 回放既有测试场景，行为序列逐条一致。**
- **验收**：tests/trade 全绿 + 新增"等待期间 tick 事件不被延迟处理"的直测；实盘影子观察一个完整调仓日。

### 4.3 trade.db 第二写者（审查 P0-5）

- **现状**：`tools/backfill_daily_decision.py:37` CLI 直写 daily_decision 表，与运行中的 trade_main 并发，靠 WAL busy 重试（`store.py:695`）兜底。
- **改法**（选便宜的）：CLI 启动时探测 8081 健康端点，trade_main 活着就**拒绝执行并提示"请先暂停交易进程或走停机窗口"**；保留 WAL busy 重试作双保险。不建"API 入队回填"通道（勿增实体）。
- **验收**：trade_main 运行中跑回填 → 拒绝退出码非零；停机后跑 → 正常。

### 4.4 daily.jsonl 双进程写（审查 P0-6）

- **现状**：`market_position/daily.jsonl` 写者 = scheduler 三个 job + server 手动 collect 接口；`_COLLECT_LOCK`（`market_position_runner.py:184`）只是进程内 threading.Lock，跨进程靠原子写 last-write-wins 兜底。
- **改法**：`_upsert` 加跨进程文件锁（`msvcrt.locking` 或 portalocker，锁定文件 + 超时 + 失败拒写并提示"另一个进程正在采集"）。不改写者格局（手动 collect 保留，需求上用它救急）。
- **验收**：两个进程同时 collect 的压测脚本，双向都是"一方成功一方收到明确冲突提示"，文件无交织坏行。

### 4.5 rotation.py 拆两块（审查 P1-9）

- **现状**：89KB / 1655 行 / 一个类 39 个私有方法，七合一。实盘期 review 成本极高。
- **改法**：**纯搬运不改行为**，先拆两个自包含块：取数降级链（`:586-650`）→ `trade/rotation_feed.py`；在途买卖双台账（`_settle_open_sells:1109`、`_settle_open_buys:1232` 及配套）→ `trade/rotation_ledger.py`。RotationFeature 变薄为编排。
- **验收**：tests/trade 轮动 34 例全绿；diff 审查确认零行为变化；公开接口数不增（子模块是内部实现，不新增对外面）。

**批次 4 测试**：tests/trade 全量 + 每项自己的专项验收。**重启要求**：每项独立选 ≥15:05 窗口重启 trade_main；4.2 上线后影子观察一日再算完。

> **实施记录（2026-09-19 全部完成，4.1~4.5）**：
> 4.2 用 `EventEngine.pump()` 实现"等待期就地消费叶子事件"（回报+行情），
> 而非把轮动 pass 重排成分段状态机 —— 这是**刻意的范围收缩**：全量事件驱动
> 续跑要动卖→买两段的时序与账本时序，风险远大于收益；当前方案达成"等待期间
> 队列不停摆"的目标且方法体语义不变（判据仍是查网关）。三个安全阀 + 5 例
> 顺序断言测试。4.5 两块都用"零接口变更"方式端出：取数链成独立类（唯一依赖
> gateway，真独立）、在途台账用 mixin（依赖特性内部状态 8+ 个，抽接口=在实盘
> 台账逻辑上做手术）。`rotation.py` 89KB → 73.5KB；剪裁脚本带"必须正好 7 个
> 方法"断言。**验收**：trade 983 例绿、全量 3025 例绿；4.2 的实盘影子观察仍是
> 上线后的事（重启 ≥15:05）。

## 五、批次 5：结构拆分（纯搬运，可长时间慢慢做）

> **5.3 实施记录（2026-09-19 完成）**：按 M7 先例修文档口径 —— CLAUDE.md 实盘铁律 8
> 扩围：`config`/`book` 一并豁免（字段/状态机即契约）、`*_api.py` 薄层按端点数不按
> 方法数、其余超标模块（executor 21 / monitor 12 / channel_manager 12 / analysis 10 /
> risk 9）**计入待拆清单但不为拆而拆**（下次动它时顺手分内聚块，新代码不得再增公开
> 面）。判据从"数方法个数"改成"能不能一口气读完 + 公开面是不是契约本身"。
>
> **5.1 切分假设的勘误（2026-09-19 依赖分析，务必先看）**：计划书原设想"四刀各拆
> 一个自包含块"，**AST 实测不成立** —— 四块全都依赖同一组共享原语，且 runner 的
> 公开面（`__all__` 里 mirror/shadow_replay/thermometer_md/momentum_buckets）也要
> 这些块，先拆任何一块都会形成 **runner ↔ 新模块 循环 import**：
>
> | 待拆块 | 依赖的 runner 内部符号 |
> |---|---|
> | ERP（5 函数） | `ERP_*` 常量、`_expected_trading_day`、`_f`、`_upsert` |
> | 指标体检（3 函数） | `VALIDITY_*`、`BARS_PER_MONTH`、`_erp_table`、`_f`、`_features_frame`、`_index_series`、`history` |
> | 牛熊 regime（3 函数） | `INDEX_SPECS`、`_f`、`_index_series`、`_num` |
> | 影子回放（6 函数） | `MIN_SEGMENTS_FOR_T`、`SHADOW_RULES`、`WINDOW_SPLIT`、`_COST_CACHE`、`_f`、`_index_series`、`history` |
> | 体温表文案（12 函数） | 上述 **全部** + `CALIBER_FOOTER`、`INDEX_SPECS`、`latest`、`mirror`、`shadow_replay`、`_regime_all`、`_dimension_validity` |
>
> **正确顺序（替代原"文案先走"）**：① 先抽**共享底座** `core/market_position_io.py`
> （`_f`/`_index_series`/`_expected_trading_day`/`_upsert`+跨进程锁/`history`/`latest`/
> 配置常量/路径），② 再拆 regime + 体检 + ERP（三者只依赖底座），③ 影子回放，
> ④ **文案层最后**（它依赖 ①~③ 全部）。每步都要先跑 AST 依赖扫描确认方向单向
> （runner → 新模块，绝不反向），再跑大盘域 123 例 + 全量。
> **未做的代价**：`market_position_runner.py` 仍是全场最大文件（约 73KB 级），
> 但**没有半拆状态**（不留"拆一半又循环"的中间态）。
>
> **5.2（core 收包）同样未动**：动 import 面太大，留到实盘平静周单独做，且需先跑
> 全仓 AST 依赖扫描确认无环。

> **5.1 第一刀 实施记录（2026-09-19 完成：共享底座）**：新增
> `core/market_position_io.py`（216 行）= 路径常量 + `_f`/`_index_series`/
> `_expected_trading_day` + 落盘族（`_upsert`/跨进程锁）+ `history`/`latest`；
> runner 2166 → 2051 行。**关键设计 = 路径常量单一所有者**：只在基座定义，runner
> 不留副本，旧入口经模块级 `__getattr__`（PEP 562）**动态**转发；runner 内部
> 一律 `mpio.X` 调用期取值。测试隔离 patch 点跟着搬到基座（conftest +
> `test_market_position_api.py`）。**三个实测坑**：①`monkeypatch.setattr(mpr, "DAILY_PATH", …)`
> 撤销时会把它实体化成真实属性、永久 shadow 转发（全量跑出 4 例失败，症状是
> "写 tmp 读生产"）→ 改打基座 + 新增 `TestIsolationGuard` 三条守卫（含
> `_IO_STATE_NAMES` 实体副本断言）；②模块内**裸全局名不走** `__getattr__`，
> 内部还在用的 `_upsert` 必须显式 import（否则运行期 NameError）；③函数对象
> 显式 import 是合法的（守卫只查路径/阈值这类可变状态）。验收：全量 3028 例绿，
> 生产 `daily.jsonl`/`erp.jsonl` mtime 未变。**下一刀**：regime + 指标体检 + ERP
> （三者只依赖底座），然后影子回放，最后文案层。

**目标**：还"体积债"。全部是移动代码不改行为，风险低但 diff 大，适合在实盘平静的周做。

1. **market_position_runner 四刀**（审查 P1-8）：文案+推送 → `market_thermometer.py`（约 630 行，先拆）；影子回放 → `shadow_replay.py`（约 480 行）；指标体检 → `validity.py`（约 230 行）；ERP → `erp.py`（约 130 行）。剩约 700 行留 runner。每刀独立提交、独立跑绿大盘域 116 例。
2. **core/ 按族收包**（审查 P1-10）：market_* 一族（约 240KB）→ `market/` 包；farm_* 一族 → `farm/` 包；数据访问一族 → `data_access/` 包。同时统一 API 层位置口径（建议：全部 `*_api.py` 住根目录，`farm_api.py` 迁出 core/）。**注意**：收包会动大量 import，必须先跑 AST 依赖扫描确认无环，再逐包迁移。
3. **铁律 8 口径修订**（审查 P2-18）：executor 21、book 19、config 17、monitor 12 等 6+ 模块超标 —— 按 2026-07-26 M7 裁决先例，**修文档口径**（把"契约面即接口面"豁免从 gateway/store 扩到 config/book 这类数据形状模块）或真拆，二选一，不留双轨。建议先修文档口径（成本为零），真拆留到具体改动该模块时顺手做。

## 六、总验收标准

- 每批次结束：`pytest tests/` 全绿 + 该批专项验收逐条过。
- 批次 3 额外：回测快照 parity 数值零漂移（改造前后同一输入逐字段一致）。
- 批次 4 额外：实盘影子观察无异常事件序列。
- 全部批次完成后的终态体检：重跑本审查的 4 个定量指标 —— 最大文件 < 60KB、tools/ 零引用脚本清单机制存在、8081 写端点有鉴权、跨进程写全部有锁。

## 七、风险与回退

- 批次 1/2/5 都是纯搬运或加防护，回退 = git revert。
- 批次 3 的 3.1 是"提公开接缝"，回退 = 工具改回复刻（不建议，回退也应保留接缝）。
- 批次 4 动实盘链，每项上线前必备：当日无在途委托、.FakeGateway 回归全绿、回退方案（git revert + ≥15:05 重启）写入实施记录。
- 任何批次中发现审查报告判断有误（例如某"破洞"实为大有意设计），停下，在本文对应条目写勘误，不硬执行。

## 八、建议执行节奏

批次 1（本周，半天）→ 批次 2（下周，1~2 天，含一个拍板）→ 批次 3（再下周，1~2 天）→ 批次 4 逐项插空（每项半天+观察日）→ 批次 5 长期插空。每批结束在 CHANGELOG 记一条、本文对应节补实施记录。
