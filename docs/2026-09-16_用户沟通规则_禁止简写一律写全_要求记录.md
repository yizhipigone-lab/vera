# 用户沟通规则: 禁止简写、一律完整表达 (要求记录)

> 日期: 2026年9月16日 ｜ 提出人: 用户 ｜ 落地位置: `AGENTS.md` 沟通风格第 6 条(单一真相源) +
> `CLAUDE.md` 协作风格第 5 条(只写指针, 不重复正文)

## 一、用户原话

> "159949 仍停 9/15什么意思，你以后能不能不要写简语，都完整表达出来，要考虑到我是小白，
> 把这个要求写道全局规则和记忆中去。"

## 二、触发这次要求的那句话, 完整意思是什么

原句: **"159949 仍停 9/15"** —— 一句话里塞了三个需要读者脑补的简写:

| 简写 | 完整意思 |
|---|---|
| `159949` | 创业板50ETF(代码 159949.SZ), 是 ETF 轮动策略里的两只"风险腿"之一; 另一只是纳斯达克100ETF(代码 513100.SH) |
| "停" | 行情软件(QMT, 也就是实盘下单用的那个客户端)本地保存的**日线数据**(每个交易日一根, 含当天开盘价/最高价/最低价/收盘价)最新只到某一天, 后面缺 |
| `9/15` | 指 2026年9月15日; 当时缺的是 2026年9月16日那一根 |

完整表述应该是:

> "创业板50ETF(代码 159949.SZ)的历史日线数据, 我们查的时候最新只到 2026年9月15日,
> 还缺 2026年9月16日那一根。同一天里, 纳斯达克100ETF(代码 513100.SH)和黄金ETF华安
> (代码 518880.SH)的数据都已经补到了 2026年9月16日的收盘价(分别是 2.218 元和 8.905 元)。"

## 三、以后写东西的硬性要求

1. **标的写全名加代码**: 写「创业板50ETF(代码 159949.SZ)」, 不写裸代码「159949」;
   同一段里再次提到也要写「该ETF」或全名, 不能退回裸代码。
2. **日期写全**: 写「2026年9月16日」或「9月16日」, 不写「9/16」「9-16」。
3. **不写自造短句**: 「仍停 9/15」要写成「它的日线数据最新只到9月15日, 还缺9月16日那一根」;
   「退成实时价」要写成「改成拿当天最新成交价当止损基准」。
4. **黑话首次出现必须配一句白话**: 回撤、换手、择基/择腿、落库、基准、闸门等。
5. **数量带单位**: 写「24万400股」「约53万元」, 不写「240400」「533207」。
6. **自检**: 每一句话都要让没看过代码、没读过前面聊天记录的人直接读懂。

## 四、为什么写进这两个文件, 而不是"记忆服务"

本机的 Hindsight 记忆服务当前**不可用**:

- 它的配置(`~/.hindsight/coding-agent.json`)和数据原本登记在旧的 Windows 用户
  `liuziheng` 名下, 而这台机器现在只有 `Administrator` 用户(`C:\Users\liuziheng`
  已经不存在, `C:\Users\Administrator\.hindsight` 也不存在);
- 本地服务地址 `http://127.0.0.1:9077` 连不上, 手动拉起所需的
  `~/.hindsight/coding-agents/dist/daemon-start.js` 同样不存在。

因此按 `CLAUDE.md` 里写好的兜底办法(本地文档仍是兜底), 这条规则落在**每次新会话都会
自动加载**的 `AGENTS.md` 与 `CLAUDE.md` 里, 并留下本记录备查。

如果要把记忆服务修回来(在本机 `Administrator` 名下重新配置), 需要另开一次任务;
操作手册见 `docs/2026-08-18_Hindsight记忆与modlens视觉本地化配置_全过程记录.md`。
