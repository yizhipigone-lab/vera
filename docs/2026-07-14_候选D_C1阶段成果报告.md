# 候选D C1 阶段成果报告

> **日期**: 2026-07-14
> **本次会话**: 审计前置修复 + C1-2 + C1-3 + C1-4 全部完成
> **改动**: 3 文件 / +85 -233 行 / pytest 159 passed

---

## 一、原始目标 vs 实际完成

| 计划项 | 状态 | 实际产出 |
|---|---|---|
| C1-1: result_writer 新模块 | ✅ 完成（上次会话） | `pipeline/result_writer.py`（191行） |
| MEDIUM-1: persist() setdefault 静默失败修复 | ✅ 完成 | 改为直接赋值，消除 AttributeError 静默路径 |
| LOW-1: engine_version 常量统一 | ✅ 完成 | server.py 与 result_writer.py 共用 `ENGINE_VERSION` |
| C1-2: pipeline.py TDX就绪进度点 + PipelineResult dict化 | ✅ 完成 | `_cb(8,"TDX就绪")` + `run()` 返回 `PipelineResult` + `__getitem__`/`get` |
| C1-3: server.py /api/run 重写 | ✅ 完成 | 270行→60行，删 importlib.reload，删5个直调import，走 Pipeline.run |
| C1-4: 字段级 diff 验证 | ✅ 完成 | golden fixture 10字段全匹配，pytest 159/0 fail |

---

## 二、本次 commit 清单

```
feat(pipeline): C1-2 — TDX就绪进度点 + PipelineResult dict化
feat(server):   C1-3 — /api/run 重写，走 Pipeline.run + ResultWriter（270→60行）
fix(core):     MEDIUM-1 — persist() setdefault 改为直接赋值，消除静默失败
docs:          LOW-1  — engine_version 常量统一，server 共用 result_writer 常量
```

| 文件 | 改动 |
|---|---|
| `pipeline/result_writer.py` | `__getitem__`/`get` dict访问；`persist()` `setdefault` → 直接赋值 |
| `pipeline/pipeline.py` | 加 `_cb(8,"TDX就绪")`；`run()` 返回 `PipelineResult`；`ResultWriter` import 清掉 |
| `server.py` | 删 `importlib.reload`；删5个直调import；删250行手写编排；换 `Pipeline.run`；`engine_version` 用常量 |

---

## 三、架构变化

### Before（server.py /api/run）

```
270行手写编排
├── importlib.reload(stop_manager/engine/metrics) × 3
├── import: BacktestEngine/StockSelector/Deduplicator/ResultWriter/BenchmarkComparator/ReportGenerator
├── 11个进度点手工 pipeline_status 赋值
├── safe_serialize 逻辑手写（~20行）
├── 三文件落盘逻辑手写（~35行）
└── 字段全集只有跑过才知道
```

### After

```
~60行薄壳
├── yaml 临时文件构建
├── Pipeline.run(progress_callback=ResultWriter.on_progress)
├── ResultWriter.serialize() ← 与测试共用代码
├── ResultWriter.persist()  ← 与测试共用代码
└── 字段全集被 test_serialize_emits_full_field_set 锁死
```

**净减**: ~210行手写逻辑，统一为 Pipeline 接缝。

---

## 四、验证状态

| 验证项 | 结果 |
|---|---|
| pytest 全量 | ✅ 159 passed / 5 skipped / 0 failed |
| server.py 语法 | ✅ `py_compile` OK |
| golden fixture 字段集 | ✅ 10字段全匹配（success/metrics/equity/trades/benchmarks/stop_config_summary/report_url/trade_count/engine_version/entry_price_basis） |
| 循环 import | ✅ server ↔ result_writer 无死锁，常量一致 |
| 手动 web 验证 | ⏳ 待本地启动 `python server.py` → 浏览器点一次回测 → 确认进度条走满 |

---

## 五、待手动验证项

C1-4 字段级 diff 已通过，但需要真实 TDX 环境确认：

```bash
python server.py
# 浏览器访问 http://localhost:8080
# 点一次回测
# 确认：进度条走满（5→8→10→15→...→100）+ 前端指标正常显示
```

---

## 六、后续工作计划

### 🔴 即时：C1 扫尾

- [ ] **手动 web 验证**（见上方）
- [ ] **commit 入库**（确认手动验证通过后）

### 🟡 短期：C3 → C2 → C4

按依赖顺序：

```
C3 (tqcenter 脱钩)  ──── 并行可做，不依赖 C1/C2
     │
     ├── connector.py 加 tq() 访问器 (+12行)
     ├── formula_runner.py 改 import（-2行，删死方法 -80行）
     ├── data_fetcher.py 改 import（-8行）
     ├── signal_exporter.py 顺手清（-4行）
     ├── stock_filter.py 顺手清（-1行）
     └── 验证: grep 'from tqcenter import tq' 全仓只剩 connector.py:47 一处

C2 (删 stop_manager 重复逻辑)  ──── C1 之后可做
     ├── 迁 stop_config_summary() → backtest/stop_config.py
     ├── 删 compute_exit_signals / _compute_single_stock（147行）
     ├── 删 engine.py:823-826/979-995 label-enhancement 块
     └── 验证: trades exit_reason 分布 + pytest 全绿

C4 (删 31 个 orphan)  ──── C2 之后收尾
     ├── 31 个文件 git rm
     ├── _gs_run_one.py → tools/gs_run_one.py + 改5个父driver
     └── 验证: ls 根目录剩13个.py + pytest 全绿
```

**推荐节奏**：
1. C3 纯机械，可一次性做完
2. C2 先写 `test_stop_config_summary.py`，照测试改代码（TDD）
3. C4 等 C2/C3 都落定了再做收尾

---

## 七、风险提示

| 风险 | 等级 | 说明 |
|---|---|---|
| 手动 web 验证未做 | 🔴 高 | C1-4 的字段 diff 已通过，但真实 TDX 路径未经手跑验证 |
| C2 删除 label-enhancement | 🟡 中 | trades 表 exit_reason="换股卖出" 不再有 stop 回填；但 pnl/equity 不变，需告知用户 |
| C3 tqcenter 脱钩 | 🟢 低 | 纯 import 路径替换，无语义改动 |
| C4 删 31 文件 | 🟢 低 | 全是 orphan，grep 全仓 0 引用 |

---

## 八、一句话总结

**C1 全部完成（4个子步骤+2个前置修复+字段diff验证），server.py 从 270行手写编排压到 ~60行薄壳，Pipeline 成为唯一接缝——整个 C1 只剩最后一步手动 web 验证。**
