# 候选 D 阶段成果与后续建议报告

> **日期**:2026-07-14
> **范围**:候选 D(架构全面收编 C1+C2+C3+C4)的 plan 质量审计 → 据反馈修正 → 阻塞项清零 → C1 实施启动(C1-1 完成)
> **产出**:1 份审计报告(v2)、plan 入库 + 4 处修正、1 个代码 bug 修复、1 个新模块(C1-1)、共 5 个 commit
> **关联文档**:[审计报告](audit/2026-07-14_候选D_plan质量审计.md) · [实施计划(已入库+修正)](plans/2026-07-14_候选D_实施计划.md)

---

## 一、本次会话干了什么(时间线)

| 阶段 | 动作 | 产出 | commit |
|---|---|---|---|
| 1. 审计 | 三角色审 plan(4 并行核实 agent + 主审交叉验证) | 审计报告 v1:CRIT×1/HIGH×6/MED×7/LOW×4,plan 评 7/10 | — |
| 2. 复核修正 | 用户给 6 缺口,全部坐实补入 | 审计报告 v2(9/10 完整度):plan 入库、路径补全、_gen_md 捆绑、进度倒退 bug、exit_signal 白算、核对命令附录 | `19b59e5` |
| 3. 决策 | HIGH-2"唯一接缝"范围 | **走 A**:两层接缝(完整流程=Pipeline.run / 回测核心=BacktestRunner 候选B);方案 B 否决(ab_compare 跑两次回测,塞进 Pipeline 破坏功能) | `19b59e5` |
| 4. 修 bug | server 进度倒退 40→30 | 改为先准备(30)再执行(40),单调递增 15→30→40→70 | `0bfa6d5` |
| 5. 修计数 | HIGH-5 算术三错 | 删 30→27、ONE_SHOT 标(27)→(24)、driver 6→5、验证算术 | `f98c4c6` |
| 6. 落 CRIT-1 | 4 漏分类文件归类 | 用户拍板**全删**,C4 删 31/defer 11,根目录最终 13 | `49bcaa9` |
| 7. C1 实施 | result_writer 新模块 | PipelineResult + ResultWriter,9 测试全绿,零破坏 | `1d17559` |

---

## 二、commit 清单(本次会话,正序)

| commit | 类型 | 摘要 | 改动 |
|---|---|---|---|
| `0bfa6d5` | fix | server 进度倒退 bug(候选E插点 40→30 顺序写反) | 1 file +6/-5 |
| `19b59e5` | docs | plan 入库 + 接缝边界精确化 + CLAUDE.md server 记忆修正 + 审计报告 v2 | 3 files +671/-1 |
| `f98c4c6` | docs | HIGH-5 计数算术修正(C4 删27/标24/driver5/验证算术) | 1 file +9/-9 |
| `49bcaa9` | docs | CRIT-1 落定 — 4 漏分类文件全删,C4 删31/defer11 | 1 file +10/-5 |
| `1d17559` | feat | result_writer.py — PipelineResult + ResultWriter(C1-1) | 2 files +334 |

**全部在 master 分支**(个人实盘项目、本地部署,不走 PR 流程)。每个 commit 可独立 revert。

---

## 三、验证状态

| 项 | 状态 | 证据 |
|---|---|---|
| 全量 pytest | ✅ 0 失败 | 进度点全 `.` 和 `s`(约 147 passed / 5 skipped,end_to_end 等需 TDX/浏览器的预期跳过) |
| 新增 result_writer 测试 | ✅ 9/9 GREEN | `tests/test_result_writer.py` 0.60s |
| server.py 语法 | ✅ | `py_compile` OK |
| TDX 连接 | ✅ 可连 | `TdxConnector.is_ready()=True`(E:\NEW_TDX\PYPlugins\user\tqcenter.py) |
| golden fixture | ✅ 可用 | `output/last_result.json` 9.3MB(当前 server 路径产物,可作 C1-4 字节级/字段集 diff 基线) |

---

## 四、当前状态快照

| 对象 | 状态 |
|---|---|
| 候选 D plan | **可启动**(7/10 → 阻塞项全清:CRIT-1/HIGH-1/HIGH-2/HIGH-5 均 ✅) |
| CLAUDE.md | ✅ 已修正 server 行错误记忆(原"走 Pipeline+11回调" → 现状描述) |
| C1 实施进度 | **1/4**:C1-1(result_writer)✅ → C1-2(pipeline 进度点)⏳ → C1-3(server 重写)⏳ → C1-4(golden diff)⏳ |
| C2/C3/C4 | 未启动(plan 已就绪) |

---

## 五、关键决策记录(3 个,供后续追溯)

### 决策 1:HIGH-2 接缝边界 → 走 A(两层接缝)
- **背景**:plan 原文"Pipeline.run 钉成唯一的'怎么跑一次回测'接缝"措辞过宽
- **决策**:精确化为两层——**完整流程接缝**=Pipeline.run(C1 达成,统一 server/main/_gs_run_one)、**回测核心接缝**=BacktestRunner(候选 B,收编 ab_compare/quantqq_2026/sector_batch)
- **否决方案 B 的硬理由**:ab_compare 跑两次回测做 A/B 对比,Pipeline.run 只跑一次,硬塞破坏核心功能;且 quantqq_2026 直调 FormulaRunner 绕过 StockSelector,塞进 Pipeline 会引入口径分裂(违反业务铁律)
- **落地**:plan 末尾"接缝边界说明"小节(commit 19b59e5)

### 决策 2:CRIT-1 四个漏分类文件 → 全删
- **文件**:`_gen_md.py`(162行,与 _scan_gs_txt 写同一 scan_report.md 必须捆绑)/`_backfill_engine_version.py`(102行,F2 任务已完成)/`_gen_report.py`(114行)/`_extract_trades.py`(38行)
- **依据**:4 个均 orphan(生产子树 0 引用),与 C4"清根目录影子管线"目标一致
- **落地**:C4 删 27→31,defer 15→11(commit 49bcaa9)

### 决策 3:C1 实施方案 → 推荐 B(待确认)
- **背景**:plan 写 run() 返回 PipelineResult dataclass + dict shim(方案 A),牵连 main.py + 失败路径
- **推荐 B**:**run() 返回 dict 不变**,server 一行包成 `PipelineResult(...)` 喂 ResultWriter.serialize
- **理由**:务实、零牵连 main.py、先跑通;PipelineResult 仍作 serialize 的 typed 入参;未来要全 typed 再升 A
- **状态**:⏳ **未拍板**,C1-2/C1-3 启动前需确认

---

## 六、后续建议(按优先级)

### 🔴 即时:C1 剩余(C1-2 → C1-3 → C1-4)
- **C1-2**(小):`pipeline/pipeline.py` 加 `_cb(8, "TDX就绪")` 进度点(对齐 server 的 8% 点);若采方案 A 还要改 run() 返回类型 + dict shim
- **C1-3**(大,高风险):`server.py /api/run` 删 `importlib.reload` + 删 5 个真直调 import + 250 行手写编排 → 薄壳(`Pipeline.run` + `ResultWriter.serialize/persist`)
- **C1-4**(验证):golden fixture 字段集 diff + pytest 全绿 + 手跑 web `/api/run` 确认响应字段集不变
- **建议节奏**:C1-3 是生产入口 270 行重写,**值得单独专注**,不要和别的混做

### 🟡 短期:C2 / C3 / C4(plan 已就绪,按依赖顺序)
```
PR-1 (C1) 地基 [进行中, 1/4]
   ├─ PR-2 (C3) tqcenter 脱钩 — 不依赖 C1, 可并行早做, 风险最低
   └─ PR-3 (C2) 删 stop_manager label-enhancement + exit_signal 死输出
        └─ PR-4 (C4) 删 31 + 移 1 + 改 5 父 driver
```
- **C3** 建议接着 C1 做:纯 mechanical(tqcenter import 改 accessor),22 处收口到 connector 1 处,零语义改动
- **C2** 已查清命门:删 `compute_exit_signals` 不影响实际止损(止损 100% 在 `_simulate_core_v3` 内),只改 exit_reason 显示 + 消除 exit_signal 死输出
- **C4** 最后:删 31 个 orphan(含捆绑的 _gen_md/_scan_gs_txt)

### 🟢 中期:候选 B 阶段 2(BacktestRunner 抽象)
- 收编 defer 的 11 个 BATCH_DRIVER(ab_compare/quantqq_2026/sector_batch 等),让"回测核心接缝"也统一
- 这是第二层接缝的归宿,本 plan 不碰

### ⚪ 不在本次范围(审计已记录)
- C5(5 处配置 schema)/C6(5 处缓存)/C7(死代码 enrich)— 用户已剔出 4 候选套餐

---

## 七、风险与注意事项

| 风险 | 等级 | 应对 |
|---|---|---|
| **C1-3 server 生产入口重写** | 🔴 高 | 必须 golden fixture 字段集 diff 兜底;response_data 三共用(/api/run 返回 + last_result.json + {ts}.json)改形状三处同断;改完手跑 web 验证 |
| **C1 方案 B 偏离 plan** | 🟡 中 | B 改 run() 返回不变,需在 C1-2 启动前确认;若坚持 plan 的 A,要重构 main.py + 失败路径 |
| **importlib.reload 删除影响开发 DX** | 🟡 中 | reload(stop_manager/metrics/engine) 疑为开发热重载,删后改核心模块需重启 server;建议提供 uvicorn --reload 替代 |
| **ResultWriter 命名冲突** | 🟡 中 | `pipeline/result_writer.py:ResultWriter` 与 `selection/result_writer.py:ResultWriter` 同名不同物,import 时注意来源(C1-1 已注) |
| **golden fixture 9.3MB** | 🟢 低 | 字节级 diff 太重,建议 C1-4 用字段集 + 关键数值 diff 代替全字节 diff |
| **TDX 环境依赖** | 🟢 低 | TDX 可连但输出 GBK 乱码(无害);CI 环境无 TDX 时相关测试 skip |

---

## 八、一句话总结

**候选 D plan 从审计时的 7/10 修正到可启动状态(4 阻塞项全清),CLAUDE.md 记忆纠偏,server 进度倒退 bug 已修,C1 第一步(result_writer 新模块,9 测试)落地——接下来的重头戏是 C1-3 的 server 生产入口重写(建议方案 B + 单独专注 + golden diff 兜底)。**
