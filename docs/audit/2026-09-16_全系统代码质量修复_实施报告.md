# 2026-09-16 全系统代码质量修复 实施报告

> **依据**: [审计报告](2026-09-16_全系统代码质量_审计报告.md)（P0×9 / P1×39 / P2×24，133 处引用机器校验 100%）。
> **用户拍板范围**: P0 修 8 条（P0-4 实盘 API 8081 零鉴权+CORS\* **有意不修**——2026-08-18 手机访问拍板的设计）;
> P1 全修; P2 修一行式小项; T1 用保守方案（只改漂移告警文案，不加买侧核销台账）;
> G4（prep 四份合一）与接口拆分（DataFetcher/DataCache/PositionBook）**有意不动**。
> **施工方式**: 5 个修复代理按模块并行（trade / backtest+core / selection+llm+brain+server / 农场+研究脚本 / 基建+tests），
> 主会话对全部约 78 个改动文件逐行 diff 审查; 随后**两轮独立自查**（未参与修复的新审查员）。
> **结论**: 第一轮**有条件通过**（抓到 1 个漏网黑名单副本，当场修掉）→ 第二轮**通过，可放行**。
> 全量 `pytest tests` **EXIT=0 全绿**（修复后共跑 4 轮全量，含最终轮; 快照基线字节级零变化）。
> **引用纪律**: 本文所有 `[文件.py:行号]` 为**修复后**行号，已跑 `python -m docs.audit._verify_references` 机器校验。

---

## 一、P0 落地清单（8 条，P0-4 有意不修）

| 条目 | 修法 | 位置 |
|---|---|---|
| P0-1 选股缓存撞 key | 缺省表驱动: `_DEFAULT_TRUE_KEYS = {"exclude_quit"}` + `_keep_universe_key()` 单键判定（显式 False 保留进 key、True 与缺键同 key; 拆开独立判定防 Python `False == 0` 误剔）; 新增两条回归测试锁 | `[selection/selection_cache.py:36]`、`[selection/selection_cache.py:44]` |
| P0-2 F6 首末根口径 | `_close_at` 取当日**最后一根** bar（`iloc[-1]`），与 manifest last_close 末根口径对齐; 配套 `tools/import_lc5_to_cache.py` 接缝校准侧同步取末根 | `[core/kline_cache.py:453]` |
| P0-3 阶梯兜底双卖 | monitor 兜底标档后补 `tier_state.save` 双写，与 executor 预埋路径完全同构 | `[trade/monitor.py:338]` |
| P0-5 对账键名 | 改取 `"total_asset"`（网关实际口径），取不到保持 None 交视图层告警，不再本地兜底 | `[trade/analysis_api.py:245]` |
| P0-6 旧回填脚本 | `tools/backfill_daily_asset.py` 整文件删除（git status: D; 全仓 grep 无调用方） | — |
| P0-7 达标丢回撤腿 | 两份脚本补回撤腿 `maxdd.abs() <= 上限`，年化/回撤阈值改引 `core/farm_rules` 常量; 笔数下限 300/3000 保留课题自有口径 | `[tools/quantqq_1m_sweep.py:50]`、`[tools/quantqq_1m_sweep.py:382]` |
| P0-8 三套达标线 | quantqq_5m 的 0.23 硬编码改引 farm_rules（15%/15%）; gs_1d_detail_report 文本改 `farm_rules.describe()` 动态生成 | `[tools/quantqq_5m_sweep.py:46]`、`[tools/gs_1d_detail_report.py:49]` |
| P0-9 分段指标缩进 | v0/cum/n_days 三行缩回 for 循环体内（对照同文件 `_max_dd_from_segments` 正确写法） | `[tools/formula_pipeline/stage_5m.py:231]` |

## 二、P1 落地清单（39 条）

### trade/
- T1 买侧无核销（**保守方案**）: 漂移告警文案升级为明确指引（失真机制 + 人工修账入口: 改 rotation_lots 表或清 lots_initialized 标志重启触发迁移重建），记账结构未动 `[trade/rotation.py:977]`
- T2 线程池泄漏: 模块级常驻 `_CALL_POOL`（max_workers=4），原"超时后线程可能仍在跑"语义保留 `[trade/gateway.py:201]`
- T3 成本缺失守卫: 档位循环内 `avg_cost <= 0` → 记 audit 跳过（fail-closed，对齐 monitor 守卫）`[trade/executor.py:385]`
- T4 `_sync_tranches()` 从信号工作线程挪到 `start()` 消费者线程侧（spawn 前调用，happens-before 保证），docstring 注明调用纪律 `[trade/rotation.py:256]`

### backtest/
- B1 流动性约束恒失效: `PreparedMatrix` 补 `turnover_day_np` 可选字段 `[backtest/prepared.py:40]`; engine 直接取属性 `[backtest/engine.py:884]`; auto_iter 供数 `[auto_iter/common.py:505]`; 共享段加"约束已配置但无换手数据"告警（run/run_cached 双入口同享）`[backtest/engine.py:379]`
- B2 矩阵缓存绕过条件去掉周期限制（约束开启即不用缓存）`[backtest/engine.py:562]`
- B3 涨停判定收口 `detect_limit_up`（浮点顺序 prev\*(1+ratio) 再 \*0.997 逐元素保持，快照基线字节级零变化）`[backtest/_constants.py:39]`，三处共调 `[backtest/engine.py:1209]`、`[backtest/degrade_5m.py:77]`、`[backtest/entry_next_open.py:81]`
- B4 公式卖出失败结果带标记: `BacktestResult` 新增 `formula_sell_failed` 字段（_UNSET 惯例）`[backtest/result.py:56]`，engine 接线 `[backtest/engine.py:665]`

### core/
- C1 ST 过滤静默 fail-open: 补模块 logger + info 缺失占比超 5% 告警 `[core/stock_filter.py:25]`、`[core/stock_filter.py:115]`
- C2 sqlite 连接泄漏: `KlineCache.close()` `[core/kline_cache.py:88]` + data_fetcher 类级单例池（key 含 cache_dir，测试改写目录自动重建）`[core/data_fetcher.py:171]` + maintenance 侧 `_handles` 池 `[core/kline_cache_maintenance.py:45]`
- C3 拉取失败不盖 24h 冷却戳: `_fetch_and_store` 返回 bool，三处重拉路径失败不 `_mark_refetch` `[core/kline_cache.py:455]`
- C4 体检进度"死后重放"改 Popen 流式逐行读 `[core/lab_runner.py:242]`
- C5 manifest 写全部收口进锁（发现锁内调锁路径，Lock 改 RLock）`[core/kline_cache.py:65]`

### selection/ + llm/
- S1 L0（5m/1m）当日不落盘防线 `[selection/selector.py:287]`
- S2 `_recent_market_days` 异常返 None + warning，调用方按"全部 recent"最保守校验 `[selection/signal_day_cache.py:153]`、`[selection/signal_day_cache.py:208]`
- S3 畸形 200 响应解析包进 try（warning + 返 None 契约）`[llm/providers.py:130]`
- S4 单臂 rank 过滤委托生产 `apply_rules`（"因子:规则"格式，语义逐行一致）`[tools/overheat_ab_test.py:82]`

### brain/
- N1+N2 judge 收口: 共享 `_run_cli_oneshot`（env 走 `_cli_env()` → AI 设置页标准档对 judge 生效; 超时 `_kill_tree` 杀树）`[brain/claude_cli.py:179]`; eval/sentiment 两个 judge 删副本改调用
- N3 流式分支 stdin 写入移入受保护块（异常走杀树+关管道+失败返回）`[brain/claude_cli.py:347]`
- N4 CancelledError 改 `_kill_tree` 后再 raise `[brain/claude_cli.py:391]`

### 根目录入口
- W1 连通测试端点 async def 改 def（同步端点走线程池，不再堵事件循环 20 秒）`[ai_api.py:101]`
- W2 `Executor.set_on_pending_died()` 公开接线方法，替代外部直写私有属性 `[trade/executor.py:266]`、`[trade_main.py:450]`

### 农场 + 流水线
- F1 空块补第三返回值 `[]` `[tools/formula_pipeline/interpreter/runner.py:334]`
- F2 `no_kline`（暂态）不再入永久零信号记忆 `[tools/formula_farm/farm_parallel_sweep.py:124]`
- F3 断点续跑合并 prev 行去重（keep="last"）再写 `[tools/formula_farm/oos_segment.py:100]`
- F4 未来函数黑名单收口: 新建单一真相源 `tools/future_tokens.py`（27 token = 三份清单并集）`[tools/future_tokens.py:16]`，消费方改引: `[tools/formula_farm/static_vetting.py:76]`、`[tools/formula_pipeline/common.py:38]`；**追加**（主会话+两轮自查补齐的漏网副本）: `[tools/gs_formula_filter.py:38]`、`[tools/scan_gs_formulas.py:15]`（本地保留 FILTERX 追加）、`[tools/gs_make_report.py:37]`
- F5 复核判定线从被测工具导出 `[tools/repaint_check.py:34]`、`[tools/future_func_check.py:42]`，farm_verify 改 import
- F6 `push_feishu` 三份合一到 formula_farm/common `[tools/formula_farm/common.py:60]`
- F7 硬编码批次目录改按 mtime 取最新批 + 结果源差异声明 `[tools/formula_farm/farm_batch_sweep.py:51]`
- F8 去重解析失败逐条 warning + 计数提示 `[tools/formula_farm/dedupe.py:22]`

### tools 研究脚本
- G1 四处"最优"加笔数守卫（pick_best / MIN_TRADES 过滤，不足样本标注"样本不足"）`[tools/gs_1d_detail_report.py:60]`、`[tools/gs_make_report.py:114]`、`[tools/gs_report.py:140]`、`[tools/gs_1d_yearly.py:45]`
- G2 两个口径互斥脚本互相标注"不可直接比较"（数字口径零改动）`[tools/rotation_ma_filter_compare.py:12]`、`[tools/etf_rotation_freq_backtest.py:18]`
- G3 结论段数字改由本次运行 metrics 动态生成 `[tools/rotation_ma_filter_compare.py:286]`
- G5 读取失败公式进报告"读取失败"段 `[tools/gs_make_report.py:207]`

### tools 基建
- U1 断线回调补 reason 参数 `[tools/p0_disconnect_watch.py:35]`
- U2 除权表拉取异常 fail-closed（记 error + 跳过该股不导入）`[tools/import_lc5_to_cache.py:199]`
- U3 失败天写 sidecar 清单 + 明显 WARNING `[tools/fetch_event_factors.py:90]`

### tests/
- E1 conftest 隔离补 kline_cache 接缝（`_KLINE_CACHE_DIR` 指 per-test tmp，原值保存/恢复）`[tests/conftest.py:122]`
- E2 真实管线测试显式 `use_kline_cache: False` `[tests/test_end_to_end_real_pipeline.py:31]`
- E3 网络焊死补 requests（假 200）与 smtplib（抛 RuntimeError 拦死）`[tests/conftest.py:216]`
- E4 影子日志路径指 per-test tmp（含 rotation 按值 import 处理）`[tests/conftest.py:129]`
- E5 名不副实测试改名 `test_max_drawdown_synthetic_range` + 清乱码注释 + 区间按实测校准（-50%~-30%，合成数据实测 -40.9%）`[tests/test_metrics_annualization.py:89]`

## 三、P2 小项落地（22 条）

硬止损缺省常量 `[backtest/engine.py:47]` / 未知原因码显式化 `[backtest/engine.py:970]` / degrade_report 缺省对齐 stop_config `[backtest/degrade_report.py:57]` /
formula_runner 日期截 8 位比较 `[core/formula_runner.py:240]` / `to_formula_int("3")` 同走钳制 `[core/dividend_type.py:89]` /
stock_filter 预填条目盖时间戳 `[core/stock_filter.py:135]` / farm_runner `_save_last` 留痕 `[core/farm_runner.py:73]` /
`ladder_tier_qty` 收口 `[trade/book.py:73]` / `_today_range` 合一 `[trade/analysis.py:29]` / `load_open_orders` 补锁 `[trade/store.py:614]` /
下单异常补 audit `[trade/executor.py:302]` / EOD 用注入时钟 `[trade_main.py:951]` /
server 三处清 error `[server.py:288]` + 日期归一 helper `[server.py:483]` / data_cache_api limit 入 try `[data_cache_api.py:44]` /
stop_dsh docstring 说实话 `[brain/dsh_channel.py:183]` / `_spawn` 失败清临时日志 `[brain/dsh_channel.py:260]` /
会话日志按 t0 过滤 `[brain/dsh_channel.py:396]` / market_panel 缓存键带日期 `[brain/market_panel.py:120]` /
news_dedup 版本号 `[brain/news_dedup.py:8]` / a2 死引用+calmar 走 metrics `[tools/a2_build_baseline.py:30]` /
mf_to_score 合一 `[tools/smoke_event_factors.py:47]`

## 四、两轮独立自查结论

**第一轮（挑刺核对，未参与修复的审查员）**: **有条件通过**。
8 条 P0 全部修对（行为级验证）、P1 抽查与收口专项无修错、快照 parity 实测零变化、测试更新无放水。
唯一实质问题: `tools/gs_make_report.py` 的 EXCL_FUNCS 是第六份手写黑名单副本（比并集少 ZXNH）——
主会话当场一行收口（本报告 F4 追加项），另两份漏网（gs_formula_filter/scan_gs_formulas）此前已由主会话补收。
两条低严重度备注（quantqq 判定式本地写法、测试进程单例池累积）确认为可接受折中，不挡通过。

**第二轮（独立复审，另一名审查员）**: **通过，可放行**。
第一轮的实质问题确已修复且未引入新问题; 全仓 grep 确认黑名单手写副本清零（消费方全部改引单一真相源）;
三条最关键实盘修复（monitor 落库/对账键名/_close_at 末根）抽核全部属实;
`tools/future_tokens.py` 的 27 token 与三份旧清单（git show HEAD 逐一取出核对）的并集完全一致;
`pytest tests/test_audit_references.py tests/test_snapshot_parity.py` 29 条全绿。

## 五、验证与边界

- 全量 `pytest tests` 修复后共跑 4 轮（含各修复组自验 + 主会话两轮），**全部 EXIT=0**;
  快照基线 `test_snapshot_parity.py` 字节级零变化（B3 收口未改任何数值）。
- 改动规模: 78 个文件（含 1 个新文件 `tools/future_tokens.py`、1 个删除 `tools/backfill_daily_asset.py`），+962/-638 行。
- 有意未动: P0-4（8081 鉴权，用户拍板维持）、G4（prep 四份合一）、接口拆分三项、tdx_tq 双引导。
- 环境噪音（非本次引入）: `farm_onboard.py` 的 `import psutil` 缺包系预置依赖，py_compile 通过、不影响套件。
