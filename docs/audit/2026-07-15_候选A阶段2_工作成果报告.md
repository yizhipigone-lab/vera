# 工作成果报告 — 候选 A 阶段 2 核心回测循环拆解 + 配套重构

> **报告日期**: 2026-07-15
> **分支**: `feat/候选A阶段2-核心循环拆解` 等多个特性分支,均已快进合并至 `master`
> **基线**: `5c461b6`(159 passed + 5 skipped)
> **完成**: `157ab95`（302 passed + 5 skipped）
> **规模**: 18 个提交,42 文件,+5077 / −188 行

---

## 1. 一句话成果

把 527 行、39 参数、谁都不敢碰的回测核心函数 `_simulate_core_v3`,拆成 16 文件 Strategy 模式子包 `backtest/loop/`,行为与原版**字节级一致**(50 组合成 + 3 组真实 TDX 数据 parity 守护),并完成 C2/C3/C4/C5/C6 五项配套重构 + ATR 止损策略从模板验证到生产可用。全程零回归(159→302 测试全绿)。

---

## 2. 完成的工作

### 2.1 核心循环拆解(候选 A 阶段 2,5 阶段)

| 阶段 | 内容 | 产出 |
|---|---|---|
| 计划书 v3 | 修正前两轮审计自身 3 个错 + 落回正文 | docs/plan/ 计划书 622 行 + 基线快照 |
| Stage 1 | 数据结构 + 6 策略 adapter + dtype 断言 | state.py / base.py / 6 strategies,32 单测 |
| Stage 2 | ExitDispatcher 多结果模型 + FormulaSell | exit_engine.py / absolute.py,17 单测(含 6 双触发场景) |
| Stage 3 | BacktestLoop 主循环 + 字节级 parity | loop.py / entry.py / equity.py / builder.py,50 parity |
| Stage 4 | 兼容壳 + ENGINE_VERSION bump | `_simulate_core_v3` → 45 行壳,legacy 保留作甲骨文 |
| Stage 5 | 文档 + ATR 范例 + 修 docstring | docs/architecture/loop.md |

**关键设计**:
- **CA1 双触发**:`ExitDispatcher.evaluate() -> List[TriggerResult]` 多结果模型,精确复刻 `trailing_first` 同 bar 2 笔交易(ladder 部分卖 + trailing 全卖剩余)
- **CA4 执行价折叠**:TriggerResult 含 execution_price,check 内算好
- **capability gating**:禁用策略构造时不进 dispatcher
- **向后兼容**:39 参数签名零改动,6 个直调测试文件零改动全绿

### 2.2 真实数据对拍(关键质量闸)

`tools/real_parity_check.py` + `tests/test_real_parity.py`:用真实 TDX K 线 + QUANTQQ 信号 + 真实 default.yaml 止损配置,monkeypatch 切换壳/legacy,3 优先级字节级对拍。

**抓出并修复了合成数据漏掉的 bug**:`trades` profit 列 1-ULP 浮点差异——根因是 `1-(c+s)` vs `(1-c)-s` 结合律不同。修:`comm_factor = 1.0 - commission - stamp_tax`(同 legacy 结合律)。

### 2.3 ATR 波动率止损策略

- `strategies/atr_stop.py`(reason=13):peak_hi - N×ATR 回撤线,Low 触及全卖
- **从模板验证升级到生产可用**:接入 `run_cached`/`run`,配置 `stop_config["atr_stop"] = {"enabled":bool,"period":14,"multiplier":3.0}`,内部 `_compute_atr_matrix` 从 high/low/close 预算
- 优先级:止损类,cost_stop 之后;opt-in 默认禁用,不影响现有策略
- 10 个测试(5 单测 + 1 集成 + 4 生产路径)

### 2.4 配套重构 C2/C3/C4/C5/C6

| 项 | 内容 | 收益 |
|---|---|---|
| **C2** | 抽 `_post_process` 共享后处理 | run/run_cached 改 equity_curve 只改一处,消 drift |
| **C3** | 回测结果 dict → BacktestResult dataclass | typed 返回 + dict-like 兼容(_UNSET 精确 dict 语义) |
| **C4** | 删 selection/ResultWriter 孤儿 | 消除两个 ResultWriter 命名冲突 |
| **C5** | DataFetcher connector 注入缝隙 | 可注入 mock 测试,27 外部调用点零改动 |
| **C6** | DataFetcher 缓存抽到 DataCache 类 | 缓存职责单一,可独立测试 |

### 2.5 两轮项目质量审计

- **第一轮**(8.5/10):3 角色并行(架构/代码/parity),9 个最危险共识发现 8 VERIFIED + 1 行号漂移(已修)。修复类型标注/除零守卫/R9 只读/鲁棒性 + 性能基准
- **第二轮**(9/10):覆盖 C2/C3/C5/C6。CRITICAL 0 / HIGH 0 / MEDIUM 1(测试隔离,已修)/ LOW 4。修复 C6 测试隔离 + C2 契约测试 + _UNSET 文档

---

## 3. 关键成果指标

| 指标 | 基线 | 完成 | 说明 |
|---|---|---|---|
| 测试通过 | 159 | 302 | +143 新测试,5 skipped 不变 |
| 字节级 parity | — | 50 组合成 + 3 组真实 TDX | 全绿,含双触发/退市/停牌/T+1/换股 |
| 性能退化 | — | 1.40x(真实批量 2500×500) | <2x 阈值,无需优化 |
| 向后兼容 | — | 39 参数签名零改动 | 6 直调测试文件零改动 |
| 新策略添加 | 开颅手术 | 60 分钟模板 | ATR 验证:不碰老代码 |
| 审计评分 | — | 9/10 | 可交付,无 CRITICAL/HIGH |

---

## 4. 新增文件清单

**`backtest/loop/` 子包(1581 行)**:
- `state.py`(BacktestParams/Context/Position/PositionBook/TradeBuffer/Bar/TradeColumns)
- `loop.py`(BacktestLoop 主循环)/ `exit_engine.py`(Priority + ExitDispatcher)/ `absolute.py`(FormulaSell)
- `entry.py` / `equity.py` / `builder.py`
- `strategies/`:base + cost_stop/ladder_tp/trailing/time_stop/cond_time/first_day/atr_stop
- `backtest/result.py`(BacktestResult dataclass)

**其他**:`core/data_cache.py`、`tests/` 10 个新测试文件、`tools/real_parity_check.py`、`docs/architecture/loop.md`、`docs/plan/` 计划书、`docs/audit/` 基线快照 + 审计报告

**engine.py 改动**:`_simulate_core_v3` 527 行 → 45 行兼容壳;旧实现保留为 `_simulate_core_v3_legacy` 作 parity 甲骨文;ENGINE_VERSION `v3.3` → `v3.4-loop-refactor-20260714`;`_post_process` 共享后处理;`_compute_atr_matrix`

---

## 5. 提交历史(18 个)

```
157ab95 feat(item4-6): PipelineResult typing + loop.md 补 C3/C5/C6/ATR + legacy评估
1895d4b feat(atr-production): ATR 接入 run_cached/run 生产路径
4ef7654 fix(审计2): C6测试隔离+C2契约测试+BacktestResult _UNSET文档
f43d4f0 refactor(C6): DataFetcher 缓存抽到 DataCache 类
cacee43 refactor(C5): DataFetcher connector 注入缝隙(轻量解耦)
04bcc25 refactor(C3): 回测结果 dict → BacktestResult dataclass (dict兼容)
c17f73c refactor(C2+C4): 合并run/run_cached共享后处理 + 删selection/ResultWriter孤儿
d9082b5 test(审计迭代): 真实parity pytest化 + ATR双触发/组合测试 + reason_map 13
ed38b58 feat(atr-stop): 新增 ATR 波动率止损策略 (reason=13) — 加新策略范例
2d2af8a fix(候选A阶段2): 真实数据对拍抓出 1-ULP 浮点结合律 bug
1119385 docs(候选A阶段2): 项目审计报告书 — 可合并判定
45bcfb5 fix(候选A阶段2-审计修复): 类型标注+除零守卫+R9只读+鲁棒性+性能基准
118b96e feat(候选A阶段2-stage5): 文档+ATR范例+修docstring/CLAUDE.md参数数
5d2006a feat(候选A阶段2-stage4): 兼容壳+ENGINE_VERSION bump+50组parity
d27edd8 feat(候选A阶段2-stage3): BacktestLoop主循环+字节级parity验证
cb598b7 feat(候选A阶段2-stage2): ExitDispatcher多结果模型+FormulaSellStrategy
e45d9f2 feat(候选A阶段2-stage1): 数据结构+6策略adapter+dtype断言
57eca19 docs(候选A阶段2): 计划书v3修正版 + 基线快照
```

---

## 6. 回滚方法

```bash
git reset --hard 5c461b6   # 回到拆解前基线
```

基线快照见 `docs/audit/2026-07-14_候选A阶段2_基线快照.md`。

---

## 7. 残留与下一步

| 项 | 状态 | 说明 |
|---|---|---|
| push 远端 | 待用户确认 | 本地 master 已就绪,未 push |
| 真实实盘策略跑一次 | 待做 | 确认 ATR 在真实策略下表现符合预期 |
| `_simulate_core_v3_legacy` 删除 | 等下个发版周期 | 现保留作 parity 甲骨文;删前须转"壳 vs 固化快照"parity(详见 loop.md §7.7) |
| 性能优化 | 无需 | 实测 1.40x < 2x |
| C3 `to_dict()` | 前瞻预留 | 当前无消费方,接受 |

---

## 8. 业务铁律核对

| 铁律 | 状态 |
|---|---|
| 回测买入价 = 信号日 T 收盘价 | ✅ EntryEngine 保留,真实对拍验证 |
| 两套买卖口径不混 | ✅ 只走回测 T 收盘路径,未碰 sim_trader |
| 不破坏现有功能 | ✅ 159→302 测试全绿,6 直调测试零改动 |
| 宏观/地缘不联仓位 | ✅ 本次未触及 |

---

**审计结论**:可交付。系统从"527 行单体函数"演进为"可单独测试/维护/扩展的 Strategy 模式子包 + typed 返回 + 可注入数据层",行为字节级不变,9/10 评分,无阻塞项。

**报告生成**: 2026-07-15 01:21
