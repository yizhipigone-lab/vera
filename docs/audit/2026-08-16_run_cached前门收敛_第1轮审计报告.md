# run_cached 前门收敛计划书 · 第 1 轮审计报告

> 成文日期 2026-08-16 · 文档类型 审计报告 · 审计对象 `docs/plan/2026-08-16_run_cached前门收敛_计划书.md`（v1 初稿）

## 总评

架构主干全对（selections 确为死参数、共享段隔离成立、run() 零波及、tuple hack 移出正确、旧壳/新方法不冲突、快照 parity 不破坏），但 **2 处前提性错误 + 1 处漏项**，v1 不能直接照做。

## 必须改（3 处，均已改正）

1. **调用方清单错**：`pipeline/pipeline.py` 的 `run_cached` 是 `StockSelector.run_cached`（选股缓存接缝，P1-3 引入），非 `BacktestEngine.run_cached`；`test_engine_run_path.py` 实际 2 点非 3。真实数字 **22 点 / 14 文件**（5 生产 + 3 研究 + 14 测试），非 24/15。
2. **`close_raw` 错标**：是原始未 ffill 价的 DataFrame/2D 矩阵（只在边界自建 tradable），不是「开关/标量」。
3. **§3.2 漏理由**：只解释 formula_exit_np 不打包，漏了 close_raw 不打包的理由。

## 必须补（1 处，已补）

4. **阶段 4 漏点名 4 个签名守卫测试**：`test_engine_run_path.py` 的 `test_run_cached_positional_params_unchanged` / `test_run_cached_capability_kwargs_keyword_only` / `test_run_cached_capability_kwargs_default_values` / `test_run_cached_forwards_capability_keywords`（后者含硬编码 `src.find('def run_cached(self, close')` 锚点，删壳正名后 find 返回 -1 必炸）。

## 建议改（2 处，均已吸收）

5. 验收 grep "run_cached(" 永远无法清零（StockSelector.run_cached 恒在）→ 已改口径为「只看 BacktestEngine.run_cached 旧签名调用点」。
6. 补「禁止 bump ENGINE_VERSION」约束 → 已加为验收第 7 条。

## 结论

v1 经审计修订为 v2，2 处前提性错误 + 1 处漏项 + 2 处建议全部改正，13 处 `[文件:行号]` 引用机器校验 100% 通过。v2 可进入第 2 轮审计。
