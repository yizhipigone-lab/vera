# run_cached 前门收敛计划书 · 第 2 轮审计报告

> 成文日期 2026-08-16 · 文档类型 审计报告 · 审计对象 `docs/plan/2026-08-16_run_cached前门收敛_计划书.md`（v2 修订版）
> 本文所有 `[文件:行号]` 引用均经 `docs/audit/_verify_references.py` 机器校验。

## 一、第 1 轮修订是否到位（逐条核对）

| # | 第 1 轮结论 | 落地状态 | 证据 |
|---|---|---|---|
| 1 | 调用方清单 24/15 → 22/14 | ✅ 已正确落地（结论数字对），但验收 grep 有精度隐患 | `git grep` 实测 22 个 `BacktestEngine.run_cached` 真实调用点 / 14 文件（5 生产 + 3 研究 + 14 测试），§2.4 分组逐项吻合；无 StockSelector/BacktestEngine 混淆（`pipeline/pipeline.py:94` 是 `self.selector.run_cached`，`selection/selector.py:230` 是 def）。⚠️ 但 raw `git grep -c "\.run_cached("` 返回 **24 matches / 15 文件**（比 22/14 多出 `pipeline.py:94` 一处 + `test_engine_run_path.py:2` docstring 一处），22/14 是「排除后」计数，非 raw 计数 |
| 2 | close_raw 改标「能力矩阵」 | ✅ 已正确落地 | §2.1 已把 close_raw 归入「能力矩阵 ×2」（原始未 ffill 价的 DataFrame）；§3.2 一致地说「close_raw 是只在边界自建 tradable 的原始价 DataFrame（不进共享段）」，与 formula_exit_np 同属「可选能力」 |
| 3 | 阶段 4 补 4 个签名守卫测试（行号 + 硬编码锚点） | ⚠️ 部分落地 | 4 个测试行号 `[353-367]`/`[369-385]`/`[387-406]`/`[408-433]` 逐行吻合；锚点 `src.find('def run_cached(self, close')` 确实在 `[419]`。但**漏了同类锚点的第 2 处**（`[306]`，见新发现 N1） |
| 4 | 验收 grep 口径 + 禁 bump ENGINE_VERSION | ✅ 已正确落地 | §4 阶段 3 验收明确「只看 BacktestEngine.run_cached 旧签名调用点」、排除 StockSelector；§6 #7 「禁止 bump ENGINE_VERSION」；且 ENGINE_VERSION 确实进 matrix_cache key（`matrix_cache.py:14`、`engine.py:452-460`/`[551]`），bump 会改缓存 key 使已有缓存失效，理由属实 |

## 二、本轮新发现

### 必须改

**N1. 漏了第 5 个硬编码锚点：`test_both_entries_call_shared_segment` 也会被删壳正名炸掉**

`tests/test_engine_run_path.py` 里 `src.find('def run_cached(self, close')` 出现 **两处**，不是一处：

- `[tests/test_engine_run_path.py:419]`（`TestRunCachedSignature.test_run_cached_forwards_capability_keywords`）—— 计划书 §4 阶段 4 已点名；
- `[tests/test_engine_run_path.py:306]`（`TestNoNewPositionalParamsAdded.test_both_entries_call_shared_segment`，`[tests/test_engine_run_path.py:302-318]`）—— **计划书漏了**。

删壳正名后 `find('def run_cached(self, close')` 返回 -1 → `run_end=-1` → `cached_body = src[-1:cached_end]` 因 start>stop 变成空串 → `[tests/test_engine_run_path.py:312]` `assert 'self._resolve_stop_and_build_loop(' in cached_body` 必炸。这个测试**不在**计划书点名的 4 个测试里（那 4 个在 `TestRunCachedSignature`，`[tests/test_engine_run_path.py:346]` 起；这个在 `TestNoNewPositionalParamsAdded`，`[tests/test_engine_run_path.py:271]` 起）。阶段 4 必须把 `[306]` 的锚点同步改成 `'def run_cached(self, prepared'`。

### 建议改

**N2. §2.3「第 2 轮审计 H」是循环/错误前向引用**

`[docs/plan/2026-08-16_run_cached前门收敛_计划书.md:75]` 把共享段结论标为「关键结论（第 2 轮审计 H）」。本轮就是第 2 轮审计，写 v2 时它还没发生；第 1 轮审计报告编号是 1-6、没有「H」。疑似从父计划书 `docs/plan/2026-08-16_深模块接口债治理_计划书.md:127` 的「关键澄清（审计补）」复制时误改，会给读者「已过第 2 轮审计」的错觉。改为中性标签（如「关键结论（审计补）」）。

**N3. 验收 grep 的 docstring 残留没点名**

迁移完成后 `git grep "\.run_cached("` 仍会命中 `[tests/test_engine_run_path.py:2]` 的 docstring `engine.run_cached()`（加上 `pipeline/pipeline.py:94` StockSelector 调用、`selection/selector.py:230` 与 `backtest/engine.py:679` 的 def）。§4 阶段 3 只点名排除 StockSelector，没点名这处 docstring。建议验收口径写死「旧签名调用点清零后 grep 应只剩：2 处 def + 1 处 StockSelector 调用 + 1 处 docstring」，免得实现者把 docstring 当漏迁。

**N4. 改写 4 个守卫测试时的两处隐含改动要写明**

- `[tests/test_engine_run_path.py:372-376]` `test_run_cached_capability_kwargs_keyword_only` 的 `required_kw` 集合现含 `open_np/tradable_np/last_tradable_idx` —— 这三者迁入 PreparedMatrix 后不再是 run_cached 的关键字，必须从集合里**删除**（剩下 6 个）。
- `[tests/test_engine_run_path.py:390-399]` `test_run_cached_capability_kwargs_default_values` 的 `expected_defaults` 同理要删这 3 个 key。
- §6 #4 写「位置参数 = 5 个」，但 `[tests/test_engine_run_path.py:355]` 现有测试用 `inspect.signature(BacktestEngine.run_cached)`（类访问、**含 self**），当前断言「10 = self+9」。改写后若仍类访问会得到 6（self+5），不是 5。建议要么断言 6、要么改用实例签名（`engine.run_cached`，不含 self）= 5。计划书没写清这个 self 口径。

**N5. 迁移后 stale 注释/docstring**

- `[tests/test_engine_run_path.py:347-350]` `TestRunCachedSignature` class docstring 还写「锁 10 旧位置参数 + 9 新 keyword-only」「40 调用方依赖」（现 22 调用方、9 位置 + 9 关键字）。改写时顺带改对。
- `[auto_iter/common.py:504]` 「run_cached 不读 selections (仅为签名兼容位), 传空壳」在删 selections 后成死注释，阶段 3 迁移该文件时删/改。

### 验证通过（非问题，记录存档）

**N6. PreparedMatrix 类型对齐：精确，unpack 不会出错**

§3.1 声明 close/entries = `pd.DataFrame`、high_np/low_np/open_np/tradable_np/last_tradable_idx = `np.ndarray`，与实际用法逐一对齐：

- close：`_post_process` 用 `close.index`/`close.columns`（`[backtest/engine.py:778]`、`[backtest/engine.py:785]`）、共享段用 `close.values`（`[backtest/engine.py:357]`、`[backtest/engine.py:390]`）→ 必须 DataFrame；
- entries：`entries.values`（`[backtest/engine.py:748]`）、`_filter_limit_up` 内 `isinstance(entries, pd.DataFrame)`（`[backtest/engine.py:1055]`）→ 必须 DataFrame；
- last_tradable_idx：`recompute_last_tradable_idx` 返回 np.ndarray（`[backtest/degrade_5m.py:260]` 签名已标）；
- `np.ndarray | None` 注解在 `requires-python = ">=3.10"`（`[pyproject.toml:5]`）下合法，无需 `from __future__ import annotations`。

**N7. PreparedMatrix 放置位置：两方案均无 import 顺序问题，建议独立文件**

- 放 engine.py 顶部：np/pd 已在 `[backtest/engine.py:13-14]` import，无冲突。
- 放独立 `backtest/prepared.py`：只依赖 numpy/pandas，engine.py 单向 import，无循环依赖。

**明确建议：独立 `backtest/prepared.py`** —— (1) 与 BacktestResult 放 `backtest/result.py` 的先例一致（`[backtest/engine.py:25]` 已 `from backtest.result import BacktestResult`）；(2) engine.py 已 1095 行，别再往里塞；(3) 22 个调用方 `from backtest.prepared import PreparedMatrix` 不会误拖引擎重模块，符合 AGENTS.md「新功能优先独立新文件」。

**N8. `_filter_limit_up` 非原地改，旧壳不会污染调用方数据（第 1 轮顾虑确认是 non-issue）**

`_filter_limit_up`（`[backtest/engine.py:1048-1081]`）用 `vals = entries.values.copy()`（`[backtest/engine.py:1075]`）+ 返回新 DataFrame（`[backtest/engine.py:1077]`），全程不写 entries。(a) 新方法 `entries = self._filter_limit_up(prepared.entries, close)` 只是局部重绑定，prepared.entries 不变（frozen dataclass 也不受影响）；(b) 旧壳传调用方原 entries 也不会被改。建议计划书风险表加一句「已确认 _filter_limit_up 非原地改（1075 copy）」存档，免得后人再查。

**N9. 没有漏掉的其他非调用方签名依赖**

- `[tests/test_backtest_result.py:49-60]` `test_run_vs_run_cached_key_sets` 直接构造 BacktestResult、只锁返回 key 集合，不调 run_cached，零影响；
- `[tests/test_engine_post_process.py:40-80]` 全部直调 `eng._post_process`，不调 run_cached；
- docs/ 里所有「9/10 位置参数、40 调用方」都是历史日期文档（审计报告/旧计划书），属当时记录不需回改；唯一活文档 `docs/architecture/loop.md` 只讲返回 key 集合、不讲签名。
- 结论：除了 N1 那个源码扫描测试，没有别的「不直调但断言签名/结构」的漏网依赖。

## 三、第 2 轮审计总评 + 剩余必须修改清单

**总评**：v2 对第 1 轮 4 条结论全部正确落地（调用方 22/14、close_raw 归位、4 守卫测试补位 + 行号准确、grep 口径 + 禁 bump 引擎版本）。第 1 轮担心的 PreparedMatrix 类型/放置/旧壳 mutate 三大深挖项，本轮逐一验证后都是**安全**的（N6/N7/N8 证据齐全，无隐患）。**唯一实质漏项是 N1**：删壳正名会额外炸掉 `test_both_entries_call_shared_segment`（`[tests/test_engine_run_path.py:306]` 的第二处硬编码锚点），这是会真让全量测试红掉的硬漏项，必须补。

### 必须改（1 条）

1. **N1**：阶段 4 把 `[tests/test_engine_run_path.py:306]`（`test_both_entries_call_shared_segment`）的 `src.find('def run_cached(self, close')` 一并改成 `'def run_cached(self, prepared'`——计划书 §4 阶段 4 item 5 目前只列了 `[419]` 一处。

### 建议改（4 条）

2. N2：改 `[docs/plan/2026-08-16_run_cached前门收敛_计划书.md:75]` 的「第 2 轮审计 H」标签为中性标签。
3. N3：验收 grep 口径补点名 `[tests/test_engine_run_path.py:2]` docstring 残留。
4. N4：改写守卫测试时写明 (a) 从 keyword-only/默认值集合删 `open_np/tradable_np/last_tradable_idx`；(b) 位置参数断言 self 口径（6 vs 5）说清。
5. N5：迁移时清掉 `[tests/test_engine_run_path.py:347-350]` 与 `[auto_iter/common.py:504]` 的 stale 注释。

**结论**：改完 N1（一条锚点）+ 4 条建议，即可进入最终定稿。不存在需要推翻 v2 主干的前提性错误。
