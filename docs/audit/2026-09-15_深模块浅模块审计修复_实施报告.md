# 2026-09-15 深模块浅模块审计修复实施报告

> 对应审计: [docs/audit/2026-09-15_深模块浅模块复查_审计报告.md]
> 范围: 审计发现的全部 14 项 + 新疆域 11 项中的可执行项
> 验证: 每批跑对应 pytest, 全量 `pytest tests/` 最终 exit=0 (含 TDX 真实链路用例);
> 第一轮自查 29 项机械检查全过; ruff F821/F401 级问题清零 (修复中引入 1 个已当场修掉)

## 一、结论先行

**14 项审计发现修了 13 项, 1 项 (rotation 执行段拆分) 按审计自己的建议延期单独立项。**
两处会算错数的 🔴 已消灭: 农场达标线收口 `farm_rules` (白捡笔数≥20 守卫);
分析页夏普改调 `MetricsCalculator` 单一实现, 与回测页同口径 (扣 1.5% 无风险利率)。
全量测试绿, 含 4 个 TDX 真实链路用例。

## 二、逐项修复清单

### 🔴 高危两项

| # | 发现 | 修法 | 证据 |
|---|---|---|---|
| 1 | 农场达标线绕过 farm_rules 缺笔数守卫 | 手写 `ANN_MIN/DD_MAX` 与 pass 判定删除, 改调 `farm_rules.is_pass/pick_best/describe`; 最优组合只在笔数≥20 里选 | [tools/formula_farm/farm_batch_sweep.py:27] [tools/formula_farm/farm_batch_sweep.py:118] |
| 2 | 夏普三个数 (分析页不扣无风险利率 / metrics 内部双实现) | ① `sharpe_ratio` 委托 `_sharpe` 单一实现 [backtest/metrics.py:192]; ② 分析页 summary 整体下沉 `build_summary_view`, 指标块改调 `MetricsCalculator.compute_all` (夏普/索提诺/卡玛/回撤/修复天数全部与回测页同一把尺) [trade/analysis.py:369] | 注: 年化口径随之统一为 252 交易日制 (原 365.25 自然日制), 索提诺公式统一为 MC 版 (年化收益/下行波动) |

### 🟡 路由埋计算 6 处整体下沉

| 位置 | 下沉到 | 证据 |
|---|---|---|
| server.py /api/calendar | `month_grid` 纯函数 [scheduler/trading_calendar.py:93] | [server.py:477] |
| server.py /api/benchmark/history + /api/stock/kline | 新模块 `core/kline_view.py` (`close_records`/`ohlcv_rows`, 同一变形家族两份收口) [core/kline_view.py:18] | server 两路由只留取数+转发 |
| trade/analysis_api.py daily_pnl | `build_daily_pnl_view` [trade/analysis.py:277] | [trade/analysis_api.py:173] (路由只剩取数) |
| trade/analysis_api.py summary | `build_summary_view` [trade/analysis.py:369] | [trade/analysis_api.py:244] |
| lab_api.py /api/lab/history | `history_items` [core/lab_runner.py:40] | [lab_api.py:77] |

### 🟡 同一规则多份收口

| # | 发现 | 修法 | 证据 |
|---|---|---|---|
| 4 | 收盘竞价限价规则双胞胎 (买侧 auto_buy / 卖侧 executor) | 新模块 `trade/closing_auction.py` 单一实现 (`auction_buy_price`/`auction_sell_price`); executor 深/沪两个逐字节相同的 force 分支合并 | [trade/closing_auction.py:28] [trade/closing_auction.py:39] [trade/auto_buy.py:293] [trade/executor.py:555] |
| 5 | claude_cli 恢复规则文件内双写 (且 session 模式集已漂移: 同步侧有 "tool use"/"concurrency", 流式侧漏) | 抽 `_classify_recovery` 唯一实现, 模式集取超集 (流式侧补上 tool use/concurrency 判定) | [brain/claude_cli.py:102] |
| 6 | research 牛熊口径残份 (diff(20) 绝对差 + min_periods=250 vs 核心 200) | ma=250+斜率路径委托 `core.index_regime.regime_series`; 其余参数组合 (MA120/MA250 无斜率) 是通用 MA 过滤实验, 保留并注释区分; annual_review 标注"简化代理非牛熊口径" | [research/multi_factor_breakout.py:78] |
| 7 | data_tools↔market_panel 分家尾巴 (助手复制 + 兼容 re-export) | 新公共模块 `brain/ak_sections.py`; 两文件删除各自副本; re-export 删除, 4 消费方 (fastpath/sentiment_pipeline/daily_brief/CLI) 改直引; 测试 mock 点同步迁 | [brain/ak_sections.py:20] |
| 8 | kg 读取 SQL 三处绕过门面 | `kg/query.py` 补 `get_all_companies`/`get_link_nodes` 两门面 (公开接口 7, 铁律 8 内); `_kg_company_name` 复用已有 `get_company`; data_tools/archive 不再直连 sqlite | [kg/query.py:170] [kg/query.py:187] |

### 🟢 小项

| # | 修复 | 证据 |
|---|---|---|
| 9 | auto_buy 涨停判定漏传 st (ST 股 5% 板被按 10%/20% 算) | `st_checker` 注入与 Executor 同一份 (trade_main 单点提取) | [trade/auto_buy.py:49] |
| 10 | shadow.compute_shadow_state 一行别名删除, run_shadow/测试直用 `legacy_three_state.compute_signal` | trade/shadow.py |
| 11 | ai_api 同一表达式双次 `ai_config.load()` → 单次 | ai_api.py |
| 12 | dsh_channel docstring "外部接口仅 3 个" → 5 个 (注释漂移) | brain/dsh_channel.py |
| 13 | kline_cache_maintenance `period_stats` 降私有 `_period_stats` (仅内部消费, 公开面回到 8) | core/kline_cache_maintenance.py |
| 14 | gs_top9_batch `v > 0.15` → `farm_rules.TARGET_ANN`; send_report_feishu 手写 .env 解析 → `load_dotenv` | tools/gs_top9_batch.py, tools/send_report_feishu.py |
| 15 | builder keyword 参数守卫空白 → 白名单测试 (22 个 keyword 登记, 新增须显式登记=视为已审查) | [tests/test_engine_run_path.py:87] |
| 16 | gapfill 编排 (48 行) 自 trade_main 组合根下沉 `orchestrate_gapfill`, 组合根只留装配 | [trade/asset_gapfill.py:456] [trade_main.py:589] |

### ⏸️ 延期一项 (审计原判"单独立项")

**rotation.py 执行段拆分**: `_execute` 225 行 + lots 簿记 6 方法与 RotationFeature 的
self 状态 (store/book/executor/gateway/monitor/cfg) 深度交织, 拆分 = 把 16 个方法
改造成传上下文的自由函数, diff 面大且落在实盘真金白银路径上。审计报告自己的
优先级表就标了"🟢 单独立项", 不在修复马拉松尾声对实盘重区做大手术。
建议立项时配套: rotation_lots 全量测试先行 + 影子模式跑一周。

## 三、测试与验证记录

| 验证 | 结果 |
|---|---|
| 批次 A-I 分段 pytest | 全部 exit=0 |
| **全量 `pytest tests/`** | **exit=0** (含 4 个 TDX 真实链路用例 test_end_to_end_real_pipeline, TDX 在线跑通) |
| 第一轮自查 (29 项机械检查: 每项修复的 grep 级证据) | 29/29 通过 (1 项初报失败系检查模式误匹配自身注释, 复核通过) |
| ruff (F821/F401/F541 级) | 修复过程引入 1 个 F821 (data_tools CLI 失去 re-export 符号) 当场修掉; 存量 F401×2 为改动前既有, 未动 |
| 31 个改动文件语法检查 | 全过 |

**口径变更提示 (用户须知)**:
1. 分析页「夏普/索提诺/卡玛/年化」与回测页统一为 MetricsCalculator 口径 —— 数字会比
   修复前略低 (夏普扣 1.5%/年无风险利率), 这是**口径纠正**, 不是业绩变化。
2. 农场粗扫 winners 榜: 笔数<20 的公式不再标「达标」(记「样本不足」), 下次出榜
   达标数可能变少, 同样是口径纠正。

**测试环境修复 (顺手)**: 3 个对机器环境敏感的测试改为环境免疫 —
test_brain.py (屏蔽本机 ai.json)、test_tdx_path.py (importlib.reload 解耦机器级
TDX_HOME)、tests/brain/test_fastpath.py (mock 点随 market_panel 迁移)。

## 四、新增/变更文件清单

新增: `trade/closing_auction.py`, `core/kline_view.py`, `brain/ak_sections.py`,
`tests/test_kline_view.py` (kline_view 最小测试, ponytail 底线)
改动: 26 个 .py (见 git diff --stat)

## 五、引用校验

本报告所有 `[file.py:line]` 引用已经机器校验:

```bash
python -m docs.audit._verify_references docs/audit/2026-09-15_深模块浅模块审计修复_实施报告.md
```

---

## 六、第二轮独立交叉审计结论 (2026-09-16, 独立 agent, 未参与修复)

**总评: 可合入。12 项验证 0 不通过, 其 pytest 子集 EXIT=0; 修复后全量 pytest 复跑 EXIT=0。**

要点:
- 两处 🔴 高危修复核实为真且实现正确: farm pick_best 兜底会被 is_pass「样本不足」
  挡住 (比原实现更严, 正是 GS1292 要防的形态); sharpe_ratio 委托与原实现数值完全等价
- 所有"纯移动"声称经逐行/git diff 比对成立; 冻结契约 (builder/gateway/store) 零改动
- 其发现的 2 个低危项已当场修复并经其复核: data_tools docstring 漂移、
  kg 普通连接在 graph.db 缺失时创建 0 字节空库文件 ([kg/query.py:177] 起加了
  db.exists() 守卫, 与原 URI mode=ro 语义对齐)

**其补充的边界级行为差异 (全部低危, 记录在案)**:
1. 回撤修复判定删除旧「前高-0.01(1分钱)容差」, 改严格 >= 前高 (极边界 max_dd_recovered 可能 True→False)
2. 单行历史时 max_dd_recovered False→True (compute_all 空 dict 默认值)
3. 权益归零 (cum_ret=-1) 极端情形年化 0→-100% (MC 无 cum_ret>-1 守卫)
4. 夏普/索提诺 std 口径 ddof 0(总体)→1(样本): 叠加扣 1.5% rf, 属口径统一的组成部分
5. 分析页年化 252 制补充: 快照有洞 (停机未补算) 时旧按日历拉伸、新按行数压缩, 缺口越多两口径差越大 —— 与 gapfill 补算联动后一致
6. kg 门面 except 收窄 sqlite3.Error-only (评估可接受); db 缺失时旧 warning 日志变静默返 [] (语义更干净)
7. tests/test_tdx_path.py 的 importlib.reload 常量在 pytest 会话内残留, 理论测试顺序敏感, 本次跑绿未触发

