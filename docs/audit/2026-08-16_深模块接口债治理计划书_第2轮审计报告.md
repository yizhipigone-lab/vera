# 深模块接口债治理计划书 · 第 2 轮审计报告

> 成文日期 2026-08-16 · 文档类型 审计报告 · 审计对象 `docs/plan/2026-08-16_深模块接口债治理_计划书.md`（v2）+ 评估报告
> 审计方式：逐条读代码验证第 1 轮修订是否落地 + 深挖第 1 轮未查透点。本文 `[文件:行号]` 均经机器校验。

## 总评

第 1 轮 7 处修订中 5 处干净落地（P1-3 / P0-3 / P1-1 / P1-2 / P2-2 主干都对）。但 **P2-1 的核心数字纠正本身又带进了一个新的前提性错误**（「6 矩阵」枚举错），加上评估报告声称已同步却残留 2 处矛盾项，v2 不能定稿。

## 必须改（2 项，均已在本最终版改正）

1. **`计划书` P2-1「6 矩阵」枚举错误**：原写「close/high/low/open/tradable/last_tradable_idx」，但 `open/tradable/last_tradable_idx` 是 keyword-only 参数，且漏了位置参数 `entries`、`ladder_profits`、`ladder_ratios`。实测签名（`[backtest/engine.py:679-686]`）9 个位置参数 = `close, entries, high_np, low_np, stop_config, selections, ladder_profits, ladder_ratios, n_ladder`。→ 已改为「4 K 线矩阵 + 2 阶梯数组 + stop_config + n_ladder + selections 死参」。
2. **评估报告 2 处未同步**：section 五「删 L0 缓存」→「接缝回收 L0」；section 六「signal_exporter 删」→ 已删（与 section 四「暂不判改」自相矛盾）。→ 均已改正。

## 建议改（8 项，均已吸收进最终版）

3. P2-1「18→~8」算术欠清 + 括注自相矛盾 → 已补「折 7 矩阵为 1 + 删 1 死参」的算术与 keyword 能力数据归属。
4. P0-1「立即缓解」会禁用全部预筛未提代价 → 已改为「未知策略检测」机制（比对 dispatcher.strategies 与已知集合，仅未知策略才恒 True），并承认当前 8 策略是现状而非 bug。
5. P2-2 漏 tick_ts_missing 第三重分歧 → 已补（rotation/monitor 显式查该标志、executor 不查）。
6. `engine.py:690`/`:694` docstring 仍写「40 调用方」→ 已纳入 P2-1 验收项。
7. P2-1「必然连带改共享段」过强 → 已降级为「可隔离」（共享段接收逐个矩阵参数，边界 unpack 即可不动 run()）。
8. 附录漏「合并日历方法」→ 已补进「不做」清单。
9. 「executor 顶满 8」未闭环 → 已补「暂不拆、观察」进附录。
10. 「19 个文件」口径混用 → 已改为「22 调用点（8 生产 + 6 测试文件）」。

## 结论

修完「必须改」2 项 + 全部 8 项建议后，计划书进入最终定稿（v3）。最终版 12 处 `[文件:行号]` 引用、评估报告 20 处引用均经 `docs/audit/_verify_references.py` 机器校验 100% 通过。
