# VERA 优化工作计划书 v2（施工版）

- **制定日期**：2026-07-02（周四）
- **版本**：v2 — 合并 [2026-07-02_计划书评估报告.md](2026-07-02_计划书评估报告.md) 的核验修订
- **依据**：[2026-07-02_全局架构审计报告.md](2026-07-02_全局架构审计报告.md) + 评估报告 + 与用户讨论校准
- **目标**：把回测从"不可信的尺子"修成"可信的尺子"，再补齐系统性偏差、工程化与实盘体验
- **总工期**：约 5-7 周（P0 1-4 天 / P1 2-3 周 / P2 2-4 周）

> **v2 修订说明**：v1 有 6 处上下文依赖没摸清、3 处照抄了审计错误结论。v2 已逐项对照源码修正，标注 `🔧 v2 修订` 的行为变更项。详见评估报告第 2、3 节。

---

## 0. 讨论校准结论（已与用户确认）

| 项 | 处理 |
|---|---|
| C2 阶梯止盈按高档成交 | **接受**，盘中限价单可成交；细微偏差（低档按高档计）已知，OHLC 无法区分，可接受 |
| C3 同日止损+止盈矛盾 | **接受**，悲观保守没问题；方向记档（高估亏损/低估收益），保留开盘价优先法对照 |
| H1 默认值与 yaml 不符 | **可选兜底**，入口若总传完整配置则碰不到；建议默认值对齐 yaml 作零成本兜底 |
| H2 基准策略扣费指数不扣 | **不做**，看超额只看方向 |
| H3 超额收益年化公式 | **修**，尤其周线回测年化高估 4.8 倍 |
| H4 涨停板卖出限制 | **撤回**，代理方向搞反，涨停卖得出，问题只在跌停 |
| H5 阶梯部分卖出后跳过剩余检查 | **接受**，与 C3 同款保守处理，偏差小 |
| M1 缺沪深300/中证500 | **做**，加指数 |
| M2 基准复权口径 | **不做** |

---

## P0 — 让尺子重新可信（1-4 天）

> 完成后回测绝对值才可参考。建议修完跑一次 GUPIAO_012 对照量化效果。

| # | 任务 | 位置 | 工作量 | 验收标准 |
|---|---|---|---|---|
| P0-1 | 删除 3 处 `.bfill()`，仅保留 `.ffill()`；停牌日加 `tradable` 掩码跳过成交。**🔧 v2 修订**：engine 的 `get_kline` 调用必须同时加 `fill_data=False`，否则 TDX 在源头已前向填掉停牌 NaN，删 bfill 变成空操作 | `engine.py:368,376,385`、`engine.py:356`（加 `fill_data=False`） | 1h | 停牌日不再出现未来价成交；涨停检测对停牌后复牌股生效；停牌 NaN 显式暴露而非被源头填平 |
| P0-2 | 删除 engine/stop_manager/metrics 的 `.get(key, default)` 默认值，缺失即 raise；或至少把默认值改成与 yaml 一致（-0.12/0.08/0.05/100万/2万）。**🔧 v2 修订**：必须同时改 `run_cached` 路径的同款默认值，否则 18+ 个批量优化脚本继续漂移 | `engine.py:317,327,423-425`、**`engine.py:510-512`（run_cached，v2 新增）**、`stop_manager.py:74-81`、`metrics.py:19` | 1.5h | 配置字段缺失时报错而非静默用错值；默认值与 default.yaml 逐项一致；`run()` 与 `run_cached()` 两条路径默认值一致 |
| P0-3 | selector.py 接入 `filter_stocks`，删除 line 64 字符串过滤；engine.py:628 改用 `IsSTGP` 判 ST | `selection/selector.py:64`、`engine.py:628` | 1h | ST/*ST 股从选股池剔除；ST 股按 5% 涨停阈值 |
| P0-4 | `git add` 核心源码与 tests/ 入库；`.claude/` 加入 .gitignore | 仓库根 | 0.2h | `git status` 干净；tests/ 被 git 跟踪 |
| P0-5 | 翻转 `enable_realistic_costs` 默认为 True；default.yaml 加 `enable_realistic_costs: true`、`stamp_tax: 0.0005`。**🔧 v2 修订**：无需改 server.py——server 不传该字段，翻转 engine 默认后 server 路径自动拿到 True | `engine.py:322`、`default.yaml` | 0.3h | 默认路径含印花税+滑点；老脚本可显式传 False 复现旧基线；server 路径自动生效 |

**P0 验证**：跑 GUPIAO_012 回测，对比净利/夏普/最大回撤与修复前基线（`analyze_gupiao012.py` 已有历史数字），记录变化。

**🔧 v2 新增 — 回归基线锚**：P0 动手前先固化一个"修复前快照"——commit 当前状态 + 跑一次 GUPIAO_012 存 JSON（含成本口径标注），否则改完没有可比基准。

---

## P1 — 消掉系统性偏差（2-3 周）

| # | 任务 | 位置 | 工作量 | 验收标准 |
|---|---|---|---|---|
| P1-1+P1-2 | **🔧 v2 合并**：跌停日成交价取 `min(stop_price, open_price)`；连续跌停顺延至可成交日。买入价改 T+1 开盘价，entry_idx=i+1。**前置依赖**：engine 当前只取 Close/High/Low，必须加取 Open 列、`_simulate_core_v3` 签名加 `open_np` 参数、贯穿 high_np/low_np 同款链路。**耦合风险**：`_filter_limit_up` 用 `close.shift(1)` 判涨停，entry 后移一天后 prev_close 基准要同步调整，否则引入新前视 | `engine.py:153-156`（跌停）、`engine.py:233-234,279,598-615`（买入）、`engine.py:356-364`（加取 Open）、`_simulate_core_v3` 签名、`engine.py:617-636`（涨停过滤基准） | 1-2 天 | 信号日次日开盘成交；跳空跌停按开盘价成交而非止损线；涨停过滤基准与 T+1 entry 语义一致，无新前视 |
| P1-3 | 停牌日 forward-fill 最后已知价 mark-to-market；退市触发强制平仓记 `exit_reason=退市` | `engine.py:69-70,292` | 3h | 停牌仓位不归零；退市股清仓不卡死 |
| P1-4 | 修 intraday 年化公式：`periods_per_year/len(equity)`（替换 metrics.py:40 的字面量 `252.0`）；1w 的 periods_per_year 改 52。**🔧 v2 修订**：①验收方向纠正——5m 当前是**低估**（修完后年化上升），1w 当前是**高估**（修完后年化下降）；②补 benchmark.py 同款 bug；③顺手清 `trading_days` 死变量 | `metrics.py:38-41`、`engine.py:313`、**`benchmark.py:42,131`（v2 新增）**、`metrics.py:33-35`（清死变量） | 1.5h | 5m 年化上升至正确值、1w 年化下降至正确值；benchmark 超额年化同步修正 |
| P1-5 | 超额收益改几何：`(1+str)/(1+idx)-1`，年化用复合 | `benchmark.py:130-132` | 1h | 超额收益几何口径 |
| P1-6 | 加沪深300/中证500 指数；默认指数篮子从 config 读取。**🔧 v2 修订**：必须同时改 `data_fetcher.py:INDEX_CODES`——否则 yaml 写 "hs300" 但 `get_index_data` fallback 把字符串 "hs300" 直传 TDX 取数失败。核对 zhongzhengA500 的 `000510.SH` 代码是否正确 | `benchmark.py:21`、`default.yaml:64-69`、**`data_fetcher.py:17-22 INDEX_CODES`（v2 新增）** | 1.5h | 默认含 hs300/zz500/shanghai 等；指数代码映射到 TDX 实际可取代码 |
| P1-7 | selector/engine 入口接入 `assert_consistent` 校验复权口径 | `selector.py`、`engine.py` 入口 | 0.5h | 复权口径不一致时报错 |
| P1-8 | trades 数组上限触发时 logger.warning + 扩容，不再静默截断。**🔧 v2 修订实现建议**：numpy 数组循环中途扩容成本高，改 `list` append + 末尾 `np.array` 比动态扩容简单 | `engine.py:56,189,208` | 2h | 超限有告警；trades_df 完整 |
| P1-9 | **🔧 v2 删除**：engine.py:295 注释明确"期末不平仓，按市值计入"，当前 mark-to-market 设计对"量策略"合理（实盘也可能继续持有）。原"补扣卖出费"是 hybrid 口径（没卖却扣费）逻辑不自洽。若用户要求期末平仓口径，则单独立项重定义为"期末真实平仓扣费"，不在此处夹带 | `engine.py:295-303` | — | （删除；如重定义则单独立项） |

---

## P2 — 工程化与实盘体验（2-4 周）

### P2-A 安全与体验（优先）

| # | 任务 | 位置 | 工作量 |
|---|---|---|---|
| P2-1 | `/api/run` 改同步路由（`def` 非 `async def`，FastAPI 自动丢线程池），或 BackgroundTasks 后台队列；`/api/status` 独立线程实时返回 | `server.py:192` | 4h |
| P2-2 | CORS 收紧为白名单/null；`/output` 静态挂载加鉴权或移除；`result_id` 正则校验 `^\d{8}_\d{6}$` | `server.py:31,34,475` | 2h |
| P2-3 | 前端补"导出 CSV/报告"按钮（直链 output/reports）；历史列表支持 A vs B 对比 | `web/index.html` | 6h |
| P2-4 | 报错改页面内 toast + 中文友好提示；后端按错误类型映射中文 | `index.html:584`、`server.py:447` | 3h |
| P2-5 | 修回归 bug：移除页面加载自动拉 last_result（与提交 40db325 一致） | `index.html:837-845` | 0.3h |
| P2-6 | `/api/run` 失败时清空 KPI 为 `--`；历史 cumulative_return null 防御 | `index.html:582-586,856` | 1h |

### P2-B 工程化基线

| # | 任务 | 位置 | 工作量 |
|---|---|---|---|
| P2-7 | 13 个硬编码 STOP_CONFIG 脚本迁到 `load_stop_config()` | 见报告 H1 工程化清单 | 4h |
| P2-8 | 引入 `pyproject.toml`（ruff + pytest 配置）+ 最小 GitHub Actions（lint+test） | 仓库根 | 3h |
| P2-9 | 生成 `requirements.lock`；matplotlib 提为必需。**🔧 v2 备注**：requirements.txt 当前未入 git，一并 add | `requirements.txt` | 0.5h |
| P2-10 | 38 处裸 `except:` 改 `except Exception as e: logger.exception(...)`；批量脚本失败记公式名/标的/异常 | 全项目 | 4h |
| P2-11 | 补 `stop_manager`/`formula_runner`/`pipeline` 行为测试；加 `conftest.py` 去 sys.path hack | `tests/` | 8h |
| P2-12 | 根脚本统一 `from utils.logger import get_logger`；替换 1145 处 print（保留结果展示） | 30+ 顶层脚本 | 6h |
| P2-13 | 5 个脚本改回走 `BacktestEngine.run_cached()`，不再直调私有 `_simulate_core_v3`（已核实：backtest_wr_rsi_kdj / backtest_2022_2026 / optimize_full / optimize_strategies / test_first_day） | 见报告 H11 清单 | 3h |

---

## 验证与回归计划

1. **P0 完成后**：跑 GUPIAO_012 对照，记录净利/夏普/回撤变化（预期净利进一步下降、夏普下调、回撤上调，方向更接近实盘）。
2. **P1 完成后**：跑一次 5m 周期回测，验证年化**上升至合理值**（非下降）；跑一次 1w 周期回测，验证年化**下降至合理值**；跑一次含停牌/ST 股的策略，验证成交价合理。
3. **P2 完成后**：server.py 实跑一次完整回测，验证进度条实时刷新、报错友好、导出可用。
4. **回归基线**：每完成一个 P0/P1 项，跑一次既有测试（`pytest tests/`），确保不破坏现有路径（规则 3）。

---

## 不做项（明确排除）

- H2 基准策略扣费指数不扣（用户看超额只看方向）
- M2 基准复权口径（用户不管）
- H4 涨停板卖出限制（已撤回，方向错）
- 涨停板卖出流动性检查（涨停卖得出）
- **🔧 v2 降级**：C3 的 server 路径默认值风险——server.py:74-88 自带正确 DEFAULTS 层，inline fallback 是死代码，无需改动。C3 真正风险面是直接 `BacktestEngine({...})` 且不传全 stop_config 的根脚本，由 P0-2 兜底覆盖

## 已知保留偏差（接受，知会用户）

- C2：盘中慢涨路径下低档仓位按高档成交价，偏乐观，影响有限
- C3：同日止损+止盈取悲观安全侧，高估亏损/低估收益
- H5：阶梯部分卖出后剩余仓位移动/时间止损延后一个 bar

---

## v2 修订汇总（相对 v1 的差异）

| 编号 | v1 问题 | v2 修订 |
|---|---|---|
| P0-1 | 删 bfill 但不碰 `fill_data=True`，TDX 源头已填 NaN → 删 bfill 是空操作 | 绑定 `fill_data=False` |
| P0-2 | 漏 `engine.py:510-512`（run_cached 同款错误默认值） | 补入，覆盖批量优化路径 |
| P0-5 | 引用 `server.py:77,137` 是错的，server 不传该字段 | 删除 server 同步项，翻转 engine 默认即自动生效 |
| P1-1/2 | 工期 3h+2h 远不够；漏 Open 列依赖与 `_filter_limit_up` 耦合 | 合并，工期改 1-2 天，显式列出 Open 贯穿与涨停过滤基准调整 |
| P1-4 | 5m 方向搞反（实际低估非高估）；漏 benchmark.py 同款 bug；死变量未清 | 验收方向纠正，补 benchmark.py:42,131，清 trading_days |
| P1-6 | 只动 yaml 不动 `INDEX_CODES`，hs300/zz500 取数会失败 | 补 `data_fetcher.py:INDEX_CODES` |
| P1-9 | "补扣卖出费"与"期末不平仓"设计冲突，hybrid 口径不自洽 | 删除；如需平仓口径单独立项 |
| C3 | 审计夸大 server 路径风险（server 自带正确默认层） | 降级，server 不动 |
| 工期 | P0 0.5-3 天 / P1 1-2 周 | P0 1-4 天 / P1 2-3 周 |
| 回归基线 | 缺数值锚 | P0 前先固化"修复前快照" JSON |

---

**⏱ 时间戳**
- 📅 当前时间: 2026-07-02 (周四)
- 🕐 本次回答经过: 约 1m
