# 2026-09-16 公式农场看板 审计报告（含深/浅模块设计质量评估）

## 审计对象

提交 `eb37198`（公式农场看板：流水线卡片 + 总览漏斗与达标榜，按计划书 `docs/plan/2026-09-16_公式农场流水线卡片与总览看板_计划书.md` 四阶段落地）及本轮审计驱动的修复（随本报告同提交入库）。

## 审计方法（五轨并行）

1. **独立对抗性复审**：子代理通读全部改后文件 + 真实数据实测 + cProfile 剖析 + 正则在真实历史报告上机器验证，输出 2 HIGH / 5 MEDIUM / 5 LOW；
2. **自审**：逐文件通读 + 性能实测（ payload 称克数、热缓存计时、cProfile 定位）；
3. **深/浅模块设计评估**：按开工前深模块检查单五问逐文件定性（放哪层/接口多大/有无第三份/删除测试/登记）；
4. **机器检查**：ruff 全绿、node --check JS 语法、农场测试 54 条、全量套件（除 trade）EXIT=0；
5. **引用机器校验**：本报告全部 `[file:line]` 引用经 `docs/audit/_verify_references` 校验（见文末）。

## 总体结论

**复审初判「需修复后合入」，修复后复核：可合入。** 无 CRITICAL、无数据正确性事故；2 条 HIGH（均为"每 2 秒付一次"的持续性能成本）与 5 条 MEDIUM、5 条 LOW **全部修复并补测试**。功能经真实数据冒烟验证（漏斗 1235→822→2/2，达标榜回填 822 条：达标 7 / 样本不足 118）。

## 已修复 — HIGH ×2（性能，均实测复现+修复后复测）

| # | 问题 | 修复 | 效果 |
|---|---|---|---|
| H1 | `_done_files(root)` 写在列表推导式条件里，对 1055 条 vetted 每条调用一次，每次命中缓存也先 glob+stat——cProfile 实测单次 status 约 0.37~0.48 秒，服务器约 1/4 CPU 在重复扫目录 | 提出循环（`[core/farm_summary.py:157]`） | 热缓存单次 **367ms → 9.5ms**（约 39 倍） |
| H2 | overview 全量 822 条归档塞进每次 status 响应，实测 **350,346 字节**（343 KB/次 × 每 2 秒） | 榜单截尾下发：行按组截尾（`[core/farm_summary.py:262]`，pass 200/样本不足 50/未达标 30），计数全给 `board_totals`，前端标注「仅列前 N 条」 | **343 KB → 38 KB**；复审的更优方案（拆独立端点/签名门控下发）已记录为后续跟踪项，本次选截尾因改动面最小且计数保真 |

## 已修复 — MEDIUM ×5

| # | 问题 | 修复 |
|---|---|---|
| M1 | `_CACHE` 无界增长，每日 ~1MB 的 check.json 解析结果永久驻留（长跑内存泄漏） | LRU 容量 32 条淘汰（`[core/farm_summary.py:28]` / `[core/farm_summary.py:33]`） |
| M2 | core→tools 分层倒挂（farm_summary 引 tools.formula_farm.common） | 账本三函数迁正主 `core/farm_ledger.py`（`[core/farm_ledger.py:19]` / `[core/farm_ledger.py:43]` / `[core/farm_ledger.py:60]`），tools 引 core 与 farm_rules 先例同向；common.py 兼容再导出（`[tools/formula_farm/common.py:82]`），旧调用方零改动 |
| M3 | common.py 杂物抽屉化（token/飞书/账本混装） | 随 M2 账本族迁出而瘦身（150 行 → 85 行），并清理不再使用的 glob/json/time 导入 |
| M4 | 判定装配三份手写（build_report/archive_entry/_stats_of 的 pick_best→verdict 及 invalid 字典逐字重复）+ farm_verify 三态两份（summarize/concl_of）——md 报告与 json 数字互相矛盾的隐患 | 装配唯一实现 `_verdict_of_rows`（`[tools/formula_farm/farm_backtest.py:185]`）三处共用；summarize 改走 concl_of（`[tools/formula_farm/farm_verify.py:149]`） |
| M5 | 缓存签名只取 max mtime：保留时间戳恢复旧目录/删除非最新文件 → 索引陈旧显示错 | 签名改 (文件数, 最大 mtime)（`[core/farm_summary.py:84]`），补专测（旧 mtime 拷入场景） |

## 已修复 — LOW ×5

1. **L1 TOCTOU**：glob 完到 stat 之间文件被删会抛异常 → `_mtime` 安全版（`[core/farm_summary.py:53]`），`_latest`/`_glob_sig`/verify.json 排序三处共用；
2. **L2 坏 check.json 解锁②**：解析失败回落 `{}` 被 `is not None` 当成"已检查" → 改 `bool()` 判定（`[core/farm_summary.py:222]`），补专测；
3. **L3 告警刷屏**：fail-soft 持续失败每 2 秒一条带堆栈 warning → 只记一次、恢复自动复位（`[core/farm_runner.py:144]`）；
4. **L4 读日志白付全量汇总**：`/api/farm/log` 原经 `farm.status()` 取 last → 新增轻量 `last_status()`（`[core/farm_runner.py:149]`），接口改走它（`[core/farm_api.py:70]`），补专测（全程不触发 gate_summaries）；
5. **L5 前端 XSS 两小洞**：来源链接只放行 http(s) 协议（`[web/js/vera-ui.js:565]`，url 源自股旁网抓取 HTML，`escAttr` 不拦 `javascript:` 协议）+ `trades` 字段补转义。

## 测试缺口补锁（复审指出四类，全部补上）

- **缓存失效主场景**：mtime 变化后必须读到新内容（`test_read_json_cache_invalidates_on_mtime_change`）；
- **签名边界**：旧 mtime 拷入文件（文件数变、max mtime 不变）必须刷新（`test_onboard_sig_covers_file_count_not_just_max_mtime`）；
- **生成器措辞与兜底正则防漂移锁**：`build_verify_report`/`build_report` 的**当前输出**直接喂兜底正则——措辞哪天改了，测试先红而不是页面静默退化（`test_verify_md_generator_matches_fallback_regex` / `test_backtest_md_generator_matches_fallback_regex`）；
- **`_CACHE` 跨测试隔离**：autouse fixture 清理（此前靠 tmp 路径不同碰巧安全）。另修正名不符实的 `test_backtest_json_and_md_fallback` → `test_backtest_json_card`。

## 深/浅模块设计质量评估（逐文件定性）

| 文件 | 定性 | 评语 |
|---|---|---|
| `core/farm_summary.py` | **深模块，修复后达标** | 公开接口仅 2 个（`[core/farm_summary.py:211]` / `[core/farm_summary.py:265]`），背后藏缓存/glob/兜底正则/漏斗聚合 ~300 行实现；删除测试通过（删掉它，复杂度会散进 farm_runner.status() 和前端）。扣分项 H1/M1/M5 已修 |
| `core/farm_ledger.py` | **深模块，好** | 接口 3 个纯函数，零外部依赖；账本族高内聚、有共同变化理由；分层位置正确（core） |
| `core/farm_runner.py` status() | **恰当的薄组合** | 组装 + fail-soft；函数内 import 经评估**保留**——让闸门在汇总依赖坏掉时仍可启动，属有意识的健壮性选择（复审认可，L3 刷屏已修） |
| `tools/formula_farm/common.py` | **由杂物抽屉回落至可接受** | 账本族迁出后剩 token/哈希/飞书，150→85 行；立下规矩：新函数进 common 前先找更专的家 |
| `tools/formula_farm/farm_verify.py` | **收口做对了** | concl_of 统一 md/json/summarize 三处结论（`[tools/formula_farm/farm_verify.py:134]`），防漂移正确姿势 |
| `tools/formula_farm/farm_backtest.py` | **修复后达标** | M4 装配唯一化后，md 报告与 json 同源不再可能互相矛盾 |
| 前端 vera-ui.js 农场段 | **薄渲染层，合格** | 业务判据（ready/hint/note）全在后端，前端只做格式化与展示，符合项目"业务判断全在后端"惯例 |
| `core/farm_api.py` | **薄，零赘肉** | 本轮仅改一行（L4），路由薄+实现厚惯例保持 |

## 明确无发现的类别

- **数据正确性**：判定阈值/最优评选全部走 `core/farm_rules` 单一真相源，本轮只做装配不做规则；md 兜底正则在**真实历史报告**（09-11 复核、09-11/09-16 粗扫）上机器验证匹配，09-08 旧格式优雅降级不炸；
- **安全（除已修 L5）**：其余字段逐字段核查 esc/textContent 到位；日志面板 textContent 渲染无 XSS；
- **并发**：FarmRunner 锁使用无新问题（status 的文件 I/O 在锁外，fail-soft 状态标志读写原子）。

## 遗留跟踪项（记录在案，本轮不做）

1. **H2 的进一步方案**：overview 拆独立端点或 status 按内容签名门控下发——截尾后 38 KB/2 秒可接受，归档继续长大（数千条）时再启动；
2. **intake 递归 glob** 是唯一不缓存的大目录扫描（当前 1235 文件 ~4ms），农场长大到数万条时改目录 mtime 缓存；
3. `common.py` 公开符号已 7 个，接近铁律 8 上限——再塞新功能前应先拆分。

## 验证记录

- ruff：8 个改动 Python 文件 **All checks passed**；JS `node --check` 通过；
- pytest：农场相关 **54 条全绿**；全量套件 `pytest tests/ --ignore=tests/trade` **EXIT=0**；
- 性能复测：热缓存 9.5ms/次（原 367ms），overview 载荷 38 KB（原 343 KB）；
- 本报告引用机器校验 `python -m docs.audit._verify_references`：**总引用 19 · 有效 19 · 无效 0 · PASS**。
