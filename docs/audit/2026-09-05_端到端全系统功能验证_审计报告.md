# VERA 端到端全系统功能验证 审计报告

> 审计日期：2026-09-05（晚间 23:30–23:55）
> 审计类型：全系统端到端功能验证（实际执行，非静态推断）
> 验证方式：每项结论均来自当次实测命令输出，证据留痕见文末附录
> 范围：静态健康 / 单元测试 / 真数据回测全链路 / Web 主服务 API / 实盘交易服务 API / 定时调度器 / 数据缓存层

---

## 1. 结论

**系统整体健康，未发现功能故障。** 四大子系统（回测研究、Web 平台、实盘交易、定时调度）全部实测通过。发现 3 项观察项（均非缺陷），其中 1 项涉及未提交的并行改动，建议用户确认归属。

---

## 2. 验证基线

| 项 | 值 |
|---|---|
| Git HEAD | `edb96a9`（chore(clean): 死代码+无用import清理） |
| Python | 3.13.13 |
| 核心依赖 | fastapi / uvicorn / pyyaml / pandas / numpy / pydantic 均可导入 |
| 外部环境 | 通达信 `E:\NEW_TDX` 存在，tqcenter 插件存在；QMT xtquant 已安装 |
| 运行中进程 | `server.py`(pid 31092)、`trade_main.py --config config/trade.yaml`(pid 49808)、`-m scheduler`(pid 52600)，均 2026-09-05 19:07 启动 |

---

## 3. 验证明细（8 项全过）

### 3.1 静态编译检查 — PASS

- 命令：`python -m compileall -q`（覆盖 backtest / core / pipeline / selection / report / scheduler / trade / brain / policy_pipeline / policy_kb / concept_kb / kg / llm / obsidian / evolution / notes_gen / auto_iter / utils / qmt 及 main.py / server.py / lab_api.py / research_api.py / data_cache_api.py / config_mapper.py）
- 结果：退出码 0，零输出（无语法错误）

### 3.2 全量单元测试 — PASS

- 命令：`python -m pytest tests -x -q --tb=short`
- 结果：**退出码 0**，进度条无 F/E 标记（零失败零错误），少量 `s` 环境性跳过；含真实 TDX 数据链端到端测试（`tests/test_end_to_end_real_pipeline.py` 等，TQ 连接正常建立并关闭）
- 备注：进程退出时 atexit 输出与 pytest 摘要行交叠，精确通过数未单独摘出；以退出码 0 + 零失败标记为判据
- 交叉事件：复跑 `--collect-only` 时曾瞬时抓到 3 个 brain 测试文件 `IndentationError`，数分钟后同批文件全部恢复可编译——系另一会话并行编辑 `brain/data_tools.py` 与新增 `tests/brain/test_data_tools_resilience.py` 的瞬时中间态（详见观察项 O-1），不构成本次基线的测试失败

### 3.3 真数据端到端回测全链路 — PASS

- 方式：临时配置 `.tmp_e2e_verify.yaml`（custom 股票池 5 只大盘股：600519 / 000001 / 600036 / 300750 / 601318；UPN 公式参数 3；日线；20250101–20251231；资金 20 万；max_positions 3；四套出场机制全启用），`python main.py --config` 执行
- 五步链路全部走通：**配置解析 → 选股公式（经 TDX）→ K 线缓存取数 → 回测引擎 → 指标/基准/报告落盘**，退出码 0
- 核心指标：81 笔交易，胜率 77.8%，PF(利润因子) 2.12，累计 +1.42%，最大回撤 -2.50%，平均持有 8.5 天
- 出场机制参与证据：JSON 报告 `stop_config` 字段完整记录四套机制（移动止盈优先 + 成本止损 -10% + 阶梯止盈 6%/15% + 时间止损 15 天）
- 基准对比链路正常：日志 Step 3/5 显示策略累计 1.014 vs 上证指数 1.216（超额 -16.63%，玩具配置跑输属预期，验证的是链路而非策略水平）
- 产物：`output/reports/E2E验证_均线金叉_20260905_234643.html`（153,861 字节）+ 同名 `.json`（19 项指标）
- 临时配置与日志文件验证后已清理

### 3.4 Web 主服务（8080）— PASS（15 个接口）

| 接口 | 状态 | 响应规模 |
|---|---|---|
| `GET /`（前端首页） | 200 | 86,334 B |
| `GET /docs`（FastAPI 文档） | 200 | 1,010 B |
| `GET /api/status` | 200 | 95 B |
| `GET /api/config/defaults` | 200 | 1,654 B |
| `GET /api/sectors` | 200 | 5,417 B |
| `GET /api/calendar` | 200 | 1,432 B（2026-09-05 正确标记为非交易日） |
| `GET /api/results` | 200 | 12,426 B |
| `GET /api/last_result` | 200 | 1,511,843 B（含交易明细） |
| `GET /api/benchmark/history?index=shanghai&days=120` | 200 | 937,635 B |
| `GET /api/stock/kline?code=600519&start=20250825&end=20260905` | 200 | 250 根日线，前复权，末根 2026-09-03 close 1298.88 |
| `GET /api/data_cache/status` | 200 | 三 period 统计齐全（见 3.7） |
| `GET /api/data_cache/log` | 200 | 补拉日志可读 |
| `GET /api/lab/status` | 200 | `{"running":false,"current":null,"queue":[]}` |
| `GET /api/lab/history` | 200 | 含历史公式体检记录（QUANTQQ 等） |
| `POST /api/research/chat` | 200 | 路由与处理器正常（空问句优雅返回，无 500） |

注：初次用 `limit=5` 参数调 `/api/stock/kline` 返回全量 597KB——该端点实际参数为 `start/end`（见 server.py:527-532），`limit` 被忽略属调用方参数误用，非接口缺陷。

### 3.5 实盘交易服务（8081）— PASS（5 个只读接口）

| 接口 | 状态 | 关键内容 |
|---|---|---|
| `GET /api/trade/status` | 200 | `connected:true, kill_active:false, reconciled:true, session:"closed"`（深夜收市状态正确） |
| `GET /api/trade/asset` | 200 | 总资产 ¥1,062,153.32 = 现金 527,984.52 + 市值 534,168.80，与昨资产一致 |
| `GET /api/positions` | 200 | 4,511 B 持仓明细 |
| `GET /api/trade/orders` | 200 | 空挂单（收市时段正常） |
| `GET /api/trade/config` | 200 | 1,556 B |

写接口（buy/sell/kill 等）本次一律未触碰。

### 3.6 定时调度器 — PASS

- 进程常驻（pid 52600，`python -m scheduler`），模块导入冒烟通过
- `data/scheduler_state.json`：`sentiment_daily` / `kline_cache_refresh` / `weekly_evolution` 三个任务最后成功执行日均为 2026-09-04（最近交易日），无漏跑

### 3.7 数据缓存层 — PASS（含自愈机制实证）

- manifest.db 统计：1d 5,756 只（至 20260903）、5m 6,114 只（至 20260903）、1m 5,539 只（至 20260903）
- `/api/data_cache/status` 显示 expected=20260904，5m/1d/1m 均差 1 天
- **自愈实证**：server 启动自检的后台补拉于当晚 22:42 起持续工作（`/api/data_cache/log` 可见取数记录）——"调度器 15:45 定时补拉 + server 启动自检"双保险设计实际生效

---

## 4. 观察项（非缺陷）

### O-1 【建议确认】存在未提交的并行改动
- 证据：`git status` 显示 `M brain/data_tools.py`（+52/-6 行）；新增未跟踪文件 `tests/brain/test_data_tools_resilience.py`（2026-09-05 23:37 写入）
- 影响：本次验证全程与其共存；3.2 节所述瞬时 `IndentationError` 即来自该会话的中间写入态，数分钟后自愈
- 建议：**若用户不知此改动归属，建议确认来源**；该改动未包含在本次验证基线 `edb96a9` 的测试结论内，落地前需自行跑全量测试

### O-2 【设计如此】JSON 报告不含基准对比与交易明细
- 基准对比仅出现在日志与 HTML 报告；JSON 仅 19 项指标 + stop_config。历史行为一致，如需机器可读基准数据可作后续增强项

### O-3 【自愈中】缓存落后 1 个交易日
- 数据至 20260903，expected 20260904；双保险补拉机制已在工作，预计无需人工干预

---

## 5. 与历史基线对比

| 项 | 2026-07-23 全系统验证 v2 | 本次 2026-09-05 |
|---|---|---|
| 单元测试 | 860 个，3 失败（parity 甲骨文缺参，CRITICAL） | 全过，退出码 0 |
| 端到端真数据回测 | 未单独执行 | 执行，五步链路全通 |
| Web API | 验证过（发现单位错位等 3 MEDIUM） | 15 接口全 200 |
| 实盘服务 | 未覆盖 | 5 只读接口全 200，对账一致 |
| 调度器 | 未覆盖 | 3 任务按时执行 |

当时报告的 CRITICAL-1（`_simulate_core_v3_legacy` 缺 `trailing_gap_protection`）与各项 MEDIUM，在本基线测试全过的结果下视为已修复。

---

## 6. 附录

### A. 端到端回测临时配置（已清理，留档备查）

```yaml
selection:
  formula_name: "UPN"
  formula_arg: "3"
  universe:
    type: "custom"
    stocks: ["600519", "000001", "600036", "300750", "601318"]
  period: "1d"
  dividend_type: 1
time_range:
  start: "20250101"
  end: "20251231"
backtest:
  initial_capital: 200000.0
  position_sizing: { max_positions: 3 }
stop_loss:
  cost_stop: { threshold: -0.10 }
  trailing_stop: { drawdown: 0.04 }
  time_stop: { max_hold_days: 15 }
benchmark:
  indices: ["shanghai"]
```

### B. 产物与证据位置

- 回测报告：`output/reports/E2E验证_均线金叉_20260905_234643.html` / `.json`
- 缓存清单：`data/kline_cache/manifest.db`
- 调度状态：`data/scheduler_state.json`
- 临时文件（`.tmp_e2e_verify.yaml` / `.tmp_pytest_log.txt` / `.tmp_e2e_backtest_log.txt` / `.tmp_api_check.ps1`）：验证完成后已删除

### C. 复验命令速查

```powershell
python -m compileall -q backtest core pipeline selection trade scheduler   # 语法
python -m pytest tests -x -q                                              # 全量测试
python main.py --config <cfg.yaml>                                        # CLI 回测
Invoke-WebRequest http://localhost:8080/api/status                        # 主服务
Invoke-WebRequest http://localhost:8081/api/trade/status                  # 实盘服务（只读）
```
