# VERA 项目质量审计报告(前后端端到端)

> 日期: 2026-07-20 | 范围: 2026-07-19/20 因子线全部工作成果(IC 筛选/实验室/体检页面/因子过滤/配置源统一)+ 既有接缝
> 方法: 3 路并行审计代理(后端 / server+前端 / 端到端数据流) + 主审计活体探测 + 全部引用机器校验
> 纪律: 遵循 CLAUDE.md 审计纪律——所有 `[file.py:line]` 引用经 `_verify_references.py` 校验(本报告通过率见文末)

---

## 0. 总评

**6.5/10 → 修复后 8/10。** 核心引擎(IC 筛选/终审/实验室)质量高:因果性有强制测试、双窗口纪律有执行点、互斥时序基本严密、CLI↔生产语义逐行一致。但端到端链路挖出 **1 个 CRITICAL(生产因子过滤对已采纳规则必然崩溃且静默回退)** 与 **4 个 HIGH(409 竞态、反射 XSS 链、规则渲染 XSS、体检基线配置错位)**,全部已在本轮修复。剩余主要风险集中在**配置治理**(default.yaml dirty 改动引发三方股票池分裂)与**若干"各自测过但从未端到端真跑"的接缝**。

---

## 1. 本轮已修复(含回归测试)

| # | 严重级 | 问题 | 修复 |
|---|---|---|---|
| F1 | **CRITICAL** | **生产因子过滤只喂 `close` 面板**,注册表约半数因子需 open/high/low/volume/amount(含已采纳的 `intraday20:top10`,双窗 PASS)→ 生产必 `KeyError` → `pipeline._apply_factor_filter` catch-all 静默回退未过滤,用户无感知 | `selection/factor_filter.py` 改读全 OHLCV+成交额列、warmup 90→400 天(对齐实验室);新增真实缓存回归测试 `tests/test_factor_filter.py::test_compute_factor_values_full_panel_factors` |
| F2 | **HIGH** | `/api/run` 409 互斥 **check-then-set 竞态**(sync def 在线程池真并发,可双跑回测并绕过体检互斥) | `server.py` 加 `_run_lock`,`clear_stop`+置位全部入锁 |
| F3 | **HIGH** | **反射型 XSS 链**:`/api/factor-rules` 的 hint 原样反射未校验输入 + 前端 innerHTML 未转义 | 服务端复用 `FORMULA_RE` 白名单(400);前端 `esc()` 扩展到引号,`renderFactorRules` 的 hint/rule.id/rule.label 全转义 |
| F4 | **HIGH** | **规则/队列/历史渲染 XSS**:`rule.id` 插属性、`t.error`(子进程任意输出)、`i.formula` onclick 插值全未转义 | `refreshLabStatus`/`loadLabHistory`/`renderFactorRules` 全量 `esc/escAttr` |
| F5 | **MEDIUM** | `/api/lab/report` **glob 注入**(`formula=*` 返回任意报告,主审计活体证实)、`/api/lab/run` tag 无校验、formulas 无上限 | 两个端点加 `FORMULA_RE`;tag 加 `^\d{8}_\d{8}$`;formulas ≤10;report 读取加 try |
| F6 | **LOW** | `showToast(...,'success')` 与 CSS 类名不匹配(成功提示不可见) | 改 `'ok'` |

---

## 2. 未修复发现(按严重级,含待拍板)

### HIGH(需你决策,非纯代码问题)

**B1 — UPN 终审实际用的是 QUANTQQ 的止损结构(配置错位)**
`utils/config_loader.py:39-41` 的 `resolve_strategy_yaml` 兜底硬编码 `strategy_QUANTQQ.yaml`(与公式无关);`config/current.yaml` 当前不存在 → UPN 体检终审落在 QUANTQQ 的 5m 冠军止损上(UPN 体检报告第 3 行自承配置路径)。臂间一致性成立,但"终审代表该公式生产环境"不成立。
**待拍板**:兜底改 `config/default.yaml`?或体检提交强制显式 `--strategy-yaml`?UPN 需在正确基线上复跑终审。

**B2 — 基线/终审/生产三方股票池分裂 + `default.yaml` 有未提交改动**
实验室自动补基线走 `--formula` 模式 → 股票池读 `config/default.yaml`(当前 dirty: `type: "23"` 沪深300,注释自承临时优化);S4 终审读 current/strategy yaml 的 stop_loss;生产 `/api/run` 读前端表单(默认全A 50)。**现在给新公式补基线会得到沪深300池,与既有全A基线静默混用,双窗口纪律失效**;且 `resetDefaults` 会把用户表单股票池刷成 23。
**待拍板**:default.yaml 的 `type:"23"` 还原还是提交?补基线是否强制带 strategy_yaml?

### MEDIUM(建议排期修)

| # | 问题 | 位置/证据 | 建议 |
|---|---|---|---|
| B3 | **体检进度不实时**:`subprocess.run(capture_output=True)` 跑完才解析标记,页面阶段在结束瞬间一次性跳完 | `tools/lab_runner.py:166-175`;STAGE_PROGRESS 权重是死字段 | 改 `Popen` 逐行读 + 快照暴露百分比 |
| B4 | **多规则组合语义从未被终审**:生产 `apply_rules` 顺序重排(第二条在幸存集重新 rank),终审每臂独立;同勾两条 adopted 规则时实际剔除集 ≠ 任何终审臂 | `selection/factor_filter.py:35-50` vs `tools/overheat_ab_test.py:154-160` | 二选一:生产改并集语义(各规则独立 mask 再 AND,与可终审语义对齐);或终审加组合臂。前端加"组合未终审"提示 |
| B5 | **pipeline._apply_factor_filter 零测试**;`test_factor_filter.py` 名义测 compute_factor_values 实则没测(已在本轮补上) | `pipeline/pipeline.py:91-114` | 加 mock `filter_selections` 的回退分支测试 |
| B6 | **5m 优化的移动止盈参数跑在 1d 引擎上语义退化**:drawdown 0.3% 在日 bar 内几乎必然同根触发,且假设以距日高 0.3% 成交(偏乐观);yaml 未声明 `backtest.period`(引擎默认 1d) | `config/strategy_QUANTQQ.yaml` vs `docs/audit/2026-07-18_QUANTQQ_5m参数优化报告.md:3` | 二选一:yaml 显式 `period: "5m"`;或注释声明"终审为 1d 口径,绝对年化按相对排序读" |
| B7 | **前端勾选状态双向漂移**:规则列表未渲染时 collectFactorFilter 会把已勾规则静默清空;曾 adopted 后翻未通过的规则仍被照收进 payload | `web/vera-ui.js:1493-1512` | 列表渲染完成前不写回;渲染时对非 adopted id 主动剔除 |
| B8 | **体检子进程无超时无取消**:挂死则 `/api/run` 永久 409,只能重启 server | `tools/lab_runner.py:166` | 加 timeout + `POST /api/lab/cancel` |
| B9 | **轮询泄漏竞态**:切走页签时 in-flight 回调会重启轮询;每 2s 还全量重拉历史(glob 两目录) | `web/vera-ui.js:1589-1590` | 回调先判页签 active;历史仅在状态变化时拉 |
| B10 | `DB_FACTORS` 与 `DAILY_BASIC_FACTORS` **双源真相**(方法论宣称"加因子只加一行"对生产路径不成立) | `selection/factor_filter.py:26-27` vs `tools/factor_ic_screen.py:231-232` | 生产直接 import 实验室常量 |
| B11 | 裸 500 端点:`/api/config/validate`(非法 ladder_levels)、`/api/last_result`、`/api/results`(损坏 JSON) | `server.py:244-248, 546-558` | 统一 try/except 返回 success:false |
| B12 | **rzye_ratio/hk_ratio 时滞口径与 T 收盘决策不一致**(两融 T+1 早、北向 T 晚公布),扭曲 IC 族归纳(不进生产) | `tools/factor_ic_screen.py:486-504` | 报告警告补"时滞"说明,或改用 T-1 值 |
| B13 | **默认 warmup 的拉取提示**:生产回测中途 daily_basic 缺日期会在线程内逐日拉 tushare,前端无感知 | `tools/factor_ic_screen.py:313-336` | 预热提示/日志进进度 |

### LOW(记录,不排期)

- `CLAUDE.md:29` 自引用行号失真(`assert_consistent` 实际在 [pipeline/pipeline.py:126])——该文档自立的机器校验纪律也中招;
- 因果测试参数列表缺 `f_mom5/f_mom60/f_sector_heat5` 3 个因子(`tests/test_factor_ic.py:87-93`);
- `safeFloat` NaN→0 可发 `initial_capital: 0`(`web/vera-ui.js:448`);`formula_name` payload 不 trim 而过滤键 trim,带空格则过滤静默跳过;
- benchmark 前端硬编码缺 zz500 vs `server.py:85` 默认含;
- `resetDefaults` 不清 `FACTOR_FILTER_KEY`,旧勾选残留(`web/vera-ui.js:644-645`);
- `formula_lab.py:156-157` label 三元两分支相同(死条件);`pipeline/pipeline.py` "filtered_empty" 分支不可达(死代码);
- `lab_runner._tasks` 无界增长;busy-wait 无超时;`/api/lab/history` 报告名子串匹配(UPN 可错配 XUPNY);
- 单元素截面 `rank(pct)=1.0` 必被 top 规则剔除(无过热证据却按最热处理);
- `overheat_ab_test.py:141`/`factor_ic_screen.py:446` parquet 缺失时裸 FileNotFoundError;`formula_lab --report-only` 遇旧 CSV 臂不齐时 `.loc` KeyError。

---

## 3. 验证过没问题的关键路径(给信心)

- **因果性**:21 个面板因子全部 trailing-only,截断测试强制(`tests/test_factor_ic.py:87-99`,板块/指数因子 `:102-164`);IC 计算有 ±1/噪声自校验。
- **单规则 终审↔生产 语义逐行一致**:`tools/overheat_ab_test.py:75-85` vs `selection/factor_filter.py:39-47`(阈值、NaN 保留完全相同)。
- **双窗口纪律执行点真实**:adopted 需 ≥2 窗且全 PASS([tools/formula_lab.py:149-152]);UPN 规则 JSON 单窗全 `adopted:false` 实证。
- **互斥时序(修复后)**:回测 check+set 原子锁;体检永远排队、回测在体检中 409([server.py:413-414],有测试);公式名白名单防穿越([tools/lab_runner.py:28])。
- **tmp yaml 携带 factor_filter 且 Pipeline 键路径正确**:[server.py:217] 生成 → [pipeline/pipeline.py:94] 顶层读取,`_deep_merge` 不吞。
- **子进程无命令注入**(全 list argv);`viewLabReport` 用 textContent;`/api/results/{id}` 路径遍历已防([server.py:566])。

---

## 4. 端到端覆盖坦白

因子过滤链每一环都"各自测过",但**从页面勾选到引擎的完整端到端从未真跑过**(需 TDX 在线的活体回测);UPN 页面端到端只覆盖到"体检产出规则 JSON",不覆盖规则回流生产。建议:勾 `dist_ma20:top10` + `intraday20:top10` 跑一次短区间活体,确认日志出现"因子过滤[QUANTQQ]"(F1 修复后应生效)。

---

## 5. 待拍板汇总(**2026-07-20 已全部拍板**)

1. **B1**:✅ 拍板——兜底用 `config/default.yaml`(`resolve_strategy_yaml` 已改)。
2. **B2**:✅ 拍板——保留 `default.yaml` 的沪深300池(`type:"23"` 作为默认,随本报告同期提交)。
3. **B4**:✅ 拍板——**终审加组合臂**(S4 自动追加同款档位组合,走生产同款 `apply_rules` 语义;规则 JSON 新增 `combos` 段)。
4. **B6**:✅ 拍板——接受 1d 口径(yaml/方法论已加注"1d 口径偏乐观,结论按相对排序读")。
5. **B3/B8**:✅ 拍板——**排期**(进度流式化 Popen 逐行读 + 任务超时/取消端点,记入 backlog)。

---

## 附录:引用机器校验

本报告所有 `[file.py:line]` 引用经 `python -m docs.audit._verify_references` 校验:**7 处引用,7 处有效,通过率 100% [PASS]**(2026-07-20)。
