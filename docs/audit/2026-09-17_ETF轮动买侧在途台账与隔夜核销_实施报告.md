# 2026-09-17 ETF 轮动「买侧在途台账 + 隔夜核销」实施报告

> **依据**: 计划书 v2 `docs/plan/2026-09-17_ETF轮动买侧在途台账与隔夜核销_计划书.md`
> （来源 = 2026-09-16 全系统代码质量审计 P1 条目 T1；计划书经一轮独立审阅迭代，13 处问题全部并入 v2）。
> **用户拍板流程**: 出计划书 → 审阅迭代一次 → 开工 → **完工后两轮独立质量审计** → 本报告 + 三件套登记 + 提交。
> **一句话**: 轮动买入从"委托一受理就全额记账、没人回头核"改成"同样记在途台账，当场/隔夜按 QMT 委托回报核销纠偏"；
> 安全不变量 = **只减不增**，且任何失败只往"账本 ≥ 真实"（偏多=少赚）方向退化，绝不"少记"（少记会补买超配）。
> **边界**: 本轮只动 4 个文件（`trade/rotation.py`、`trade/gateway.py`、`tests/trade/test_rotation_lots.py`、
> `tests/trade/test_rotation.py`）；未改 store/schema（零 DDL）；工作区另有**并发会话**的改动（`tests/conftest.py`、三个 `.bat`、
> 大盘位置/复盘/RAG 计划书）不属本次，提交时不含。

---

## 一、落地清单（计划书条目 → 实现位置）

| 计划书条目 | 实现 |
|---|---|
| §3.1 复用 `rotation_meta` 新键 `rotation_open_buys`，零 DDL | 全走 `RotationMetaStore.get/set`，未动 store 与 schema |
| §3.2A 逐笔下单即写台账（不攒批，防异常丢单）+ `_place_order` 异常原样上抛 | 写台账 `[trade/rotation.py:1489]`；乐观记账 `[trade/rotation.py:1501]`；异常语义写进 docstring `[trade/rotation.py:1529]` |
| §3.2B 核销顺序倒置：先台账落库成功、再改内存；失败整条跳过不扣 + 审计 | 探测写 `[trade/rotation.py:1237]` → 扣内存 `[trade/rotation.py:1249]` → 核销审计 `[trade/rotation.py:1261]` |
| §3.2C 核销期/pass 末整表替换；下单期只做新增 | 核销末尾 `[trade/rotation.py:1268]`、卖侧 `[trade/rotation.py:1061]`、迁移清空 `[trade/rotation.py:980]` |
| §3.3 条目字段 + 代码/方向/同日校验（防单号复用） | 条目构造 `[trade/rotation.py:1489]`；校验 `[trade/rotation.py:1218]` |
| §3.4 买侧判定表（异常早退 / 终态按成交量 / 在途保留 / 查不到 ≤5 轮超期移除） | 异常早退 `[trade/rotation.py:1162]`；查不到保留与超期 `[trade/rotation.py:1186]`；上限常量 `[trade/rotation.py:223]` |
| §3.4 卖侧补"查询异常早退"（唯一卖侧改动） | `[trade/rotation.py:1043]` |
| §3.5 对账前重读持仓 + 期望值 = QMT 持仓 + 我方非终态买单未成交量 | 重读 `[trade/rotation.py:889]`；期望值 `[trade/rotation.py:1301]`；`_inflight_buy_qty` `[trade/rotation.py:1319]` |
| §3.6 先核销（买+卖）后迁移；迁移清两键 + 逐条审计 | 顺序 `[trade/rotation.py:655]`；清空与审计 `[trade/rotation.py:980]`（实现 `[trade/rotation.py:995]`） |
| §3.7 fail-closed 闸：`Σ份簿记 < QMT volume` → 该代码本轮不补买 | 闸 `[trade/rotation.py:794]`；买入循环跳过 `[trade/rotation.py:821]` |
| §3.8 买侧独立短超时 1.0s + 买后等一轮再核销（审计记耗时） | 常量 `[trade/rotation.py:220]`；等待与核销 `[trade/rotation.py:845]`；`_wait_fills` 加 `timeout` 形参 `[trade/rotation.py:1506]` |
| §3.5/L1 扣到 0 删内存行；逐份 `entry_high` 加 `qty > 0` 过滤 | `[trade/rotation.py:1072]`、`[trade/rotation.py:403]` |
| §3.9 `_save_open_ledger` 收口（买卖共用写侧）+ 注释同步 | 定义 `[trade/rotation.py:1074]`；卖侧三处调用点改新名 |
| §3.4/L2 台账损坏 → 审计 + 备份 `.bak` | `_read_open_ledger` `[trade/rotation.py:1096]`、`_ledger_corrupt` `[trade/rotation.py:1118]` |
| §6 FakeGateway 部撤钩子（状态 53 原先测不到） | `[trade/gateway.py:882]` |

## 二、实施中发现并修掉的真 bug（实施过程产出）

| # | 问题 | 位置 | 性质 |
|---|---|---|---|
| 1 | `_buy_lot` 改二元组返回时两条早退分支漏改（返回裸 `0.0`），而 `gap == 0`（该腿现值已等于目标）是**正常路径** → 调用点解包抛 `TypeError`，在消费者线程里打断整轮执行 pass | `[trade/rotation.py:1463]`、`[trade/rotation.py:1468]` | 主会话 diff 审查发现；已修 + 加 `gap==0` 防回归用例 |
| 2 | 新用例初版用 `days=["friday"]` 而 `_ts()` 取当天日期 → **只在星期五通过**（日期依赖 flake） | `tests/trade/test_rotation_lots.py` | 主会话抽查发现；已改为统一预置 `tranche_target`（走"维持目标"路径，与信号日无关） |
| 3 | `simulate_partial_cancel` 漏导入 `OS_PART_CANCEL` | `[trade/gateway.py]` | 实施自测发现，已补 |

## 三、对实施者提出的 5 处张力的裁决

| # | 张力 | 裁决 | 理由 |
|---|---|---|---|
| 1 | §3.2B 的"先落台账"用了 `merge=True` 定向写（字面是"核销期一律整表替换"） | **接受** | 定向写是"写成功才扣"的探测；若用 `merge=False`，探测那一刻会把**其他在途条目**从库里抹掉（异常时静默丢条目，与 L2 相悖）。§3.2C 要的整表替换由核销末尾 `[trade/rotation.py:1244]` 承担 |
| 2 | 台账条目在 5 字段外追加第 6 位"查不到轮次计数" | **接受** | ≤5 轮需要一个持久计数器；无该位按 0 起算，向后兼容 |
| 3 | 卖侧**读侧**未改用 `_read_open_ledger`（仍静默跳过损坏） | **接受为已知边界** | 计划书把卖侧改动限定为"查询异常早退"一条；统一会额外给卖侧加"损坏→审计+.bak"行为，属另行拍板项 |
| 4 | 同 pass 内"扣回幻影后按缺口重新下单" | **接受为设计行为** | 该代码确实低配，补买是自愈；**有界**（买循环 `for 份 → for 腿` 单遍，(份,代码) 每 pass 最多一次 `_buy_lot`，无 while/重试） |
| 5 | `<key>.bak` 备份键从不清理 | **接受 + 登记遗留** | 仅在台账损坏时产生（罕见）；清理策略列入后续可选 |

## 四、测试与验证

**新增 14 条用例**（`tests/trade/test_rotation_lots.py` 12 条 + `tests/trade/test_rotation.py` 2 条）：
E1 风控拒不入台账、E3 废单清零、E4 部成按实际量、E5/E11 跨重启自愈、E6 查不到保留/超期、E7 台账损坏不丢、
M2 查询异常不动台账、H2 台账写失败绝不扣、**E13 三轮 pass 恰好扣一次（主锁）**、**E14 核销不增簿记 + 少记拦补买（主锁）**、
E9 迁移清双台账、E10 同码两单只扣废单那笔、H4 对账把在途买单算作"已解释"、gap==0 不抛不买。

**测试质量自查（主会话抽样）**：两条主锁为写实断言（审计计数 + 档位断言），非空断言/放宽断言；
12 条台账域用例全部预置 `tranche_target`（日期无关），`test_rotation.py` 两条沿用既有显式信号模式；E1 断言按语义等价改写并注明理由。

**验证**：`pytest tests/trade/test_rotation_lots.py tests/trade/test_rotation.py` = 62 passed / EXIT=0；
全量 `pytest tests` EXIT=0（实施者两次 + 主会话多次）。

## 五、上线时序（硬约束）

| 时间 | 动作 | 检查 |
|---|---|---|
| 2026-09-17 14:54 | 错峰代码**首次真实执行**（跑现有代码，不动） | 取重启前基线（已取：`rotation_meta` 无键、`lots`/`tranche_state` 0 行） |
| 2026-09-17 **≥15:05** | 重启 `trade_main`（PID 53212）装载新代码 | **绝不在 14:54–15:00 重启**：`[trade_main.py:562]` 的启动补偿会在该窗口补跑一轮调仓（撞收盘竞价）；重启前确认 QMT 已就绪（2026-09-02 冷启动 -1 教训） |
| 重启后 | 检查 | `rotation_meta.lots_initialized == "1"`；无 `rotation_catchup` 审计；`rotation_lots`/`rotation_tranche_state` 与基线一致 |
| 2026-09-18 14:54 | 首个带买侧核销的 pass | 若当日有废单：簿记 == QMT；`rotation_lots_drift` 不出现 |
| 2026-09-18 收盘后 | 验收 | ①`rotation_lots_drift` = 0（或每条都被在途台账/人工单解释）；②无在途单时 `Σ簿记 == QMT 持仓`；③台账键为空；④`rotation_lots_understated` = 0 |

## 六、两轮独立质量审计结论

### 6.1 第一轮（独立审计员挑刺）：有条件通过 —— 3 项必修

审计员逐行读真实代码 + 手工推演算术，确认 **8 条安全不变量全部成立**（恰好一次核销 / 失败方向单向"账本≥真实" /
查询异常≠查不到 / 迁移先核销后迁移 / 闸误触发面 / 对账口径 / 卖先扣买后扣的截断 / 新测试写实且日期无关），
全量测试 EXIT=0；未发现"少记→补买→超配"通道。提 3 项必修 + 2 项低项，**已全部处置**：

| # | 第一轮发现 | 处置 |
|---|---|---|
| 中-1 | **对账口径把"部成未终结"的已成交量重复计入 → 假漂移**（打脸本次目标"让漂移告警重新可信"） | 已修：`_inflight_buy_qty` 改为**身份用台账单号、数量取当次 `query_orders()` 的 `qty − filled_qty`**，终态单贡献 0；`_reconcile_lots` 查询异常**跳过本轮对账**（不造告警）`[trade/rotation.py:1319]`、`[trade/rotation.py:1293]` |
| 中-2 | 闸对人工/外部持仓会**永久拦补仓**（含避险腿黄金ETF），且文案没给修账入口 | 已修：文案引用单一真相源 `_LOTS_REPAIR_HINT` `[trade/rotation.py:794]`；新增连续 3 轮升级告警 `[trade/rotation.py:1360]`（计数落 `rotation_meta`，无新表） |
| 中-3 | E8（份内无簿记行）与 M3（单号复用校验）两条审计分支**零用例** | 已补：`test_buy_settle_no_lot_row_audited`、`test_buy_settle_order_mismatch_skipped` |
| 低-② | E14 里一条 qty=0 的注入对断言无贡献 | 已删 |
| 低-① | `<key>.bak` 备份键从不清理 | **裁决：不修**。备份走 `rotation_meta.set`（同键覆盖写），每个台账键最多留 **1 行、全局共 2 行**，且无任何读取路径消费它 —— 属"有界死行"；为它给 `RotationMetaStore` 加 `delete()` 反而增加实体（`ponytail` 阶梯：能不加就不加）。登记为已知边界 |

审计员复核并判定**保留**的两处（不改）：台账条目第 6 位"查不到轮次计数"（删掉会让"永远查不到的幻影"被期望值永久解释掉 → 漂移永不响）、
核销的 `merge=True` 探测写（删掉会让末次整表写失败时下一轮**再扣一次**，违反"失败只往偏多退化"）。

### 6.2 第二轮（另一名独立复审，未参与实施与第一轮）：有条件通过 —— 1 项必修

复审逐条验证：3 项必修的**核心逻辑全部正确收敛**（对账假漂移两点推演 + 实测异常注入漂移=0；升级告警的计数语义/生命周期正确、
KV 有界、同一持续期只升级一次、计数异常不阻断 fail-closed；E8/M3 用例走实分支且带反向断言），
**8 条安全不变量无回归**，未发现"少记 → 补买 → 超配"方向的新洞。提 1 项必修 + 3 项低项：

| # | 第二轮发现 | 处置 |
|---|---|---|
| **必修** | **新增的"人工修账入口"文案照字面做不生效**（操作层缺陷，非资金风险）：①"直接改 `rotation_lots`"在进程运行中会被内存镜像整表覆盖（实测：改库 25 万股，跑一轮 pass 后回到 5 万股）；②"清 `lots_initialized` 后重启"在表里还有任何行时是静默空操作（构造器 `if self._lots: self._migration_pending = False`，而闸命中的典型场景恰恰是"有簿记但少于持仓"） | **已修**（主会话）：两处文案（闸 `[trade/rotation.py:794]`、对账 `[trade/rotation.py:1314]`）改为**可执行**指引 —— ①必须先停 `trade_main` 再改库再重启；②改为"清空**全部行** + 清标志后重启才触发迁移（部分少记时此路无效）"，后果警示保留；并把这两条可执行性**锁进断言**（`tests/trade/test_rotation_lots.py`） |
| 低-2 | `_inflight_buy_qty` 只按 order_id 匹配、不校验代码/方向/委托日，弱于核销路径的 M3 三校验 → 单号复用时在途量记到别代码上，造假漂移 | **已修**：补齐同款三校验且**代码以台账为准** `[trade/rotation.py:1319]`；新增用例 `test_inflight_qty_skips_order_id_reuse` |
| 低-3 | 对账侧查询异常只有 debug 日志、无自身审计 | **不改**（同一轮 `_settle_open_buys` 已写 `rotation_settle_query_failed`，非静默，仅观测点分散）——登记为已知边界 |
| 低-4 | `_save_open_ledger(merge=True)` 读失败会覆盖其他在途条目（既有边界，非本次引入） | **不改**：方向=不核销=账本偏多（安全），有 warning；核销路径末尾 `merge=False` 写回可自愈 |

**修复后的独立验证**：由第三名审查员做窄范围验证（文案与代码行为逐条对照、两条不变量回归、测试退出码），结论见 §6.3。

### 6.3 修复后窄范围验证（第三名审查员，独立）

判定：**修复 A = 正确**（附 1 处 LOW 残留）、**修复 B = 正确**。

- **文案与代码行为逐条实测对照**（审查员自行复现，不只读注释）：①"必须先停 `trade_main`"属实 —— `_persist_lots` 在 `on_signals` 的 finally 与每个 pass 末尾各整表写回一次，实测进程运行中把库值 1000 改成 5000，跑完一轮回到 1000；②"表里有行则不迁移"属实 —— 构造器 `if self._lots: self._migration_pending = False`，实测三组（有行+清标志 / 有行+删标志 / 无行+清标志）只有最后一组真迁移；③两份主文案都改到了；④测试只锁住其中一份。
- **LOW 残留（已在本轮收口）**：同一份修账指引**手写三份**，第三份（升级告警 `rotation_lots_understated_persistent`）仍是旧措辞（缺"先停"、缺"全部行"），漂移文案缺"非目标腿"后果警示，且两处无可执行性断言 —— 正踩项目铁律"同一条规则手写 N 份必然漂移"。**处置**：抽出模块级单一真相源 `_LOTS_REPAIR_HINT`（`[trade/rotation.py:99]`），三处审计文案（闸 `[trade/rotation.py:797]`、对账 `[trade/rotation.py:1314]`、升级告警 `[trade/rotation.py:1395]`）统一引用；三处文案的可执行性**各加一条字面断言**锁死。
- **修复 B 的用例鉴别力**（审查员核过）：`test_inflight_qty_skips_order_id_reuse` 同时抓两种错法 —— 沿用旧实现会得 `{GOLD: 5000}`、只删代码校验会得 `{NASDAQ: 5000}`，两种都过不了；方向校验被删亦必红。
- **不变量回归抽检**：恰好一次（扣减公式/顺序倒置/条目移除同步/末次整表替换/跨轮审计条数=1）与失败方向单向（台账写失败保留条目不扣、查询异常台账与簿记都不动）均未被本次修复触及。
- **测试**：`pytest tests/trade/test_rotation_lots.py tests/trade/test_rotation.py -q` → **67 passed, EXIT=0**。
- 审查员方法学声明（照录）：功能整体未提交，无法用 `git diff` 隔离"第二轮修复"那几行，"未触及不变量"结论来自逐行读码 + 既有断言，不是 diff 隔离。




## 七、遗留与后续可选

1. 卖侧读侧损坏处置未统一（裁决 3）；
2. `<key>.bak` 备份清理策略（裁决 5）；
3. 若实盘希望"废单当天不再重下、等次日"，需另立策略规则（当前为"同 pass 自愈重下一次"，有界）；
4. `entry_high` 仍用提交价而非实际成交价（`query_trades` 的 `traded_price` 可取，属精度优化）。
