# 2026-08-25 轮动卖出模型B合规修复 · 审计报告

## 一、审计结论

**PASS（有条件生效）。** 修复正确恢复 2026-08-14 拍板的模型 B 铁律，代码改动最小  
（`trade/rotation.py` +13/-2），全量 trade 测试 **457 通过 / 0 失败**，新增回归测试  
锁死本场景。**条件：需重启交易进程才生效**（详见 §六）。

## 二、事件

2026-08-25 14:54:01，实盘系统卖出 513100.SH（纳指ETF）1,700 股 @2.2（3,740 元，  
+57.8 元）。当日未换档（目标仍是 513100）、未触发止损（止损线 1.871，收盘 2.195）。  
用户质询后还原设计过程，确认该卖出违反模型 B 铁律，属 2026-08-20 动量改造引入的  
未文档化行为回归。

## 三、根因溯源（证据链）

| 时间               | 事件                                                                                                 | 证据                                                      |
| ---------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| 2026-08-14       | 用户拍板模型 B：比例是「上限」不是强制目标；**绝不主动卖来凑比例**；卖出只由换档/止损触发；回笼现金划给低配一边；漂移是接受的成本                               | `docs/plan/2026-08-14_ETF轮动与双池资金分配系统_计划书.md` L42-46     |
| 2026-08-14 实现    | 卖循环有 `if state_changed:` 守卫，仅换档时卖，符合设计                                                             | commit `57d71ce`                                        |
| 2026-08-20 21:32 | 动量改造 commit `c08b43b` **删除 `state_changed` 守卫**，卖循环变成每日无条件对每条腿 `_sell_to` → 引入"每日削超配腿"行为           | `git show c08b43b -- trade/rotation.py`                 |
| —                | 该行为变更**三无**：计划书只写"换档卖/买"（L21、L90）、实施总结零记载、测试无覆盖；`_sell_to` docstring"仅换档时调用"保留至今未同步                | 三份文档交叉验证                                                |
| 2026-08-25       | 纳指腿市值 527,140 > 池上限 523,204（腿 +1.57% 涨得比总资产快），超出 3,936 元 ÷2.2 = 1,789 股 → 取整 100 股 = **1,700 股被削** | trade.db trades/audit 表，`rotation.py _sell_to` L720-721 |

## 四、修复内容

`trade/rotation.py` `_execute` 卖循环增加一行守卫——**只清「目标值为 0」的腿**（即  
正在退出的旧腿），目标腿（风险模式=胜者腿，避险模式=黄金篮子）超配不削：

```python
for code, tval in legs:
    if tval > 0:
        continue  # 目标腿(风险腿或避险篮子)超配不削, 漂移由买入侧预算帽兜住
    oid = self._sell_to(code, cur[code], tval, ...)
```

依据 `momentum_target_values`（L177-201）的腿结构：`tval>0` 恒等于"当前要持有的  
腿"，`tval=0` 恒等于"要退出的腿"，判定在全部状态下（风险/避险/双避险腿）成立。  
同步修正 `_sell_to` docstring（旧注释"仅换档时调用"已与行为不符）。

买入侧不动：预算帽 `pool_gap = max(0, pool − 池内持仓)` + 只用自由现金补低配，  
本就符合模型 B（低配→补、超配→漂移）。

## 五、行为矩阵（修复前 vs 修复后）

| 场景         | 修复前       | 修复后           | 模型B |
| ---------- | --------- | ------------- | --- |
| 同腿超配（本次事件） | ❌ 每日削回池上限 | ✅ 零动作，接受漂移    | 合规  |
| 同腿低配       | 补买（自由资金）  | 补买（不变）        | 合规  |
| 换档 A→B     | 全清 A、买 B  | 全清 A、买 B（不变）  | 合规  |
| 止损切避险      | 全清风险腿、买黄金 | 全清风险腿、买黄金（不变） | 合规  |
| 避险模式黄金超配   | ❌ 削黄金     | ✅ 零动作         | 合规  |

## 六、测试验证

- 新增回归测试 `test_overweight_target_leg_no_trim`（`tests/trade/test_rotation.py`）：  
  复刻本次事件结构（总资产 105 万、池 52.5 万、持仓 55 万），断言**零买零卖**  
  （修复前该场景会卖 25,000 份）。
- `tests/trade/test_rotation.py` 32/32 通过（含存量换档/止损/补仓/预算帽用例全兼容）。
- `tests/trade/` 全量 **457 通过 / 0 失败**（41.6s）。

## 七、生效条件与运维注意

1. **必须重启交易进程**：运行中的 trade 进程仍加载旧代码，明日 14:54 若纳指腿仍  
   超配会再次削腿（单次约数千元量级）。建议盘前重启。
2. 明日起，若目标腿持续超配，账面比例会长期漂在 etf_ratio 之上——这是模型 B 的  
   设计语义（换低手续费），监控页看到超配**不再是异常**。
3. 本次修复**未提交 git**（工作区另有其他会话的未提交改动：server.py、web/*、  
   trade_main.py 等，与本次修复无关，未触碰）。建议单独提交本文件对，避免混入。

## 八、遗留事项

- [ ] 盘前重启交易进程（用户操作）
- [ ] 单独提交 `trade/rotation.py` + `tests/trade/test_rotation.py`（待用户确认后执行）
- [ ] （可选）在设置面板/监控页给"模型B=比例上限"加一行说明，避免未来再被当成 bug
