# 2026-09-16 公式农场达标榜回填回测页 审计报告（第二轮）

## 审计对象

第二轮独立复审：提交 `0280083`（功能实现）+ `71b2731`（第一轮修复）的**修复后状态**，逐条验证第一轮 12 条的修复是否真修对，并找新引入的问题。本轮发现 1 HIGH + 4 MEDIUM + 6 LOW，**其中 3 条 MEDIUM/LOW 是上一轮修复新引入的**；全部已修（随本报告同提交入库）。

## 总体结论

复审初判「需修复后合入」（复核可信度 6/10：表单回填这一半可信、同口径复跑这一半当时不可信）；**本轮修复后：可合入**。

一句话说清两轮的差别：第一轮修掉了"口径填错"（语义/池子开关静默漂移），第二轮修掉了"口径填对了但**数字仍不可并排比**却没人告诉用户"（引擎入口差异无披露）+ 上一轮修复自己引入的三处状态管理缺陷。

## 第二轮 HIGH ×1

| # | 问题 | 证据（复审实读 + 我复验） | 修复 |
|---|---|---|---|
| HIGH-A | 回填后复跑仍不是粗扫口径，三项差异全不可见：粗扫走 `engine.run_cached`（**不支持 degrade_5m**，缺 5m 的股-天丢信号）而回测页走 `run()` 且 `config_mapper` 恒 `degrade_5m=true`（缺数据用日线补满 48 根 bar 照样开仓）；再加粗扫 60 交易日**稀疏窗口** vs 本页连续区间。第一轮报告把这三点以"已在口径文案中隐含"关掉——**该理由不成立**（原话讲的是移动止盈 confirm，与丢信号/补线毫无语义关联） | `[tools/quantqq_5m_sweep.py:17]`、`[config_mapper.py:139]`、`[tools/gs_5m_sweep.py:296]`、`[backtest/loop/entry.py:142]` | `_caliber_text` 补完整披露（`[core/farm_summary.py:255]`）：「⚠ 粗扫走稀疏60交易日窗口且缺5m的股-天丢信号, 本页为连续区间且5m降级补线(缺数据用日线补满), 笔数可能偏多, 数字别与粗扫并排比」；横幅另显示**区间**（`[web/js/vera-ui.js:668]` 附近）。第一轮报告的错误关闭理由已在本报告纠正 |

## 第二轮 MEDIUM ×4（3 条为上一轮修复新引入）

| # | 问题 | 修复 |
|---|---|---|
| B | **不可逆的用户设置丢失（优先级最高）**：板块快照取自内存 `_selectedSectors`，而它只在 `loadSectors()` 成功分支从 localStorage 载入——**通达信没开时**停在 `[]`；此时回填把 `[]` 记成快照、`clearSectors()` 立刻把磁盘上的真实选择抹成 `[]`，点「恢复」因 `if ([])` 为真又写一遍空 | 快照改读**磁盘** `localStorage[SECTORS_KEY]`（`[web/js/vera-ui.js:623]`）；板块 UI 未加载（`_allSectors` 空）时**只清内存不落盘**（`[web/js/vera-ui.js:642]`），结果口径不受影响（提交走内存） |
| C | 恢复板块后漏调 `toggleUniverseDropdown()` → 下拉框仍显示"沪深300"可用，而 selector 会因 sectors 非空忽略它（HIGH-2 的镜像复现）；根因是"变更板块后同步下拉框"手写在 4 处 | 新增唯一写入口 `setSectors(list)`（`[web/js/vera-ui.js:687]`，含持久化+重绘+**toggleUniverseDropdown**），恢复走它；`clearSectors` 改为 `setSectors([])` 收口 |
| D | 「知道了」只隐藏横幅不清快照 → 再点第二条公式时快照被覆盖，此后「恢复原配置」恢复的是**上一条的回填值**（MEDIUM-3 只堵住了横幅仍显示那条路径） | 「知道了」一并清 `_farmPrefillSnapshot`/`_farmPrefillSectorsSnap`（`[web/js/vera-ui.js:717]` 附近） |
| E | 程序化写 `cfgFormula` 不派发事件 → 因子规则面板仍列上一个公式的规则（`applyConfigDict` 同病，**两份实现**）；用户手动勾选会把别家规则勾到 GS 名下 | 收口 `notifyFormulaChanged()`（`[web/js/config.js:39]`），农场回填（`[web/js/vera-ui.js:631]`）与 `applyConfigDict` 共引——同时**顺带清掉 LOW-J 的脏数据**（派发后规则面板按新公式重渲染，残留 rule id 被覆盖） |

## 第二轮 LOW ×6（全部已修）

- **LOW-F 六键校验名不符实**（实为四键，`cond_days/cond_profit` 缺失会静默改语义：无条件时间止盈 / 「持仓 N 天必卖」）：已改真六键（`[core/farm_summary.py:293]`），测试改为循环六键（名字与实现一致）；
- **LOW-G `min_buy` 属口径却未回填**（`entry.py` 是硬闸门，用户把它设 >2万则一笔都开不出来，而横幅还说"单票上限2万"）：**拍板为口径**，纳入 `SWEEP_CALIBER["min_buy"]=2000.0`（`[core/farm_rules.py:58]`）+ 回填写 `cfgMinBuy`（字段表 28 项）+ 横幅显示「最低买入2000元」（`[core/farm_summary.py:250]` 的 `_money_txt` 带单位）；
- **LOW-H `window` 缺失静默沿用旧区间**：改为拒（`[core/farm_summary.py:298]`，加进缺项清单），实测 783 条 best 全有 window，无误伤；
- **LOW-I `refreshAllSummaries()` 排在横幅/切页上游**（抛错会被 catch 吞成"回填失败"，而 27 字段已写进表单、无横幅不切页——直接破坏"人工过目"前提）：挪到横幅显示与 `switchTab` **之后**并自吞异常（`[web/js/vera-ui.js:681]`），恢复路径同样处理（`[web/js/vera-ui.js:704]`）；
- **LOW-J 因子过滤脏数据**：经 MEDIUM-E 的事件派发顺带清除（见上）；
- **LOW-K 记账不一致**：第一轮报告第 40 行的错误关闭理由已纠正（见本报告 HIGH-A 行）；第一轮提交信息里「2H+5M+5L」与报告「2H/3M/6L」的笔误在本报告如实记录（提交信息不可改写，以此为准）；「六键全验」经 LOW-F 修复后已与代码相符。

## 第一轮 12 条修复的独立复核结果（复审逐条实读，我复验）

- **真修对 9 条**：HIGH-1（含前端 `select` 确有 `value="intraday"`，否则写值会静默回落 real）、HIGH-2（六开关+池子深合并复核为一致）、LOW-7（783 条已无 `"None"` 残留）、LOW-8、LOW-9、LOW-10、板块恢复守卫（形式正确，漏项见 MEDIUM-C）、自审 pct100 浮点（`(0.2*100).toFixed(6)` → "20"）、MEDIUM-4（位置对，顺序问题见 LOW-I）；
- **只修对一半 1 条**：MEDIUM-3（横幅可见路径已堵，`知道了`路径漏 → MEDIUM-D）；
- **部分修对 1 条**：LOW-6（四键而非六键 → LOW-F）；
- **未修 1 条（如实记录）**：MEDIUM-5（`cond_days>0`/`ladder≠off` 前端分支无自动化测试；生产档案两者均 0 条，分支不可达）。

## 明确无发现的类别（第二轮复审结论）

字段名端到端错配**无**（28/28 机械核对，快照集合 == 直写集合，脚本可复现）；第 29 个直写字段**不存在**；`cfgFactorFilterEn` 确实被远端消费（`[pipeline/pipeline.py:118]` 的 `enabled` 闸，残留 rules 不被消费）；`_caliber_text` 映射覆盖度够用；LOW-6 的 `is None` 判定**不误伤** 0/0.0 合法值；`/api/farm/prefill` 白名单与 400/404/409 正确；CALIBER 别名与 `is` 身份锁真生效；达标榜与回填包同读 `archive.json`（不会"榜上有行、回填 404"）；真实档案 783 条 best 健康（全有 params/window，ladder 全 off）。

## 深/浅模块逐文件定性（两轮合并）

| 文件 | 定性 | 评语 |
|---|---|---|
| `core/farm_rules.py` | **深 ✓ 但仍不完整** | 判定口径+执行口径同居"农场口径"是自然聚合；本轮补 `min_buy` 后执行口径更完整，但 `degrade_5m`/引擎入口这类"复跑差异"仍只能靠文案披露而非结构化字段 |
| `core/farm_summary.py` | **深 ✓** | `backtest_prefill` 藏 archive schema+口径组装+中文文案，公开接口 3 ≤ 8；本轮把六键校验与 docstring 对齐（消除"看着验了其实没验全"的浅层症状） |
| `core/farm_api.py` | **薄 ✓ 且薄得正确** | 18 行只做白名单 + KeyError/ValueError 映射 |
| `tools/formula_farm/farm_backtest.py` | **合格** | CALIBER 别名化 + `_slim_best` 扩 params，职责正当 |
| `web/js/vera-ui.js` | **浅层适配层，本轮减少了一处规则手写** | 板块状态收口进 `setSectors`（原 4 处手写 → 1 处）；但 28 字段表与"该复位什么口径"仍在这层——这是 HIGH-1/2、LOW-G 的共同根因，也是该层零自动化测试的代价。**结论：可接受，但前端状态机是当前最薄的一环**（两轮共 7 条发现落在这里） |
| `web/js/config.js` | **合格** | 本轮新增 `notifyFormulaChanged` 把"程序化改公式要通知下游"收成一处，消灭两副本 |
| `web/index.html` | **结构 OK** | 横幅 fixed 悬浮；操作列只加在达标组表头且与行 td 数一致 |

## 验证记录

- ruff：全部改动 Python 文件 **All checks passed**；`node --check` 两个 JS 文件通过；
- pytest：农场测试 **63 条全绿**（含本轮新增 window 必填、六键校验、披露文案断言）；全量套件（除 trade）**EXIT=0**；
- 机械核对（node 脚本）：字段表 28 == 直写 28，`直写但未快照: []`、`快照但未直写: []`、`HTML 缺元素: []`、`setSectors 调用 3 处`、`toggleUniverseDropdown 存在`；
- 真机冒烟（真实档案 GS0607）：`caliber_text` = 「沪深300 · 5分线 · 300万本金 · 单票上限2万 · 最低买入2000元 · T日收盘买入 · 移动止盈盘中触线(回测页标注为偏乐观的旧语义) · 移动止盈优先 · ⚠ 粗扫走稀疏60交易日窗口且缺5m的股-天丢信号, 本页为连续区间且5m降级补线(缺数据用日线补满), 笔数可能偏多, 数字别与粗扫并排比」；
- 本报告引用机器校验 `python -m docs.audit._verify_references`：**总引用 10 · 有效 10 · 无效 0 · PASS**（校验器只提取 `.py` 形式引用；`.js`/`.html` 引用为人工实读确认）。

## 遗留跟踪项（记录在案，不做）

1. **前端零自动化测试**：两轮共 7 条前端发现全部靠人工冒烟与机械核对兜住——建议后续引入最小 JS 测试能力（或把状态机逻辑下沉到可测的纯函数）；
2. **`run_cached` vs `run()` parity 实测**：本轮用文案披露差异（诚实但粗）；理想是做一次 GS0607 的双入口实测量化差值，把"笔数偏多多少"写进报告；
3. **`_caliber_text` 里把"复跑差异"结构化**（如返回 `rerun_diff` 字段供前端醒目展示）比长文案更可测——本轮未做。
