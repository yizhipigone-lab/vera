# 前端 UI/UX 全系统优化 计划书 两轮审阅报告

- 成文日期: 2026-09-05
- 被审对象: [docs/plan/2026-09-05_前端UIUX全系统优化_实施计划书.md](../plan/2026-09-05_前端UIUX全系统优化_实施计划书.md)
- 审阅方式: 第 1 轮逐行核验(计划书引用的每一处 文件:行 打开现场代码比对 + 技术论断验证 + 方案可行性挑刺);第 2 轮独立审读(新增/修订锚点复验 + 整合一致性 + 实施顺序风险 + 验收标准可执行性 + GO/NO-GO 判断)
- 最终结论: **GO**(第 2 轮 GO WITH CONDITIONS,5 条条件已全部写回计划书)

---

## 第 1 轮: 逐行核验(约 165 处锚点)

**汇总**: ✅ 约 148 处 / ❌ 3 处 / ⚠️ 约 14 处。核心事实(四处红绿反向、brain_chat 全量重渲染、echartsInit 销毁重建、enableZoom 监听泄漏、1 秒轮询全量 innerHTML)全部属实。

### ❌ 硬伤 3 处(已修入计划书)

1. **W2-2 张冠李戴**: 原稿称"api.js post(17-35 行)已有 AbortController+超时+r.ok 模式"——实际 api.js 全文件无此模式,真正有的是 trade.js:13-36 的 post(且连它也无内置超时,超时是 cmd() 从外面包的)。照抄原稿会把改造做在错误前提上。
2. **W2-2 锚定冲突**: trade.js 是经典 IIFE 脚本无法 import api.js;且 tdsSaveBtn 的 PUT 经 BASE 打 8081 跨源(api.js 只打同源 8080),原稿"走改造后的 api.put"照字面执行会 404。已改为就地套 cmd() 同款 4s 超时。
3. **W1-4 白名单锚定方式错误**: index.html:414-422 页签按钮只有 id 无 data-tab,白名单应以 vera-ui.js:415-421 switchTab 实际调用的名字为准(7 个名字本身全对)。

### 遗漏 5 项(已补入计划书)

1. **mobile.html:317-320 急停无 confirm**(P0 级,并入 W1-6)——全站最重的交易动作在移动端裸触发
2. **trade.js 亏损色用 --ok 而非 --down**(182/184/239/389/395,并入 W2-3.e)
3. **polish.css:16-18 全局 `table thead th` 无作用域**(并入 W4-6.3,与斑马纹同病)
4. **lab 报告渲染路径(vera-ui.js:404-406)也会注入 h1**(并入 W4-6.8)
5. **mobile.html statusText 无 aria-live**(并入 W1-7.6)

### ⚠️ 主要更正(已修)

- W1-3: 619 行是 select,label 在 618
- W2-1: 六接口消费字段无每秒必变时间戳,但比较键必须按消费字段构造(防响应体携带 server_time 类字段白干);tdsSaveBtn 保存成功后也调 refresh()(:1002),保存后要清对应 key;"复制委托号"举例不成立(无该列),换持仓行场景
- W2-3.c: 两处键名不同("成本止损" vs "硬止损"),两份映射分别改
- W3-1: SSE 协议实为 data:{JSON} 按 type 三分(channel/line/done),done 只写盘不渲染,.then 才全量渲染——改法随之简化
- W3-2: 现场签名 echartsInit(id) 单参+._charts 字典+不传 theme,复用方案简化为字典判存在
- W3-3: enableZoom 闭包还捕获 dzOn/dzOff/parentBox/zoomActive,单例化要一并处理
- W3-5: 补 chartMonthly(charts.js:592)第四处空数据残留
- W4-1: mobile 硬编码色实为 4 处(非 11);接入后只有 JetBrains Mono 加载(Space Grotesk 0 处引用);--green/--red 实为 5 处(25,26,97,101,105)
- W4-2.3: 8px 圆角恰 8 处、5px 恰 6 处,清单补全
- W4-3.1: viewport-fit=cover 现状已有,只删 maximum-scale/user-scalable
- W4-6.9: 日历标题覆写需数据加载成功,空/失败分支残留旧值,兜底改空串
- submitBacktest 必须显式豁免 api 层默认超时(vera-ui.js:158 已有 2h AbortController,默认 8s 会掐死 5M 池提交)

---

## 第 2 轮: 独立审读(约 150 处锚点复验)

**汇总**: 通过率约 98%,无硬伤。数量型断言全部精确(transition:all 恰 6 处、8px 圆角恰 8 处、--green/--red 恰 5 处、notMerge 主路径恰 12 处、六路接口一致)。关键前提确认: index.html:153 `.pos{color:var(--up)} .neg{color:var(--down)}`,即 pos=红/neg=绿,W2-3.d 的对齐逻辑成立。

### 轻量问题 4 个(已修/已注明)

1. W2-3.e 的 395 行是方向列非亏损色(定性已校正;trade.js:346 的 --ok 是健康语义,保留,grep 验证排除)
2. W1-5 的 .btn 预期清单漏 filter/transform(.btn-primary:hover 用了 brightness+translateY,已补)
3. W4-3"工具按钮不足 30px"表述边缘(实测约 30px,不影响决策)
4. polish.css:16-18 行号指选择器行,字号 11.5px 在 19 行(归属无争议)

### 整合一致性

未发现自相矛盾。4s(trade.js 就地)/8s(api.js 默认)双超时路径、W2-3 五处与验收"5 处"、W1-6 标题与内容、状态行与正文标记,均一致。

### 实施顺序风险(条件 1,已写入计划书 0.3)

- index.html 被 W1 五项触碰,不可并行,须从尾部锚点往前串行
- W1-2 与 W1-5 交叉改 index.html:75 同一行,须合并为一次编辑
- 行号一律按语义定位,防 W1-1 头部插行/W4-1 删块导致漂移

### 验收标准补充(条件 4,已写入 W2-1)

- 无 QMT 环境下 W2-1 验证用 DevTools Local Overrides 改写六路响应替代
- W3-1"不卡"以 Performance Long Task 对比留档为准;W4-3 iOS 跳变需真机

### 最终判断

**GO WITH CONDITIONS → 5 条条件全部写回计划书 → GO,开工。**
