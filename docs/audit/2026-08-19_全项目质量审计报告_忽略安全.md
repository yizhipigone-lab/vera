# 2026-08-19 全项目质量审计报告（忽略安全维度）

- **审计对象**: VERA 全项目（605 个 Python 文件 / 884 个 git 追踪文件 / 约 10 万行）
- **审计时点**: 2026-08-19 01:41，分支 `feature/policy-layer-a`，HEAD = `2caca2c`
- **审计方式**: 主线亲自实测（pytest 全量、ruff CI 同口径、12 条业务铁律逐条核验、parity 根因 git 追溯）+ 3 路并行只读子代理（代码质量 / 文档卫生 / 测试质量），所有结论独立复核，不沿用前序报告
- **安全维度**: 用户明确要求忽略，本报告不评分、不展开；仅在与质量相关的边界处（如静默吞异常是否掩盖对账告警）提及
- **结论先行**: **系统综合 6.0 / 10**。业务内核依旧很硬（12 条铁律全部合规、测试隔离三件套一流、limit_ratio 单一真相源已落地），但 **parity 验钞机红了 2 天 14 个提交没人管**（最硬的流程红灯）、**ruff 2383 违规**、**CI 形同虚设**，三件事都是"看门人"层面的失效。前序报告（2026-08-18，含安全，5.5/10）的安全 CRITICAL 本报告不涉及；质量层面两者结论一致：**核心问题是流程，不是内核**。
- **修复状态（2026-08-19 当日执行）**: Q1（parity 验钞机）、Q2（CI）、Q3（对账告警静默）**三项 P0 已修复并验证转绿**（见 §二 各条目"已修复"标注），全程贯彻深模块设计——不新增模块/转发层、不写第二份实现、最小侵入。**另：P2 项 9（拆 TradeApp 上帝类 / api.py 计算下沉）已由深模块治理计划完成**（见 §四 行动清单）。修复后综合预估 6.5/10。

---

## 一、记分牌

| 维度 | 评分 | 状态 | 一句话 |
|---|---|---|---|
| 业务铁律合规 | **9.0** | CLEAN | 12 条铁律全部合规，代码+测试双锁定；扣分在规则核心合并欠账 |
| 测试健康 | **7.0** | NEEDS WORK | 1862 用例 99.5% 通过，但 parity 哨兵 9 个全红 2 天；少量断言内 skip 会掩盖回归 |
| 代码质量 | **6.5** | WARNING | 纪律面好；风险在对账静默吞异常、夏普双口径、3 个巨型文件 |
| Lint (ruff) | **2.0** | CRITICAL | 2383 违规（CI 同口径实测，无正确性级错误，799 个可自动修） |
| CI / 工程化 | **2.0** | CRITICAL | CI 只挂 master 从未在本分支跑过；依赖不全；无 lock 文件 |
| 文档与仓库卫生 | **7.0** | WARNING | CLAUDE.md 架构表 14/14 全真、git 零泄密；CHANGELOG 停更 28 天、loop.md 过时 |

**综合 = 9.0×20% + 7.0×20% + 6.5×20% + 2.0×10% + 2.0×10% + 7.0×20% = 6.0 / 10**
（安全权重 25% 按用户要求剔除，其余权重归一化。）

历史对比（质量口径近似）：2026-07-02 约 4.5 → 2026-07-13 约 7.5 → 本次 6.0。**内核没有退步；拉低分的是 parity 哨兵失修 + CI 失效 + lint 积压三个"看门人"。**

---

## 二、HIGH 发现（先看这里）

### Q1. 回测 parity 验钞机红 2 天、14 个提交照常落地（流程红灯）

- 现象：`tests/test_run_cached_parity.py` **9/9 全红**（主线独立实测，非沿用前序报告）。断言为 `np.array_equal`（[tests/test_run_cached_parity.py:174]），差值约 6e-9 相对值（cached 999546.6750 vs direct 999546.6810）
- 根因（git 时间线已追溯）：08-16 20:58 提交 `97ff548` 在 [backtest/engine.py:326] 将生产路径价格矩阵统一转 float32；但测试对照组 `_direct_call` 仍用 float64 喂直调循环（[tests/test_run_cached_parity.py:135]）。该提交修了 `test_open_positions` 容差、重生了快照基线，**唯独漏了这个文件**——其最后一次修改停在 08-16 11:39（`61f180d`），即 float32 提交之前
- 之后 `97ff548..HEAD` 共 **14 个提交**（含实盘修复 `c6bc87d`、ETF 轮动避险腿 `e5caed4` 等）全部照常落地，无人发现
- 严重度判断：数值差异本身无实盘意义（百万分之六），**真正的问题是流程**——这是"run_cached 与直调核心必须一分不差"的验钞机，红了 2 天说明这期间提交没跑全量测试
- **✅ 已修复（2026-08-19）**：采用方向 ①——在测试直调入口 `_direct_call` 复刻生产的 float32 转换点（[tests/test_run_cached_parity.py:131-140]），**保持 `array_equal` 字节级断言强度不放宽**。9/9 用例转绿；同批快照基线/能力透传测试（test_snapshot_parity / test_run_cached_capabilities / test_loop_parity / test_open_positions）85 个全过。改动仅限测试文件，生产零改动

### Q2. CI 形同虚设（Q1 的漏网通道）

[.github/workflows/ci.yml](.github/workflows/ci.yml) 三个洞（主线实测确认）：
1. 只在 `master` 分支触发（原第 4-7 行）——当前开发分支 `feature/policy-layer-a` 的提交 CI 从没跑过，Q1 由此漏网
2. 依赖只装 `ruff pytest pandas numpy`（原第 18 行）——缺 fastapi/pydantic/pyarrow/python-dotenv，真跑了也会大面积 import 失败
3. `ruff check .` 必红（当前 2383 违规）——就算触发也过不了

**✅ 已修复（2026-08-19）**：① 触发分支扩为 `master` + `feature/*` + `feat/*` + `fix/*` + `refactor/*`；② 依赖装全（pyproject dependencies 核心 12 个包，fastembed/ddgs 为惰性 import 不阻塞测试）；③ lint 改为**正确性级门槛** `--select F821,F822,F823`（未定义名/重复定义，当前 0 违规）——不设虚门，也不被 2383 个存量风格违规堵死；风格债清理仍在 P1 计划内

### Q3. 实盘对账告警静默吞掉（违反"对账只告警"精神）

- [trade/api.py:762-767]（修复前 939-940）：资产页 QMT 对账差异检查 `except Exception: pass`——**查询失败与"对账通过"无法区分**，`reconciliation_warning` 静默丢失，页面不显示任何告警
- 这是对账铁律（"只告警+熔断"）在 UI 层的最后一公里断点：对账本身有急停兜底（[trade/reconciler.py:200-204] 确认合规），但资产页的温和告警通道被吞
- **✅ 已修复（2026-08-19）**：except 块补 `logger.exception(...)` 并置 `reconciliation_warning = True`（fail-closed：对账结果未知即提示人工核对，不静默吞掉），见 [trade/api.py:762-767]。logger 已存在（[trade/api.py:31]）未新增实体；前端消费路径（[trade/api.py:785] 返回 reconciliation_warning）原样复用

### Q4. 夏普双口径 + 第三份指标实现

- [trade/api.py:706]：实盘页夏普 `mean_ret/std_ret*sqrt(252)`，**不含无风险利率**
- [backtest/metrics.py:131]：回测夏普 `excess = returns - risk_free/252`（默认 0.015），**含无风险利率**
- 且 [trade/api.py:682-741] 整段手写指标（夏普/Sortino/Calmar/回撤/胜率/盈亏比/PLR）与 `backtest/metrics.py` 的 `MetricsCalculator` 构成第三份实现——**同一指标两把尺子，实盘页与回测页数字不可直接对比**，用户对账时会被误导
- 修复：实盘页复用 `MetricsCalculator`（或给 `_sharpe` 加 risk_free 参数并透传），删手写段。**注：深模块治理（2026-08-19）已将回撤计算下沉到 `trade/analysis.py::calc_drawdowns`（api.py:689 改调），但夏普/Sortino 手写段仍在 api.py 路由内，尚未收编**

### Q5. 文档双源打架：loop.md 描述已删组件为现行体系

- [docs/architecture/loop.md] §5/§7.7 仍写"保留 legacy parity 甲骨文"、引用不存在的 `tests/test_real_parity.py`——而 `_simulate_core_v3_legacy` 与兼容壳 2026-08-01 已删（CLAUDE.md 架构表 14/14 经核验全部真实、写的是"已删除"），**两个文档对同一事实说法相反**
- CHANGELOG.md 停更 28 天（最后 2026-07-21）且第 10 行有死链（指向不存在的"前置"报告）；此后 trade/ 实盘、1m 支持、缓存二期、ETF 轮动、Hindsight 本地化全部无记录，与 pyproject version=3.3.0 脱钩

---

## 三、各维度详情

### 3.1 业务铁律合规（9.0/10）—— 12 条全合规（主线逐条核验）

| 铁律 | 结论 | 关键证据 |
|---|---|---|
| 回测买入价 = T 日收盘 | ✅ | [backtest/loop/entry.py:25] 单一真相源 `EntryPath.BACKTEST_T_CLOSE`；换股特殊路径（reason=1 无滑点无印花税）有注释明确口径 |
| 对账只告警+熔断，不改账 | ✅ | [trade/reconciler.py:200-204] CRITICAL 差异唯一动作是激活急停；B vs C 交叉验证兜底（[trade/reconciler.py:172-185]）；一致也留行证明跑过（193-198） |
| 回调线程只做入队 | ✅ | [trade/gateway.py:133-134] 回调只转发；[trade_main.py:229-238] 先 append_raw 落盘再入队 |
| 交易状态唯一写者 | ✅ | 业务代码零锁；仅清仓锁+基础设施锁兜底（子代理核验 book 写点均在消费者线程链） |
| kill switch 三重态 fail-closed | ✅ | [trade/risk.py:33-43] 任一源为真即真；DB/文件读取异常 fail-safe 视为激活（39-42、75-78） |
| 预埋单买一锚定/涨停跳过 | ✅ | [trade/executor.py] 预埋单流水线；涨停档跳过写 audit |
| 原始回报先落盘 JSONL | ✅ | [trade_main.py:229-233] append_raw 在 sink.put 之前（铁律 5 注释在 225） |
| 卖出规则唯一（parity 锁死） | ✅* | [tests/trade/test_exit_parity.py:27-34] 直接 import 回测侧策略类对照；*规则核心合并仍是 P2 欠账，monitor.py 保留逐 tick 手写实现（[trade/monitor.py:336-409]，parity 锁口径） |
| 缓存安全三防线 | ✅ | [tests/conftest.py:105-122] autouse 隔离四缓存根到 per-test tmp；当日永不缓存由 signal_day_cache 守卫 |
| 5m 降级守卫 `==48` | ✅ | [backtest/engine.py:241]（注释记录了原 `>1` 被 1m 踩中的事故）；1m 显式告警降级不适用（246-249） |
| 前视偏差抽查 | ✅ | 信号侧唯一 shift 朝过去（[selection/signal_rules.py:52]）；引擎层跨日宁丢不顺延；TDX 公式体未来函数超出静态审计能力 |
| 政策事件不联入交易 | ✅ | 全仓 grep：policy_pipeline 只被 brain/ 与 tests/ import，trade//backtest//selection/ 零引用 |

**扣分项（-1）**：铁律 2g 规则核心合并未完成（monitor 手写实现 + parity 锁维持等价，参数改动需双处同步）；实盘夏普口径与回测不同（Q4，属铁律 4"同口径"精神的延伸欠账）。

### 3.2 测试健康（7.0/10）—— 量大质优，哨兵失修

- 总量：**1862 用例**（143 个测试文件），主线实测失败 **9**（全为 Q1 parity 哨兵），通过率 99.5%
- 测试基建一流：conftest 三道 autouse 安全网（[tests/conftest.py:95-102] 连接器重置 / 105-122 缓存隔离 / 125-149 线程收尾）；skip 共 10 处全为合理条件 skip（TDX 不可用/数据依赖/当日报告不存在）
- 覆盖盲区（子代理+主线双重核验）：**零直接测试 5 个模块**——`main.py`、`utils/logger.py`、`utils/sysutil.py`、`core/signal_exporter.py`、`trade/signals.py`（仅经 trade_main 间接）；`config_mapper.py` 最弱（仅经 server re-export 被 test_server_stop_defaults 碰一块）；`core/limit_ratio.py` 无专属文件但有 parity 间接覆盖（[tests/test_filter_limit_up_vectorized.py:92-102]，可接受）；server.py 部分覆盖（`/api/run` 无 HTTP 测试）；trade_main 强间接（11 个文件引用）
- 断言内 skip 5 处（[tests/test_end_to_end_real_pipeline.py:82,110,116,146,174]）：TDX 数据状态下无信号/无交易时跳过断言——**合理但会掩盖"信号日收盘买入"类回归**（无交易就永远不验证原则，文件 :97 注释自认该模式），建议数据缺失时标失败或独立标记而非 skip
- 测试替身签名不一致：`StubExecutor.execute_exit` 三份拷贝两种签名——[tests/trade/test_session_etf.py:133] 已带 `manual=False`（与生产 [trade/executor.py:330] 对齐），[tests/trade/test_monitor.py:46] 与 [tests/trade/test_monitor_peak.py:47] 缺 `manual` 且 `pending_check` 首参名漂移（生产 [trade/executor.py:390] 首参 `now_ts`）——当前 monitor 只调 `qty=`/`now_hhmm=` 故不炸，但是"改一个忘两个"的定时炸弹
- 0 处注释掉的断言（3 处 `#assert` 均为标题注释）、0 空测试（AST 扫 1702 个 test_* 函数，20 个无直接 assert 的函数全部核实为 helper 断言或"不抛即过"负向测试）；~403 处裸 `==` 均对照合成输入（安全）
- 网络焊死细节：conftest 只替换 `urllib.request.urlopen` 一个出口（[tests/conftest.py:216]）——requests/httpx 等库不经过它，若未来引入走 socket 的依赖需同步补焊
- DeprecationWarning：FastAPI 旧式 `@app.on_event("startup")`（[server.py:56]），建议迁 lifespan

### 3.3 代码质量（6.5/10）

**规模**：605 个 .py / 约 10 万行；research/ 占 28.7%（225 个一次性实验脚本）。

**好的方面**：trade/ 业务零锁纪律完好；日志统一走 `get_logger`（生产库裸 print 全在 `__main__` 自检块）；`_simulate_core_v3`/`stop_manager` 零活引用；TODO 仅 4 处且全为分级待办（无 FIXME/HACK）；limit_ratio 单一真相源落地并有 parity 测试；无循环 import。

**风险点**（HIGH 见 Q3/Q4；**深模块治理已于 2026-08-19 落地**，下述审计时点规模——trade_main 1351 行/37 方法、api.py 961 行——现为 trade_main 1235 行、api.py 788 行，日报/回撤等计算已下沉到 `trade/daily_report.py` 与 `trade/analysis.py`）：
- **MEDIUM**：巨型文件残留——trade_main.py 1235 行（TradeApp 仍 27+ 私有方法，事件处理与通知编排未全拆）、backtest/engine.py 1094 行（`run()` 416-690 约 275 行单函数）、trade/api.py 788 行（`positions()` 157-280 约 120 行，含 4 次补丁注释，当日盈亏复杂度积聚）
- **MEDIUM**：`qmt/qmt_portfolio_runner.py`（341 行）无任何 import 方，是**第二套手动实盘执行脚本**（自建 position_book.json 账本、DRY_RUN 开关、注释自述"未实测"）——与官方 trade/ 事件驱动系统并存，账本/规则漂移风险；`kg/chainkg_loader.py` 同类但属一次性数据导入工具，保留合理
- **MEDIUM**：实盘路径四处静默降级无日志——[trade/rotation.py:423-424] 查资产失败→cash=0.0 静默→预算帽归零不买；[trade/rotation.py:272-273] 盘中实时价失败静默回退昨日信号；[trade/api.py:349-350] 今日买入查询失败→{} 当日盈亏"今买腿"缺失；[trade_main.py:940-941] 轮动信号读取失败 pass 无日志（有注释"取不到就缺省"）
- **MEDIUM**：[core/lab_runner.py:129-130] `proc.kill()` 失败 pass 却返回"已停止"——**假成功**
- **LOW**：`tools/gen_snapshot_parity.py:20` 生产工具反向 import tests 包；`tools/formula_lab.py` 被 `core/lab_runner.py` 生产调用却含 9 处 print（stdout 污染）；[trade/monitor.py:29] TODO 注释已解决残留
- **LOW**：吞异常全仓 79 处 `except: pass`，trade/ 包 90 处 except 多数有 `_logger.exception` 或审计留痕——纪律总体优秀，上表 4 处是真问题

### 3.4 Lint（2.0/10）—— CI 同口径实测

`ruff check .`（pyproject 已排除 output/、.claude/，与 CI 命令等效）= **2383 违规**。分布：E402 模块级 import 846、E702 一行多语句 629、I001 import 未排序 431、E401 一行多 import 185、**F401 未使用 import 166**、F841 未使用变量 40、E741 歧义变量名 38、E701 22、F541 无占位 f-string 18、E731 7、E712 1。

注：无 F821 未定义名等正确性级错误；**799 个可 `ruff --fix` 自动修**（F401/I001/E401/F541 等）。建议先 `--fix` 清机械违规，再决定 E402/E702/E741 是修还是降级为 warn。

### 3.5 CI / 工程化（2.0/10）

见 Q2。另外：无类型检查（mypy/pyright 未配置）；无 lock 文件（依赖全 `>=` 下界钉法，[pyproject.toml] 6-20 行，重装环境不可复现，建议导出 `requirements-lock.txt` 入库）。

### 3.6 文档与仓库卫生（7.0/10）

**好的方面**：CLAUDE.md 架构表 **14/14 模块路径全部真实存在**（子代理逐行核验）；git 零泄密（.env、dump.rdb、*.parquet、*.db、config/trade.yaml 从未进库，git log --all 全历史验证为空，gitignore 全覆盖）；文档命名合规率 **94.5%**（git 追踪的 10 个目标目录共 182 个 .md，违规 10 个，多为规则生效前存量）；最近 4 份计划书（run_cached_prepared/PreparedMatrix、core/window.py、float32、hedge_etf2/hedge_ratio）声称事项全部经代码核实属实。

**要修的**（HIGH 见 Q5）：
- **MEDIUM**：根目录无 README，新人无法只靠现有文档自助跑起来（pyproject 有依赖说明、start_vera.bat 有启动注释，但 docs/ 无安装/启动文档）
- **LOW**：根目录被追踪杂物 3 个（research_tab_fixed.png / research_tab_p1.png / _feishu_preview.txt）+ docs/audit 下 1 个 .docx 入库；docs/plan/（44 文件）与 docs/plans/（1 文件残留）两目录并存，可合并；10 个违规文档待改名（docs/adr/0001-policy-match-three-layer-phased.md、docs/architecture/loop.md、docs/brief/热点追踪简报_2026-07-30.md、docs/frontend-optimization-plan.md、docs/gs_formula_signal_scan.md、docs/公式因子体检方法论.md、notes/weekly_2026-W31.md、research/hh_c1288/PROCESS.md、research/mmt/MMT_改良报告_大白话版.md、research/results/analysis_oos.md）
- **LOW**：CLAUDE.md 两处表述不准（vera-ui.js 实际在 web/js/vera-ui.js；"根目录仅余 main.py/server.py 两入口"未计入 trade_main.py 与 config_mapper/data_cache_api/lab_api/research_api 模块）；docs/audit/2026-08-18_全项目质量检查审计报告.md 在磁盘但未被 git 追踪（建议确认后入库）

---

## 四、行动清单（按优先级，全部非安全项）

**P0（本周）——✅ 已全部完成（2026-08-19）**：
1. ✅ 修 Q1：parity 测试对照组适配 float32（保持 array_equal 强度，9/9 转绿），改动 [tests/test_run_cached_parity.py:131-140]
2. ✅ 修 Q3：[trade/api.py:762-767] 对账查询失败补日志 + 置告警（fail-closed）
3. ✅ 修 Q2：CI 触发分支扩 feature/* 等 + 依赖装全 + lint 改正确性级门槛 [.github/workflows/ci.yml]

**P1（下周）**：
4. `ruff check . --fix` 清 799 个机械违规（F401/I001/E401/F541）
5. 修 Q4：实盘页复用 MetricsCalculator 统一夏普口径，删 api.py 手写指标段
6. 修 Q5：更新 loop.md §5/§7.7 删过时描述；补 CHANGELOG（7-21 以来月度变更）+ 修死链
7. 补 `trade/signals.py` 直接测试；三份 StubExecutor 签名统一；断言内 skip 改 xfail
8. [trade_main.py:1150] TODO P0：xtdata 昨收落地（卡在预埋单涨停判定上游）

**P2（月度）**：
9. ✅ **拆 TradeApp 上帝类 / api.py 计算下沉 —— 已完成（2026-08-19，深模块治理，提交 72f2388/a1051e4/f806b1a/cc88057/c8fe301）**：新增 `trade/daily_report.py`（build_trade_summary/enrich_sell_quotes/diff_positions）与 `trade/analysis.py`（calc_drawdowns/entry_and_closed/hold_days/name_of/rows_to_dicts/diff_dicts/deep_merge），trade_main.py 盘后日报逻辑与 trade/api.py 计算逻辑全部下沉为纯函数模块（依赖参数化，零 self 依赖），路由留薄转发；e2e 行为锁（test_notifier/test_daily_report/test_analysis）全绿。详见配套 [docs/plan/2026-08-19_深模块浅模块治理_计划书.md] 与 [docs/audit/2026-08-19_深模块浅模块_审计报告.md]
10. 裁决 qmt/ 第二套实盘脚本去留（与 trade/ 双轨是隐患）
11. 止损规则核心合并（消 monitor 第二份实现，铁律 2g 闭环）
12. rotation.py:423/272、api.py:349 三处静默降级补日志；lab_runner.py:129 kill 假成功修复
13. 补 README；清孤儿截图/docx；合并 docs/plans/；13 个违规文档改名
14. 导出 requirements-lock.txt；FastAPI lifespan 迁移

---

## 五、审计可信度说明

- 本报告所有 `[文件:行号]` 引用均经实际 Read 核实，并经 `python -m docs.audit._verify_references` 机器校验（结果附文末）
- 主线独立实测项：pytest 全量（1862 收集/9 失败）、ruff CI 同口径（2383）、parity 根因 git 时间线（97ff548 vs 61f180d）、12 条铁律逐条 Read、StubExecutor 签名、覆盖盲区
- 子代理审计项（代码质量 7 项 / 文档 7 项 / 测试 7 项）均要求先 Read 再引用，输出已由主线抽查复核
- 能力边界：TDX 公式体内的未来函数（用户自写公式含 REFX 类引用）无法静态排除；实盘 QMT 链路未做动态联调（只读审计）
- 回测绝对值可信度提示沿用历史结论：方向系统性乐观，相对排序在同口径下可参考
- 与 2026-08-18 报告的关系：本报告是其质量侧独立复核——除安全 CRITICAL 外，parity/ruff/CI/夏普/对账告警等发现全部经独立手段复现属实；评分差异（5.5→6.0）来自剔除安全维度后的权重归一化，非结论分歧
