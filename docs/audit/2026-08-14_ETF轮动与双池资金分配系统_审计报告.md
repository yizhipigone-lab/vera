# 2026-08-14 ETF轮动与双池资金分配系统 审计报告

> 审计对象：`docs/plan/2026-08-14_ETF轮动与双池资金分配系统_计划书.md` 及其对应实现。
> 审计方式：两遍（第一遍程序化机器校验，第二遍逐行语义比对）。日期 2026-08-14。

## 结论

**放行。** 两遍审计共发现 1 处遗留不一致（计划书 §九「状态机」vs 实现「状态派生」），
已修正；未发现符号缺失、字段不符、文件缺失、接线遗漏或行为语义错误。

## 第一遍：程序化机器校验（PASS）

用 `inspect`/`importlib` 逐项断言，全部通过：

| 校验项 | 结果 |
|---|---|
| `trade/rotation.py` 的 `compute_signal`/`_derive_state`/`RotationFeature`/三态常量/`STATE_RATIOS` 存在 | PASS |
| `RotationFeature` 公开接口 `start`/`on_signals`/`last`/`running` | PASS |
| `RotationConfig` 字段集 == `{enabled, etf_ratio, signal_index, cyb_etf, gold_etf, ma_window, high_window, drawdown_threshold, execute_time}` | PASS |
| `TradeConfig.rotation` 有默认工厂、`_FIELD_TYPES`/`_coerce_rotation` 存在 | PASS |
| `BaseGateway`/`RealGateway`/`FakeGateway` 均有 `query_daily_closes` | PASS |
| `EVENT_ROTATION == "rotation"` 且在 `_CRITICAL_TYPES` 关键事件集 | PASS |
| `OrderIntent.rotation` 字段 + `_check_sizing` 引用 `intent.rotation` | PASS |
| `AutoBuyFeature.__init__` 有 `budget_provider` 参数并赋值 `_budget_provider` | PASS |
| `TradeStore.save_rotation_signal`/`load_rotation_signal` + `_SCHEMA` 含 `rotation_state` 表 | PASS |
| `TradeApp` 构造 `_rotation`、含 `_stock_budget`/`_stock_pool_value`/`rotation_last`、接线 `EVENT_ROTATION` | PASS |
| `create_api_app` 含 `/api/trade/rotation/last` 与 `/api/trade/rotation/run` | PASS |
| `trade/rotation.py`/`tests/trade/test_rotation.py`/`config/trade.yaml` 文件存在 | PASS |
| `config/trade.yaml` 含 `rotation:` 段、`etf_ratio: 0.5`、`execute_time` | PASS |
| `web/js/trade.js` 含 `renderRotation`/`tdsRotRatio`/轮询端点；`web/index.html` 含 `tdsRotEn`/`tdsRotRatio`/`tdRotRunBtn`/`tdRotLast` | PASS |

## 第二遍：逐行语义比对（1 处修正）

对照计划书每个声称的行为与真实代码：

| 计划书位置 | 声称 | 代码事实 | 结果 |
|---|---|---|---|
| §二 | MA20/250日高点/回撤算法、三档决策 | `compute_signal` 同式；冒烟测试三态/回撤-25%→half 通过 | 一致 |
| §三 | `E_target=etf_ratio×总资产`、`S_target=(1−etf_ratio)×总资产`、预算帽公式 | `_execute` 的 `pool=etf_ratio*total`；`_stock_budget` 的 `(1-etf_ratio)*total − stock_val` | 一致 |
| §四表 | 各文件改动 | 程序化校验全部命中 | 一致 |
| §五 | 9 字段 schema | `RotationConfig` 字段集完全一致；无 `signal_time` 残留 | 一致 |
| §六 | 单一时点、`end=昨日`、状态派生、换档先卖后买、补仓不主动卖 | `_worker` 的 `end=(now−1d)`；`_derive_state` 派生；`_execute` 卖→`_wait_fills`→买；`state_changed` 门控卖 | 一致 |
| §六 | auto_buy 预算帽 `max(0,S_target−股票市值)`、关闭返回 None | `_stock_budget` 返回该式 / `None` | 一致 |
| §七 | 轮动绕过 `max_buy_amount` 与 `max_positions`，其余四道闸照常 | `_check_sizing` 对 `intent.rotation` 跳过两者；闸1/2/3/4 未动 | 一致 |
| §八 | `exclude_etf=true` 使选股系统不碰两只 ETF | `monitor._evaluate`/`executor.place_ladder`/`auto_buy` 均以 `is_etf` 跳过；`is_etf(159949/518880)=True` | 一致 |
| §九 | 分阶段清单 | 第 2 条原写「状态机」，实现为「状态派生」 | **已修正** |

## 发现与处置

1. **§九「状态机」→「状态派生」**（遗留不一致）：计划书 §四已改为「状态派生」，
   §九分阶段清单漏改。已修正为 `信号纯函数 + 状态派生 + 调仓执行`。

## 附注（非计划书缺陷，但需真机确认）

- 信号取数 `query_daily_closes` 依赖 QMT xtdata `get_market_data_ex`，399673.SZ
  指数可取性需真机 spike 确认（计划书 §八已列为风险，属预期待验证项，非本次审计缺陷）。
- 本审计在沙箱内因 pytest 临时目录 `os.scandir` 被拦，无法跑完整 pytest 套件；
  核心逻辑（信号三态/配置校验/调仓订单/预算帽）已用独立脚本逐项断言通过，
  测试文件 `tests/trade/test_rotation.py` 已按真机 pytest 环境写好。
