# 候选 A 阶段 1 主体审计报告（commit 980b04f）

- **审计日期**：2026-07-13（周一）
- **审计师**：三角色并行（高级架构分析师 / 功能开发分析师 / 高级测试师）+ 交叉验证
- **审计对象**：commit 980b04f — run_cached 加厚前门 + capabilities 三开关 + 19 新测试
- **方法**：3 路 Explore agent 独立读代码 + 实跑测试 + 内省签名 + git diff 证据链
- **基线**：97 passed + 5 既有失败（均非本次引入，证据链见下）

---

## 0. 要求 vs 实现比对

| 要求（plan） | 实现 | 证据 | 结论 |
|---|---|---|---|
| run_cached 加厚前门（10 旧参不动+9 keyword-only） | ✓ | engine.py:827-846 | ✅ |
| 三类能力 config 开关显式化（默认全开） | ✓ | engine.py:887-907 | ✅ |
| 40 调用方零改动 | ✓ | grep 40 调用方 + parity 字节级 | ✅ |
| 不破坏回测口径（信号日 T 收盘买入） | ✓ | engine.py:475-484 未触碰 | ✅ |
| 守卫锁新签名 | 部分 | test_engine_run_path.py:336-374 | ⚠️ 默认值未锁 + 位置守卫 1-buffer |
| _simulate_core_v3 锁私有 | ✗ | 4 脚本仍直调 | ❌ 阶段 1.5 待办 |
| 11 条隐含约定进接口/断言 | 部分 | priority/ladder warning | ⚠️ close_raw/tradable 成对未断言 |

---

## 1. 严重级别汇总

**CRITICAL 0 / HIGH 4 / MEDIUM 7 / LOW 10**

### HIGH（4 项，三角色共识去重）

**H1. close_raw 自建 tradable_np 路径无测试 + 静默错配**（架构+功能+测试三角色共识）
- 证据：engine.py:896-904（10 行照搬 run 的 650-655，无独立测试）；test_run_cached_parity.py:79-140 的 _direct_call 漏复制此分支；6 个 capabilities 测试全直接传 tradable_np，无一传 close_raw
- 功能 H1：close_raw 是 2D numpy/list 时 `pd.DataFrame(close_raw)` 生成整数列名 → reindex 按 close.columns（股票代码）对齐 → 全 NaN → tradable_np 全 False → 买入全跳过 → 空交易空 equity，**无 warning/error**
- 风险：close_raw 是"收编直调脚本"的核心交付物，此 bug 会让 step 6 收编静默出错

**H2. run_cached 9 keyword 默认值未锁**（测试师）
- 证据：test_engine_run_path.py:341-357 守卫只查 `default != empty`，未锁精确值。对比 _simulate_core_v3 守卫（:77-82）锁了 `formula_exit_np.default is None` / `ratio==1.0` / `lag==1`
- 实测默认值：filter_limit_up=True / open_np=None / ... / return_raw=False —— 一个都没锁
- 风险：filter_limit_up=True 是 40 调用方行为命脉，被改 False 守卫不报

**H3. 兜底 priority 行为变更**（架构师）
- 证据：stop_config.py:71 兜底 dict 新增 `priority: trailing_first`；旧兜底无此键 → engine.py:856 回退 stop_first
- commit message 称"零回归"，但兜底路径（yaml 缺失时）priority 从 stop_first 变 trailing_first
- 有测试守护（test_stop_config_capabilities.py:25-34），是 intentional fix（兜底应与 yaml 对齐），但措辞不严

**H4. close_raw 自建无校验/异常/log**（功能师）
- 证据：engine.py:896-904 reindex 后未检查 tradable_np 是否全 False、未 try/except、未 log
- run 方法同逻辑（650-655）作用于自建 close_raw 故标签必然匹配；run_cached 接外部 close_raw，标签匹配无保证
- 风险：标签不匹配时静默全仓退市（reason 11）或退市检测失效

### MEDIUM（7 项，节选）
- M1 close_raw 逻辑分层错位 + 与 run 代码 drift（应抽 `_build_tradable_from_raw` helper 共用）
- M2 tradable_np/last_tradable_idx 允许单传 → 退市永不触发（应成对断言）
- M3 cap_gap=False / filter_limit_up=True / 非法 priority / 非升序 ladder 四条新增 gate/warning 路径无测试
- M4 parity _direct_call helper 手工复制 run_cached 逻辑（脆性，未测路径漂移静默）
- M5 位置守卫 1-buffer（<=11 实测 10，应 <=10）+ 注释错误
- M6 formula_exit_ratio keyword 跳过 float() 转换
- M7 n_ladder == len(ladder_profits) 未进断言

### LOW（10 项，略）
cap_formula=False 时 ratio 死计算 / priority warning 未重置变量 / close_raw 对齐契约未文档化 / return_raw=False 路径无测试 / 源码扫描守卫子串匹配可绕过 / 等。

---

## 2. 三角色总评

| 角色 | 评分 | 加分 | 扣分 |
|---|---|---|---|
| 架构师 | 8.0/10 | 向后兼容执行扎实（keyword-only+默认 None=旧行为）、配置三处一致、可回滚、无共享状态 | close_raw 无 parity（-1.0）、兜底 priority 变更（-0.5）、分层错位（-0.3） |
| 功能师 | 7.5/10 | 核心 parity 硬核兑现、gate 语义与 run 一致、不可变性零问题 | close_raw 静默错配（H1/H2）、tradable 单传（M1）、n_ladder 未断言（M3） |
| 测试师 | 7.0/10 | TDD RED→GREEN 证据链清晰、parity 真字节级、签名守卫分层合理 | 默认值未锁（H1）、close_raw 零测试（H2）、4 条 gate/warning 无测试（M1-M4）、位置守卫 1-buffer（M6） |

**整体 7.5/10。可交付：是**（核心 parity 主路径正确、0 回归），但 HIGH 项需在阶段 1.5 修复（close_raw 是 step 6 收编前置）。

---

## 3. 5 个既有失败均非本次引入（证据链）

1. git show 980b04f --stat：test_end_to_end_real_pipeline.py / test_metrics_annualization.py / metrics.py **未修改**
2. engine.run()（end_to_end 用）未改 — 只改 run_cached
3. run() 不读 capabilities（gate 只在 run_cached）
4. pickle output/gupiao012_a2_real.pkl 日期 2026-06-26，commit 2026-07-13，pickle 未在 commit 改
- **test_metrics_annualization ×3**：pickle 数据与硬编码期望漂移（ann 22.49% vs 期望 >25.5% 等），metrics.py 未改
- **test_end_to_end_real_pipeline ×2**：QUANTQQ 公式对 601872.SH 返回空选股（TDX 数据状态依赖），commit 时有信号、现在空

---

## 4. 建议修复项（阶段 1.5 前置，按优先级）

1. **H1+H4+M1 close_raw**：抽 `_build_tradable_from_raw(close_raw, close)` helper（run + run_cached 共用，消 drift）+ 补 close_raw 功能测试 + parity 覆盖 + 全-False warning + 标签对齐断言
2. **H2 默认值守卫**：锁 9 keyword 精确默认值（filter_limit_up=True / 其他 None/1/False）
3. **M5 位置守卫**：<=11 改 <=10 + 修注释
4. **M2 tradable 成对断言**：tradable_np 非空 ⇒ last_tradable_idx 非空
5. **M3 补测试**：cap_gap=False / 非法 priority warning / 非升序 ladder warning / return_raw=False
6. **H3 兜底 priority**：接受 intentional fix，commit message 后续纠正"零回归"措辞

修完 HIGH 项后达 9/10，可安全推进 step 6 脚本收编 + 候选 B。
