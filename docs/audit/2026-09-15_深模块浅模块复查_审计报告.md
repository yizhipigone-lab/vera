# 2026-09-15 深模块浅模块复查审计报告

> 审计范围：全项目（trade / backtest / selection / core / pipeline / brain / llm / scheduler / policy_pipeline / policy_kb / concept_kb / kg / evolution / notes_gen / server 及根目录 API 文件）
> 方法：AST 量化扫描（公开符号数/参数宽度/实现行数）+ 三路并行逐模块源码核实 + 与 2026-08-19 基线对比
> 判定框架：John Ousterhout《软件设计哲学》深/浅模块 + 本项目铁律（公开接口 ≤8；计算下沉纯函数；同一规则不许第二份）
> 基线：[docs/audit/2026-08-19_深模块浅模块_审计报告.md]

## 一、结论先行（大白话）

**骨架健康，纪律在起效，但有三处"同一规则第二份"需要动手，其中两处会影响数字结论。**

- 上次 17 个深模块标杆**无一退化**；三笔旧债修了两笔半（trade/api.py 从 876→492 行薄路由、日报域抽出 [trade/daily_report.py:19]，builder 继续冻结）
- 三个老"多份"坑（行情陈旧判定、下单口、复权口径）**已收口单源**，治理方向被验证是对的
- 但新发现三处第二份实现，按危险度排：
  1. 🔴 **农场达标线漏网**：[tools/formula_farm/farm_batch_sweep.py:30] 手写 ANN_MIN/DD_MAX 判定，绕过 [core/farm_rules.py]，**缺笔数≥20 守卫**——正是 2026-09-11 GS1292 事件（3 笔 100% 胜率被当最优）要防的事故形态
  2. 🔴 **夏普三个数**：分析页手写夏普不扣无风险利率 [trade/analysis_api.py:340]，回测扣 1.5% [backtest/metrics.py:186]，且 metrics.py 内部还有第二份 `_sharpe` [backtest/metrics.py:131]（只为测试存在，零波动守卫语义已分叉）——**用户对比分析页和回测页必然对不上**
  3. 🟡 **牛熊口径 research 残份**：[research/multi_factor_breakout.py:66] 手写 MA250+斜率，且斜率定义与核心不一致（绝对差 vs 相对斜率 [core/index_regime.py:38]）
- 路由埋计算的老毛病没根除，只是挪了窝：从 trade/api.py 搬到 [server.py:495] 三个分析端点、[lab_api.py:74] 一处、[trade/analysis_api.py:137] 两个厚端点

**总评：8/10**（与上次同级，债务在换地方而不是在消失——需要一道"新域编排/计算一律下沉"的闸门）。

---

## 二、2026-08-19 三笔旧债现状

| 旧债 | 状态 | 证据 |
|---|---|---|
| 🔴 trade_main.py 组合根过胖 | **部分修复，总量仍在涨**（1351→1416 行，私有 27→37） | 三个纯计算已抽出 [trade/daily_report.py:19] [trade/daily_report.py:100] [trade/daily_report.py:121]；`_notify_daily` 编排器留根合理 [trade_main.py:1000]；但停机日补算编排 `_run_gapfill` 又嵌进组合根 [trade_main.py:579]，rotation 错峰加约 30 行 [trade_main.py:1074] |
| 🟡 trade/api.py 计算埋路由 | **大部分已修，没抽干净** | api.py 现 492 行全薄路由，计算已下沉 [trade/analysis.py:261] [trade/view_calc.py]；分析端点迁出 [trade/analysis_api.py]；但 analysis_api 自己埋两个厚端点 [trade/analysis_api.py:137]（135 行）和 [trade/analysis_api.py:306]（115 行） |
| 🟢 builder.py 宽构造器 | **继续冻结，参数 40→42** | 新增 buy_price_np [backtest/loop/builder.py:67]（2026-08-20 open_t1 口径）和 max_turnover_pct [backtest/loop/builder.py:69]（2026-08-28 流动性约束），均 keyword-only 带默认值零行为变化；但守卫只锁位置参数 [tests/test_engine_run_path.py:60]，keyword 增长对守卫静默 |

---

## 三、深模块标杆复查（17 个无一退化）+ 新标杆

上次标杆全部稳固：[core/formula_runner.py]（1 公开扛 224 行）、[backtest/loop/loop.py]（1 公开扛 433 行）、[trade/reconciler.py]（2 方法扛 524 行，接口杠杆全包最大）、[core/kline_cache.py]（4 公开扛 557 行）等。

**新代码里长出来的深模块（好榜样）**：

| 模块 | 公开面 | 判定依据 |
|---|---|---|
| [trade/daily_report.py] | 3 纯函数/141 行 | 旧债 1 的修复产物，零 self 依赖 |
| [trade/asset_gapfill.py] | 6 符号/451 行 | 规划纯函数与写入分离，三入口共用 |
| [trade/config.py] | 真接口 4 函数/778 行 | 12 个"公开"是 schema 声明非方法；校验 fail-fast 单源 |
| [trade/quote_stale.py] | 1 函数 | 行情陈旧判定唯一真相源（三处调用全走它 [trade/executor.py:473] [trade/monitor.py:307] [trade/rotation.py:1009]） |
| [core/farm_rules.py] | 4 纯函数/116 行 | 达标口径唯一真相源 |
| [core/index_regime.py] | 2 函数/72 行 | 牛熊口径唯一真相源（生产侧） |
| [brain/claude_cli.py] | 2 公开/482 行 | 子进程生命周期/杀树/三类恢复全藏 |
| [llm/ai_config.py] | 6 公开/116 行 | 合并语义唯一真相源，_FIELDS 单表驱动 [llm/ai_config.py:83] |
| [scheduler/vera_scheduler.py] | 纯调度原语/309 行 | 零业务逻辑，舆情正文生成在脑层位置正确 |

**薄层典范（路由薄+实现厚的正确形态）**：[core/farm_api.py]（8 端点各一行转发）、[data_cache_api.py]、[ai_api.py]、[research_api.py]。

---

## 四、同一规则多份（本次审计的核心发现，按危险度排序）

### 🔴 1. 农场达标线第二份（会造成错误结论）

[tools/formula_farm/farm_batch_sweep.py:30] 手写 `ANN_MIN, DD_MAX = 0.15, -0.15`，[tools/formula_farm/farm_batch_sweep.py:115] 手写 pass 判定，未 import [core/farm_rules.py]，**缺"笔数≥20"守卫**——winners 报告会把 <20 笔的噪声标"达标"，正是 2026-09-11 GS1292 事件的复发形态。对照已收口的同类：[tools/gs_5m_sweep.py:53] [tools/formula_farm/farm_backtest.py:38]。
**修法**：换 `farm_rules.verdict()` 一行，白捡笔数守卫。
另：[tools/gs_top9_batch.py:126] 有 `v > 0.15` 手写计数（统计句非判定，轻微）。

### 🔴 2. 夏普三个数（用户两页对数会打架）

- 分析页手写 Sharpe=mean/std×√252，**不扣无风险利率** [trade/analysis_api.py:340]
- 回测 MetricsCalculator 扣 1.5% 无风险利率 [backtest/metrics.py:186]
- metrics.py 内部还有第二份：私有 `_sharpe` [backtest/metrics.py:131]（生产全走它）vs 公开静态 `sharpe_ratio` [backtest/metrics.py:186]（实际调用方只有测试 [tests/test_metrics_full.py:265]），两份的零波动守卫语义已轻微分叉
**修法**：分析页收口到 MetricsCalculator（或显式标注口径差）；metrics.py 内部 `sharpe_ratio = _sharpe` 别名化，消掉只为测试存在的第二份。

### 🟡 3. 牛熊口径 research 残份（定义已漂移）

- [research/multi_factor_breakout.py:66] 手写 MA250+斜率判定：斜率用 `ma.diff(20)` **绝对差**，核心用相对斜率 [core/index_regime.py:38]；min_periods 250 vs 核心拍板 200 [core/index_regime.py:27]
- [research/annual_review.py:93] "MA250 牛市占比"简化代理（无斜率，半份）
均为 research 实验代码不进生产链，但按铁律应收口或标注。

### 🟡 4. claude_cli 恢复规则文件内双写

流式分支 [brain/claude_cli.py:330] 与同步分支 [brain/claude_cli.py:399] 各写一份"max-turns 续跑/session 重建/瞬时网络续跑"三规则，注释自承"同款语义"。改一处漏另一处即漂移。
**修法**：抽共享的"错误分类→恢复策略"函数，两分支各调。

### 🟡 5. market_panel 分家尾巴

[brain/market_panel.py:21] 起原样复制 data_tools 私有助手（_HAS_AK/_ak/_section/_no_ak，docstring 自承"避免 import 环"）；[brain/data_tools.py:231] 留有兼容 re-export 转发层（4 个消费方可改 import brain.market_panel）。

### ⚪ 6. 低危记录项

- FEISHU_WEBHOOK_URL 环境变量名 4 处消费 [trade/notifier.py:29] [research/sentiment_notifier.py:26] [research/send_today_feishu.py:196] [tools/send_report_feishu.py:12]，但 POST 重活已收口 utils/feishu_webhook，严重度低；[tools/send_report_feishu.py:11] 手写解析 .env 与 load_dotenv 惯例不一致
- kg 读取 SQL 三处绕过门面直连 graph.db [brain/data_tools.py:82] [brain/archive.py:46]，kg/query.py 可补三个门面函数
- [ai_api.py:90] 同一表达式双次 ai_config.load()（双次读盘）
- [brain/dsh_channel.py:3] docstring 说"外部接口仅 3 个"实际 5 个（注释漂移）

### ✅ 已守住的单源（复查通过）

行情陈旧判定（[trade/quote_stale.py:25] 唯一实现）、下单口（Executor.place_order 唯一，[trade/rotation.py:1093] 与 auto_buy 均走它）、复权口径（映射只在 [core/dividend_type.py:33]）、selection 缓存 key 归一化（[selection/selection_cache.py:47] 唯一，universe_cache 复用）、merge_patch（[llm/ai_config.py:83] 唯一）、ai.json 三档消费方无第三份。

---

## 五、路由埋计算（老毛病没根除，挪了窝）

按"计算下沉纯函数"铁律，这 6 处应下沉：

| 位置 | 埋了什么 | 建议下沉到 |
|---|---|---|
| [server.py:495] /api/benchmark/history | field-major→records 变形循环 | 与下条共用一个纯函数（同一变形家族两份） |
| [server.py:571] /api/stock/kline | rows 组装 + NaN 整行丢弃 | 同上 |
| [server.py:476] /api/calendar | 月历网格构建 | scheduler/trading_calendar 的 month_grid 纯函数 |
| [lab_api.py:74] /api/lab/history | glob 规则 JSON + 解析 + adopted 计数 + 按名匹配报告 | core.lab_runner（体检域逻辑） |
| [trade/analysis_api.py:137] analysis_daily_pnl | 逐日盈亏链 + 月度聚合（135 行） | trade/analysis.py |
| [trade/analysis_api.py:306] analysis_summary | 回撤修复天数/Sharpe/Sortino/Calmar/胜率（115 行） | trade/analysis.py（顺带收口夏普口径） |

---

## 六、接口面增宽与守卫

- **builder.py 40→42 参数**：均为有记录的能力扩展（keyword-only 带默认值），位置契约未动；但 [tests/test_engine_run_path.py:60] 守卫只锁位置参数——建议把 keyword 参数名白名单加进守卫，让下次 +1 不再静默
- **_resolve_stop_and_build_loop 18 参数**（含 self）[backtest/engine.py:352]：私有+仅 2 内部调用方+测试 monkeypatch 捕获，内部接缝增宽，宽度已到盯防阈值
- **state.py PositionBook 20 公开方法**：与 2026-08-19 判定一致，性能取舍型浅容器，无新访问器物种，继续记录在案 [backtest/loop/state.py]
- **core/kline_cache_maintenance.py 9 公开**：超铁律 8 一条，period_stats 已被内部消费可降私有 [core/kline_cache_maintenance.py:99]
- **core/data_fetcher.py 13 公开 classmethod**：门面契约面，建议按 M7 先例记录在案不拆，或把 sector/name 两族内聚成子门面 [core/data_fetcher.py:419]
- **trade/executor.py / book.py / monitor.py**：类公开方法均 8/8 贴线未超铁律，健康
- **trade/gateway.py / store.py / gateway_ths.py**：契约型大接口，铁律 8 豁免，无变化

---

## 七、ponytail 发现总清单（按收益排序）

1. `replace` farm_batch_sweep 手写达标线 → `farm_rules.verdict()`，白捡笔数守卫 [tools/formula_farm/farm_batch_sweep.py:30]
2. `shrink` 夏普收口单实现：分析页调 MetricsCalculator + metrics.py 内部别名化 [trade/analysis_api.py:340] [backtest/metrics.py:186]
3. `shrink` analysis_api 两厚端点计算下沉 trade/analysis.py [trade/analysis_api.py:137] [trade/analysis_api.py:306]
4. `shrink` 收盘竞价限价规则双胞胎（买侧 [trade/auto_buy.py:283] vs 卖侧 [trade/executor.py:548]，时段边界/笼子系数各自硬编码）→ 抽 closing_auction 纯函数
5. `shrink` server.py 三个分析端点变形/网格计算下沉纯函数 [server.py:476] [server.py:495] [server.py:571]
6. `shrink` rotation.py 执行段拆分：_execute 单方法 225 行 [trade/rotation.py:575] + lots 簿记 6 方法 [trade/rotation.py:888] → rotation_exec.py，rotation.py 留信号/状态/调度
7. `shrink` trade_main.py 的 _run_gapfill 编排下沉 asset_gapfill.py，组合根只留一行调用 [trade_main.py:579]
8. `replace` research 牛熊残份 → core.index_regime.regime_series [research/multi_factor_breakout.py:66]
9. `delete` shadow.compute_shadow_state 一行别名，调用方直用 legacy_three_state.compute_signal [trade/shadow.py:33]
10. `shrink` claude_cli 恢复规则双写抽共享函数 [brain/claude_cli.py:330] [brain/claude_cli.py:399]
11. `delete` data_tools 兼容 re-export 转发层 + market_panel 助手副本 → brain 级公共小模块 [brain/data_tools.py:231] [brain/market_panel.py:21]
12. `guard` builder keyword 参数名白名单进签名守卫 [tests/test_engine_run_path.py:60]
13. `yagni`（顺手，非深浅）auto_buy 调 limit_ratio 漏传 st 参数，ST 股涨停口径偏松 [trade/auto_buy.py:252]
14. `shrink` lab_api /api/lab/history 聚合计算下沉 core.lab_runner [lab_api.py:74]

**net: 无 blocker；2 处影响数字结论（农场达标线、夏普口径）建议本周修；路由埋计算 6 处是同一铁律的未竟工程，值得一次性清扫。**

---

## 八、治理建议（按优先级）

1. **🔴 本周**：farm_batch_sweep 换 farm_rules.verdict（一行修复防错误结论）；夏普口径收口（两页对数一致）
2. **🟡 一次性清扫**：路由埋计算 6 处整体下沉（server.py×3、analysis_api.py×2、lab_api.py×1），都是纯移动低风险
3. **🟡 闸门**：立下"组合根/路由只许接线，新域编排与计算一律下沉纯函数模块"的明文纪律（trade_main 修完日报又吃进 gapfill 证明没有闸门就会反复）
4. **🟢 记录项**：builder keyword 守卫补强；research 牛熊残份收口；claude_cli 恢复双写抽共享；rotation 执行段拆分单独立项
5. **不做**：builder 42 参数契约继续冻结；gateway/store/data_fetcher 契约面大接口维持豁免记录

---

## 九、引用校验

本报告所有 `[file.py:line]` 引用已按审计纪律经机器校验：

```bash
python -m docs.audit._verify_references docs/audit/2026-09-15_深模块浅模块复查_审计报告.md
```
