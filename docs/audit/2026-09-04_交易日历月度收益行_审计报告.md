# 交易日历月度收益行 — 工作质量审计报告

- **审计对象**: 分析 Tab 交易日历"月度收益汇总行"功能(2026-09-04 实现)+ 顺带修复的"每月首日盈亏恒 0"后端基准 bug
- **审计日期**: 2026-09-04
- **审计人**: AI(实现者自审 + 独立对手复审子代理)
- **改动文件**: `trade/api.py`、`web/index.html`、`web/js/analysis.js`、`web/mobile.html`、`tests/trade/test_analysis_phase2.py`
- **审计方法**: ① 实现者逐条对照规格自查(所有引用行号经工具机器核对,`.py` 引用另经 `docs/audit/_verify_references.py` 校验) ② 独立对手复审子代理(无共享上下文的新鲜眼睛,对抗式找茬) ③ 运行环境只读探针 ④ 测试证据链复核

---

## 一、验收标准逐条核验

规格六条验收标准(2026-09-04 经用户确认拍板):

| # | 验收标准 | 核验结果 | 证据 |
|---|---|---|---|
| ① | 切月显示所看月份总收益,当月=月至今 | ✅ 代码路径核验 | 后端按所请求 year/month 聚合 [trade/api.py:573-595];前端挂载点 `analysis.js:177`(JS 行号经 Select-String 机器核对);切月竞态已加序号守卫 |
| ② | 首日盈亏=相对上月最后交易日涨跌,不再恒 0 | ✅ 测试钉死 | 修复逻辑 [trade/api.py:546-560];回归测试 [tests/trade/test_analysis_phase2.py:172-187] 红期实测 `Obtained: 0.0, Expected: 0.01` 坐实旧 bug;对手复审 A1 场景(前月行早于 25 号)亦已修复钉死 [tests/trade/test_analysis_phase2.py:235-252] |
| ③ | 空月份灰字"该月无交易数据",布局不塌 | ✅ 测试+HTTP 核验 | 后端空月返回 `{"_month": None}` [trade/api.py:594-595];测试 [tests/trade/test_analysis_phase2.py:226-230];前端兜底分支 `analysis.js:246-249`;运行中 8080 服务确认已在吐含容器的 index.html |
| ④ | 老消费方不受 `_month` 键影响;mobile 无幽灵行 | ✅ 双向核验 | 主前端按日期键查找(`analysis.js:195`,与 `_month` 无交集);mobile.html 过滤正则 `mobile.html:365`;全仓库 grep 确认 daily_pnl 消费方仅此两处;反向偏斜(新后端+旧缓存 mobile)记为已知局限 A4 |
| ⑤ | 账户首月回退当月首行,`baseline_is_prev_month=false` 可区分 | ✅ 测试钉死 | [trade/api.py:578-587];测试 [tests/trade/test_analysis_phase2.py:211-224] |
| ⑥ | pytest 全绿含全部新增用例 | ✅ 机器报告 | junitxml: **tests=481 failures=0 errors=0 skipped=0** exit=0(2026-09-04,含对手审计修复 + 2 个盲区钉死用例后复跑) |

**注**: `.js`/`.html` 行号不在 `_verify_references.py` 的 `.py` 模式覆盖范围内,已用 PowerShell `Select-String` 逐一机器定位核对(输出留痕于会话记录);`.py` 引用经校验器验证,见文末校验记录。

## 二、发现清单

### 实现者自查发现

| 编号 | 级别 | 发现 | 处置 |
|---|---|---|---|
| S1 | ~~MEDIUM~~ | **切月竞态窗口(既有,非本次引入)**: `refreshCalendarMonth` 连续快速切月时,慢的旧月响应可能后到覆盖新月渲染 | ✅ **已修**(对手复审 A2 同款): 新增 `_calLoadSeq` 请求序号守卫,`loadAnalysisData` 与 `refreshCalendarMonth` 两条路径互斥,最后发起者获得渲染权(`analysis.js:21/38/72/432/443`,行号经 Select-String 核对) |
| S2 | LOW | **像素级视觉验证未完成**: 浏览器桥受控标签页被关闭,无法截图核验。降级为: HTTP 内容核验(8080 已吐全部新文件)+ `node --check` 语法 + 降级态文案确认 | 待用户重启 trade_main 后肉眼确认一眼即闭环 |
| S3 | INFO | **降级文案歧义**: 老后端未重启 + 有数据的月份 → 显示"该月无交易数据"(实为"后端未更新")。规格批准该降级设计,但文案未区分两种成因 | 可选增强,未实施 |
| S4 | INFO | **口径说明**: 月度 `pnl_rate` 用资产原值一次除法,非逐日 `round(6)` 值连乘——数学上与复利合成等价(telescope),且免连乘舍入;展示位理论微差属精度而非错误 | 记录口径,无需处理 |
| S5 | INFO | **提交边界风险**: `trade/api.py`、`web/index.html` 工作区改动混有另一批策略层 WIP 的 hunk,本次 6 文件改动**均未提交** | 已明示;建议随策略层 WIP 一并落库 |

### 独立对手复审发现(无共享上下文子代理,对抗式审查,2026-09-04)

复审方法: 全部 `file:line` 实读校验 + 四个边角场景用真实 TestClient(fake gateway + 临时 SQLite)**实测复现** + 既有测试实跑。初裁 **FAIL**;下表为发现与处置(实现者已对每条独立复核后接受并修复):

| 编号 | 级别 | 发现(复审原文摘要) | 复现证据 | 处置 |
|---|---|---|---|---|
| A1 | MEDIUM | **前月最后行早于 25 号 → 首日基准修复失效 + 月收益错近一倍**: 取数窗起点=前月 25 日,前月末段无行(月末停机/长假/冷启动失败,CLAUDE.md 2026-09-02 事件即此类)时抓不到基准行,误走"账户首月"分支: 首日复显 0.00%(违反规格#2)、月收益 -1.98%(应 -1.00%)、tooltip 谎报"账户首月" | 实测: 7/10=1000k、8/3=1010k、8/4=990k → `_month.pnl_rate=-0.019802` | ✅ **已修+测试钉死**: 窗口起点放宽到前月 1 日 [trade/api.py:519-522];回归测试 [tests/trade/test_analysis_phase2.py:235-252](红→绿, 且钉死"前月多行取最后一行") |
| A2 | MEDIUM | **切月/进页竞态无序号守卫**: 慢的旧响应后到渲染成"新月标题+旧月数据";同文件 `_dailyReportSeq`(2026-08-07 审计 HIGH#2)修过一模一样的问题;二次进 Tab 时按钮已提前挂载,竞态窗口很宽 | 代码审读 + 同款历史缺陷 | ✅ **已修**: `_calLoadSeq` 序号守卫(与 S1 合并处置),`loadAnalysisData` 渲染前校验、`refreshCalendarMonth` 发起时取号/响应后比对;JS 无自动化测试设施,以 `node --check` + 代码审读核验(测试盲区如实记录) |
| A3 | LOW | **快照洞日成交从月买卖笔数静默丢失**: `buy_total` 原随资产快照行累加,洞日(停机缺行)成交不计入;`trading_days` 亦少计 | 实测: 8/4 缺行且有买入 → `buy_count=0` | ✅ **已修+测试钉死**: 笔数改按整月成交 `daily_trades` 聚合 [trade/api.py:569-572];回归测试 [tests/trade/test_analysis_phase2.py:254-267](红→绿);`trading_days` 快照口径已在注释注明 [trade/api.py:577] |
| A4 | LOW | **部署偏斜只防了一半**: 已知局限列了"老后端+新前端",反向"新后端+浏览器缓存旧 mobile.html"时 `_month` 幽灵行回归 | 代码审读 | 📝 已知局限补记(本节即是);mobile.html 与后端同批发布(同一工作区改动集,同时生效),窗口期风险极短 |
| A5 | LOW(存量) | `?month=13` 手输 → `datetime(2026,13,1)` 抛 ValueError → 500;`git diff` 证实非本次 diff 引入 | 参数手输 | ✅ **顺手修+测试钉死**: `Query(ge=0, le=12)` 收紧 [trade/api.py:497-498],0 哨兵保留;回归测试 [tests/trade/test_analysis_phase2.py:269-274](红→绿,500→422) |
| A6 | INFO | 测试盲区: 跨年/前月多行/短月尾界/月末含端/trades 窗边界/前端渲染均未钉死 | — | 🟡 大部分已钉: 前月多行(随 A1 [tests/trade/test_analysis_phase2.py:235-252])、跨年 [tests/trade/test_analysis_phase2.py:277-290]、trades 月窗边界(前月末日 23:59 排除/月末小数秒纳入)[tests/trade/test_analysis_phase2.py:292-302] 均固化为钉死用例(钉死型: 复审实测正确后固化防回归,生而绿)+ 复验轮遗留二连: 前月整月停机基准回退(load_prev 根治)、year 上界([tests/trade/test_analysis_phase2.py:307-323] [tests/trade/test_analysis_phase2.py:325-331],红→绿);短月 `-31` 尾界由审读+复审确认;前端渲染零测试仍为盲区 |

**复审确认无问题项**: XSS(`esc()` 全套转义,`title` 属性无法逃逸);空值兜底;flex-wrap 布局;时区窗口同机自洽;12→1 跨年实测正确;`-31` 尾界安全(字符串比较不解析日期);逐日链与月度一次除法的数学等价性(telescoping)。

**处置后裁决**: FAIL → **PASS**(A1/A3/A5 红→绿钉死,A2 修复并核验,A4 补记,复审全部有效发现闭环)。

### 复验轮 + 遗留三项闭环(2026-09-04 第三轮)

复审代理对修复轮做只读复验(现版代码实读 + TestClient 场景重放 + 全量实跑),**维持 PASS**,并留 3 项不阻塞遗留。实现者当日全部闭掉(不留"下轮"):

| 编号 | 级别 | 遗留发现(复验实测) | 处置 |
|---|---|---|---|
| R1 | LOW | **前月整月停机仍误判"账户首月"**: 8/31 有行+9 月整月无行+查 10 月 → day1=0、baseline=10-05、m_rate=0(应退 8/31 基准)。窗口放宽是治标,任意窗口总有更长停机逃出 | ✅ **根治**(红→绿): 废弃窗口法,基准行改 `store.daily_asset.load_prev` 单查「当月1号前最后一行」(`WHERE date < ? AND total_asset > 0 LIMIT 1`,无窗口概念,自带零资产过滤)[trade/api.py:519-522];回归测试 [tests/trade/test_analysis_phase2.py:307-323](红期实测 `0.0≠0.01`) |
| R2 | LOW | **year 无上界**: year=9999/10000 手输 → 500(Windows timestamp 溢出/datetime ValueError),与 month=13 同性质,原修复只做了一半。复验建议 le=9999,但 9999 本身在 Windows 仍 500 | ✅ **修复**(红→绿): `le=3000`(Windows mktime64 平台上限 3000-12-31,比建议值更严一格)[trade/api.py:497-498];回归测试 [tests/trade/test_analysis_phase2.py:325-331](3001→422、3000→200) |
| R3 | LOW(相邻既有,非本次引入) | **日报卡片复活**: 格子日报慢响应在途时切月,`hideDailyReport` 后慢响应回来把旧月卡片 `display=''` 复活(2026-08-07 起存在) | ✅ **修复**: `refreshCalendarMonth` 顺手 `_dailyReportSeq++` 作废在途日报请求(analysis.js:432,行号经核对);JS 无测试设施,`node --check` + 复验代码审读确认,版本号升 v=20260904c |

**复验轮过程事故(如实记录,绿门机制起效)**: R1 重构首跑时实现者犯了两个错——① 调用写成 `store.load_prev`(少一层 `.daily_asset`)→ AttributeError → 11 个 daily_pnl 用例全 500;② 汇总命令给用例无条件打 PASS 标签、谎报"全过"。**junitxml failure 节点 + exit=1 当场拦截**,修正调用后 481 例真绿(核验命令同步修正为先查 failure 节点再下结论)。教训: 汇总脚本必须检查 failure 元素,不能按名字匹配打标签。

## 三、过程纪律核验

| 纪律 | 执行情况 |
|---|---|
| 需求先行(grill) | ✅ 5 个决策点经 `ask_user_question` 用户逐项拍板(行内容/修 bug/月至今口径/空月文案/不做基准对比),六维规格经 `doublecheck_spec` 落档 |
| 红→绿测试纪律(首轮) | ✅ 实现已先行的情况下,用 `git stash push -- trade/api.py`(单文件+备份双保险)还原旧代码取得真实红证据:4 用例失败(含 `KeyError: '_month'` 与首日 `0.0≠0.01`);恢复后同套 10/10 绿 |
| 红→绿测试纪律(审计修复轮) | ✅ 对手复审 3 项修复(A1/A3/A5)严格先写测试亲见红(3 败: 窗口基准 `baseline_is_prev_month=False≠True`、洞日 `buy_count=0≠1`、`500≠422`),后修复转绿——本轮实现未超前于测试,无需还原技巧 |
| 独立对抗复审 | ✅ 无共享上下文子代理独立审查,初裁 FAIL + 6 项发现(2 MEDIUM/3 LOW/1 INFO);实现者逐条独立复核(含数学复算)后全部接受并闭环,处置见 §二 |
| 复验轮(裁决收敛) | ✅ 修复清单回发原代理只读复验: 四项全过(含 `ge=0` 取舍被接受)、维持 PASS;遗留 R1/R2/R3 当日全部闭环(见 §二复验轮),R1/R2 红→绿、R3 语法+审读 |
| 绿门(最终全量) | ✅ 复验轮遗留修复全部落地后复跑 `tests/trade/`:junitxml `tests=481 failures=0 errors=0 skipped=0` exit=0(11 个 daily_pnl 用例逐一确认无 failure 节点) |
| 审计引用机器校验 | ✅ `.py` 引用经 `python -m docs.audit._verify_references.py` 校验(结果见文末);JS/HTML 行号经 Select-String 机器定位 |
| 实盘安全边界 | ✅ 全程只读探针(8081/8080),未重启/未触碰实盘 trade_main;符合"宁可不动"铁律 |
| 提交纪律 | ✅ 面对混合 WIP 工作区,拒绝制造"测试必红的残缺提交",不自动 `git add`;符合"先 git diff 评估"约定 |

## 四、部署面核验(只读探针,2026-09-04)

| 探针 | 结果 | 结论 |
|---|---|---|
| `GET :8081/.../daily_pnl?year=2026&month=8` | 有日期键、无 `_month` | trade 服务为旧进程,**重启 trade_main 后生效**(冷启动纪律: 先等 QMT 就绪 30s~2min) |
| `GET :8080/web/index.html` | 含 `calMonthSummary` 容器 + `v=20260904c` | 前端静态文件已由运行中服务直接吐出,**刷新页面即生效**,无构建步骤 |
| `GET :8080/web/js/analysis.js?v=20260904c` | 含 `renderMonthSummary`、`_month` 读取、`_calLoadSeq` 序号守卫 | 同上(复验轮遗留修复后版本 c 复核通过) |
| `GET :8080/m`(移动端) | 含日期键过滤正则 | 幽灵行防护已上线 |

当前线上组合 = 新前端 + 旧后端 → 月度行显示降级文案,行为符合规格设计的降级路径(不报错、不误导为崩溃)。

## 五、测试质量评估

- **钉死的行为**(11 个 daily_pnl 用例): 首日基准(防 bug 回归)、月度汇总数值与计数、首月回退、空月形状、前月行不泄漏([tests/trade/test_analysis_phase2.py:172-187] [tests/trade/test_analysis_phase2.py:189-209] [tests/trade/test_analysis_phase2.py:211-224] [tests/trade/test_analysis_phase2.py:226-230])+ 审计修复三连: 前月行早于 25 号窗口(含"前月多行取末行")、洞日成交计数、month 参数边界([tests/trade/test_analysis_phase2.py:235-252] [tests/trade/test_analysis_phase2.py:254-267] [tests/trade/test_analysis_phase2.py:269-274])+ 盲区钉死二连: 跨年窗口、trades 月窗边界([tests/trade/test_analysis_phase2.py:277-290] [tests/trade/test_analysis_phase2.py:292-302],钉死型用例,复审实测正确后固化,生而绿)+ 复验轮遗留二连: 前月整月停机基准回退(load_prev 根治)、year 上界([tests/trade/test_analysis_phase2.py:307-323] [tests/trade/test_analysis_phase2.py:325-331],红→绿)
- **数值精度**: 断言用与响应同款 `round(x, 6)` 精度比较,防浮点边界假阳/假阴
- **红→绿三轮证据**: 首轮 4 例(还原旧代码取得真红)、审计修复轮 3 例、复验轮 2 例(实现未超前,天然红),共 9 例全部亲见红后转绿
- **测试盲区(如实)**: 前端 JS 无自动化测试设施(仓库无 JS 测试跑器接入该文件),切月序号守卫 A2/S1 靠代码审读 + `node --check` 核验;短月 `-31` 尾界、月末 31 日含端由复审实测/审读确认但未固化为用例;跨年与 trades 窗边界已补钉死用例
- **回归面**: `tests/trade/` 全套 481 例全绿,确认未破坏既有契约

## 六、总评

**结论: PASS(可交付,附条件)。**

- 审计闭环质量: 自查发现 5 项(S1-S5,其中 S1 升级为已修),独立对手复审发现 6 项(A1-A6,2 MEDIUM/3 LOW/1 INFO),复验轮遗留 3 项(R1-R3),**14 项发现全部当日闭环**——8 项修复中 6 项带红→绿回归测试、2 项带语法+审读核验,其余为补记/钉死
- 三轮收敛轨迹: 自查 PASS(盲) → 复审 FAIL(2 MEDIUM 实测复现) → 修复后复验 PASS → 遗留 3 项再闭环。复审价值实证: A1(前月末段停机窗口)与 R1(整月停机)均为实现者自查漏掉的真实缺陷——独立新鲜眼睛 + 实测复现的方法有效
- 过程也如实记录了实现者自己的失误(§二复验轮事故: `store.load_prev` 少写 `.daily_asset` + 汇总命令谎报 PASS),均被绿门机制(junit failure 节点 + exit 码)当场拦截——机制比自觉可靠
- 最大遗留动作(用户侧): **非交易时段重启 trade_main**;重启后肉眼确认月度行像素呈现(S2 闭环)
- 交接注意: S5 — 本成果未提交,依赖工作区保全;改动文件现为 6 个(+`docs/audit/` 本报告)

---

### 附: 引用机器校验记录

- 校验命令: `python -m docs.audit._verify_references.py docs/audit/2026-09-04_交易日历月度收益行_审计报告.md`
- 结果: **PASS — 总引用 15 | 有效 15 | 无效 0 | 通过率 100%** (2026-09-04 审计修复后复验)
