# 盘后日报增强 — 代码审计报告

> 📅 2026-08-07 ｜ 被审对象 commit `2767f21`（13 文件 +1162 -58）
> 方法：三角色并行 agent（后端代码质量 / 盈亏数据流追值 / 前端）+ 主控交叉验证 + 追值法
> 范围：含前端 + 全链路每一跳（audit-verification.md 高危区：盈亏计算 + 数据库 + 前后端传递）

## 总体结论

**主干 PASS，2 个 HIGH 需修后合入。**

三 agent 独立收敛到同一判断：**核心数据流单位全程一致（零 ÷100/×100 bug）、前后端字段契约零漂移、数据库迁移安全、实盘铁律合规**。问题集中在两个边缘路径（补记 / 前端竞态）和若干 UX/健壮性细节。

| 维度 | 结论 | 出处 |
|---|---|---|
| 盈亏单位全链路（元/百分数/股） | ✅ PASS | 追值法 10 跳对照表 |
| 前后端 + 飞书字段契约 | ✅ PASS | 逐字段对照 _notify_daily / _build_daily_card / renderDailyReport |
| day_pnl 基准 = _day_baseline（盘前铁律） | ✅ PASS | prev_snapshot 只做仓位变动 diff，未串入盈亏 |
| 数据库迁移（trades 加列 / daily_report 表） | ✅ PASS | 幂等 ALTER，老库不炸，历史行默认 0 |
| 实盘铁律（唯一写者/回调只入队/QMT 真相源） | ✅ PASS | _on_eod 走 EVENT_EOD 消费者线程 |
| XSS / null 兜底 | ✅ PASS | 所有插值过 esc()，逐字段 null 守卫 |

## 问题清单

### CRITICAL — 0

### HIGH — 2

#### [HIGH#1] reconciler 补记路径漏算 pnl → 日报 realized_pnl 静默欠算
- **文件**：[trade/reconciler.py:296-305](../../trade/reconciler.py#L296)
- **复现**：QMT 成交回调丢失（补记是主路径）→ 15:05 `_on_eod` 调 `sync_reports` 补记卖出 → record 字典无 pnl_amount/pnl_pct → `save_trade` 存 0 → 日报 `realized_pnl` 欠算、`/deals` 显示空。**但同笔飞书成交卡**（经 `_on_adopted_trade:351` → `_sell_pnl`）盈亏正确 —— 违反"单源 book 成本法"。
- **根因**：`pre_avg_cost`（285 行）已在手、`_sell_pnl`（trade_main.py:698 static）现成，只是 record 组装时没塞。
- **决策**：**修**。reconciler 内联算 pnl 塞 record（pre_avg_cost 已在手，算法 2 行；注释标明与 _sell_pnl 同口径）。

#### [HIGH#2] showDailyReport 无序号守卫 → 快连点显示陈旧日期
- **文件**：[web/js/analysis.js:303-316](../../web/js/analysis.js#L303)（showDailyReport）
- **复现**：点格子 A(8/7) 发 fetch_A → 未返回时点 B(8/8) 发 fetch_B → 若 fetch_A 后 resolve，日报卡片标题/内容被覆盖成 8/7，但日历高亮和 _selectedDate 已是 8/8。
- **决策**：**修**。加 `_dailyReportSeq` 序号守卫，旧响应丢弃（5 行）。

### MEDIUM — 4（修 2，跟进 2）

#### [MEDIUM#1] 首次 EOD 仓位变动把全部既有持仓误报为"新进" — 修
- **文件**：[trade_main.py:860](../../trade_main.py#L860)
- **根因**：`load_position_snapshot()` 表空时返 `{}`，`{} is not None == True` → 进 diff → 全持仓 cv>0/pv=0 分类为 `new`。首次部署/清空后第一份日报会把所有老持仓列成"今天新进"。
- **决策**：**修**。`if prev_snapshot is not None:` → `if prev_snapshot:`（空 dict 也跳过）。

#### [MEDIUM#2] load_daily_report 未包 JSON 异常 — 修
- **文件**：[trade/store.py:597-603](../../trade/store.py#L597)
- **根因**：`load_latest_daily_report` 有 `try/except → None`，`load_daily_report` 没有。payload_json 损坏 → /daily_report 端点 500（契约承诺 null）。
- **决策**：**修**。补同样的 try/except 返 None（fail-soft 一致）。

#### [MEDIUM#3] 0 值着色红 + calToday/reset 状态不一致 — 跟进
- `_pnlColor(0)` 返回红（应中性）；calToday 设 _selectedDate 不展开卡片；deals reset 清 _selectedDate 不收起卡片。三处 UX 不一致，**非数据错误**。
- **决策**：**跟进**（记录，合并后单独处理）。

#### [MEDIUM#4] win_rate 单位（小数）与 pnl_pct（百分数）不对称 — 跟进
- 同一 payload 内 win_rate 是 0-1 小数、pnl_pct 是 0-100 百分数，显示层各自正确处理（非 active bug），但命名陷阱。建议改名 win_rate_frac 或统一。
- **决策**：**跟进**。

### LOW — 4（全跟进）

保本卖出(pnl=0)在 /deals 显示空（`if col_amt:` 真值判断）；422 误报"无记录"；收起按钮 2 个多余 fetch；count 字段未 esc（纵深防御）。均不影响正确性，合并后跟进。

## 修复状态

| 问题 | 决策 | 状态 |
|---|---|---|
| HIGH#1 补记漏算 pnl | 修 | ✅ reconciler.py:306-312 内联算 pnl 塞 record + e2e 测试 `test_backfill_sell_records_pnl_high1` 锁死 |
| HIGH#2 showDailyReport 竞态 | 修 | ✅ analysis.js 加 `_dailyReportSeq` 序号守卫，旧响应丢弃 |
| MEDIUM#1 首次 EOD 误报新进 | 修 | ✅ trade_main.py:860 `is not None` → truthy |
| MEDIUM#2 load_daily_report JSON 异常 | 修 | ✅ store.py:597 补 try/except 返 None（与 load_latest 同 fail-soft） |
| MEDIUM#3/#4 + LOW×4 | 跟进 | 📝 记录（非数据错误，合并后单独处理） |

> 验证：`py_compile` 3 文件 + `node --check` analysis.js 全过；reconciler 全文件 22 测试绿（含新 HIGH#1 e2e）；全量 pytest 见 git commit。
