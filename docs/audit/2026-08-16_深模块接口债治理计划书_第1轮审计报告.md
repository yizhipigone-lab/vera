# 深模块接口债治理计划书 · 第 1 轮审计报告

> 成文日期 2026-08-16 · 文档类型 审计报告 · 审计对象 `docs/plan/2026-08-16_深模块接口债治理_计划书.md`（v1 初稿）
> 审计方式：独立对抗性审计，逐条读代码验证计划书技术主张。本文 `[文件:行号]` 均经机器校验。

## 总评

选题基本对，但 **4 处前提性错误会让实施踩坑**，v1 不可直接照做。已全部在 v2 修订版改正。

## 必须改（4 处，均已在 v2 改正）

1. **P1-3「删 L0」被代码证伪**：`signal_day_cache` 只覆盖 period=1d（`[selection/signal_day_cache.py:20]`），`selector.py:204` 有硬守卫 `self.period == "1d"`。删 L0 会让 5m/1m 选股失去唯一缓存。→ v2 改为「不删 L0，只做接缝回收」。
2. **P0-3「删 signal_exporter」错误**：有活生产调用方 `report/tdx_export.py:73`（经 pipeline step5_export_to_tdx 可达），且 `print_to_tdx` 非纯透传（补 ensure_connected/vertical/默认文件名/try→bool）。→ v2 撤回删除，评估报告同步修正「纯透传」结论。
3. **P2-1 三个数字全错**：调用方 ~22（8 生产 + 14 测试）非 40；9 位置参数里只有 6 个矩阵（另有 n_ladder 标量 + stop_config 配置 + `selections` 死参数，函数体从不引用）；"18→2" 不可达，现实下限 ~6-9。且漏了最大风险：run_cached 与 run() 共用 `_resolve_stop_and_build_loop`（`[backtest/engine.py:301-402]`），塌缩必然连带改共享段 + run() 路径。→ v2 全部纠正。
4. **P1-1 推翻 M7 裁决却未引用**：CLAUDE.md 铁律 8 明确「store 拆成多个类只会加转发层…不拆模块」（2026-07-26 M7）。→ v2 补「先重新论证 M7」前置 + DDL/migration 归属 + `get_daily_asset_count` 死代码单列删除。

## 建议改（3 处，均已在 v2 吸收）

5. P0-1：`prefilter.py:91` 兜底 `return False` 违反文件头「拿不准返 True」的 fail-open 契约 → v2 加「立即缓解：未知策略 → 恒 True」步骤；派生谓词改用标量输入束而非 `(pos,bar,ctx)`。
6. P1-2：`dataclasses.fields` 只能派生字段名/顺序/to_dict 骨架，输入侧类型放宽 + 值域校验 + tuple→list 必须手写 → v2 改口「派生骨架 + 手写放宽」。
7. P2-2：三处阈值都是 `quote_stale_sec=60`（一致），真实分歧是 ts 缺失处理（executor 判不陈旧 / rotation 判陈旧）→ v2 改口「统一到 fail-closed」。

## 可选（2 处，已在 v2 处理）

8. `degrade_report.py` 阈值重放在计划书两头不落 → v2 移入「附录：明确不做」并给理由。
9. 「1835 测试」应实测、「P2-1 依赖阶段0/1」是伪依赖 → v2 已改。

## 结论

v1 经审计后修订为 v2，4 处前提性错误全部纠正，其余建议全部吸收。v2 可进入第 2 轮审计。
