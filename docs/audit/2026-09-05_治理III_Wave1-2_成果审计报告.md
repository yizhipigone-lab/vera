# 治理 III Wave 1-2 工作成果审计报告

**审计日期**: 2026-09-05
**审计对象**: 治理 III 增补计划书（[docs/plan/2026-09-05_深模块浅模块治理III_增补计划书.md](../plan/2026-09-05_深模块浅模块治理III_增补计划书.md)）Wave 1（止血包）+ Wave 2（实盘正确性三件套）落地成果
**审计方法**: git 提交链核查 + AST 纯搬移等价比对 + 验收 grep + 定向/全量测试复跑 + 关键文件实读
**审计员立场**: 严苛挑刺、逐项验证、不采信"自己说完成"

---

## 一、结论先行

**Wave 1 + Wave 2 达标，无阻断缺陷，可进入 Wave 3。** 九个治理提交全部独立验证：纯移动等价（AST 级）、验收 grep、全量 `pytest tests/` 绿（退出码 0）。发现 **0 个阻断问题、1 个中风险观察、3 个低风险顺延/观察**，均记录在案（§四）。

| 波次 | 任务 | 判定 |
|---|---|---|
| W1 | 临时脚本清理闸 | ✅ 达标 |
| W1 | 牛熊口径收口 core/index_regime（200 门槛） | ✅ 达标 |
| W1 | 常量/舍入收口 | ✅ 达标（含一项顺延见 §四-1） |
| W2 | rotation 三态迁出 legacy_three_state | ✅ 达标（AST 等价 PASS） |
| W2 | 资金口径下沉 pool_money + 热更 apply 契约 | ✅ 达标 |
| W2 | 卖单登记收口 executor 共享槽 | ✅ 达标 |

---

## 二、逐项核验表（验收标准 → 证据 → 判定）

### A. 提交链与工作区（全波次）

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| A1 | 工作区干净 | `git status --short` 空 | ✅ |
| A2 | 治理提交可追溯 | 0bab26a(计划书) → 85c3ea0 → b783502 → 9591a63 → fa436bb → 509f4e5 → 05bafcb → 2722b3d → b2adae1，9 个治理提交 + 9 组在途分提交，提交信息含 task 标签 | ✅ |
| A3 | 根目录无垃圾 | glob `^_tmp_|\.log$|^optimization_results` = 0 | ✅ |

### B. Wave 1

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| B1 | .gitignore 防堆积规则 | 含 `/_tmp_*`、`scratch/`、雷达 db/DSH 截图/WorkBuddy 记忆 | ✅ |
| B2 | 牛熊口径单一真相源 | `core/index_regime.py` 常量 MA250/MIN_PERIODS=200/SLOPE_SPAN=20（测试锚定 7 绿）；data_tools.market_health 内联数学已删（grep 仅注释残留，实算委托 classify）；research/index_regime 委托 regime_series | ✅ |
| B3 | 用户拍板 200 落卡 | 计划书 §二 + 模块 docstring + `tests/test_index_regime.py::test_constants_anchored` | ✅ |
| B4 | DIRECTION_BUY 唯一源 | 全库 `DIRECTION_BUY = 23` 只剩 `trade/book.py:42` | ✅ |
| B5 | round_price 唯一源 | `def round_price` 只剩 `trade/book.py`（ETF 版顺延，§四-1） | ✅（有顺延） |
| B6 | 注册文件清理 | 18 个垃圾删/移；PS1 助手入库 tools/；`_feishu_preview.txt` 从跟踪移除 | ✅ |

### C. Wave 2 — ④ rotation 迁出

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| C1 | 纯移动等价 | **AST 逐节点比对**旧 `509f4e5^:trade/rotation.py` vs `legacy_three_state.py`：STATE_FULL_CYB/HALF/GOLD、STATE_RATIOS、compute_signal、_derive_state、target_values 七节点全部一致，legacy 无多余顶层符号 → **PASS** | ✅ |
| C2 | rotation 无 deprecated | grep `def compute_signal|def _derive_state|def target_values|STATE_RATIOS\s*=|STATE_FULL_` 空；行数 811→718 | ✅ |
| C3 | shadow 循环破除 | shadow.py 顶层 `from trade.legacy_three_state import compute_signal`，无惰性 import；legacy 零依赖无环 | ✅ |
| C4 | research/工具/测试改指 | research×10、tools/shadow_compare、tests/test_shadow 全部编译/通过 | ✅ |

### D. Wave 2 — ① 资金口径 + apply 契约

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| D1 | pool_money 纯函数层 | pool_split/position_value/stock_pool_value/stock_budget_cap/in_flight_sell_returns 五函数全依赖注入、零 IO | ✅ |
| D2 | 8-21 现场进单测 | `test_pool_money` 13 个含 trades 空/orders 领先→取 orders、双回报一致不重复、跨日过滤、买入忽略 | ✅ |
| D3 | 语义保持 | test_rotation 8-21 daily_loss 集成场景原断言不变绿（orders 领先 534778+补回公式）；disabled/预算帽数字断言不变仅改方法名 | ✅ |
| D4 | trade_main 三件套删除 | grep 三个 `def` 空；1278 行（1314 基线 −36） | ✅ |
| D5 | rotation._etf_value 消灭 D1 | 委托 pool_money.position_value（同一 quote 优先/成本回退口径） | ✅ |
| D6 | 热更 apply 契约 | monitor/executor/risk + trade_main._DailyTimer 四模块各有 apply；_apply_config 只调 apply 不再直改私有字段；test_api config_update 审计路径绿（全量回归兜底） | ✅ |
| D7 | risk.apply 字段正确 | `_loss_limit = cfg.daily_loss_limit`、`_sizing = cfg.position_sizing`（原热更语义逐字段保留） | ✅ |

### E. Wave 2 — ⑤ 卖单登记收口

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| E1 | rotation 无独立登记 | grep `_pending_sells` 代码引用空（仅注释提及历史）；rotation.in_flight_sells 已删 | ✅ |
| E2 | 同接口共享登记 | rotation 卖单改走 `executor.register_external_sell`；起手 `clear_external_sells`（原 `_pending_sells.clear()` 语义同点） | ✅ |
| E3 | 组合根单源 | trade_main 对账 in_flight_sells lambda = `self.executor.in_flight_sells()` 单源（不再 `{**executor, **rotation}` 手拼） | ✅ |
| E4 | 防双卖 | external_sells 不进 `_pending` 操作链（避 pending_check 追价/逃生误触发） | ✅ |
| E5 | executor 暴露合并 | in_flight_sells = `{**_pending, **_external_sells}`；既有 test_executor in_flight 断言不变绿 | ✅ |

### F. 回归

| # | 验收项 | 证据 | 判定 |
|---|---|---|---|
| F1 | 全量测试 | `pytest tests/` 退出码 0，无 FAILED/ERROR | ✅ |
| F2 | 定向复跑 | 本审计复跑 test_index_regime(7)、test_pool_money(13)、trade 子集、py_compile 全过 | ✅ |

---

## 三、验证手段说明（诚实披露）

- **等价性**：采用 AST dump 逐节点比对（结构化，忽略注释/空白差异），比"肉眼 diff"强、比"逐字节 diff"合理（搬移时允许 docstring/注释重写）。**局限**：比对的是顶层节点文本；模块 import 移除与 docstring 修改不在比对范围（已人工核对无逻辑引用遗留）。
- **热更行为**：无独立"apply 契约"单测，靠 test_api config_update（写 audit 行）+ 全量回归间接兜底（见 §四-4）。
- **市值口径**：pool_money 单测 + rotation 集成测试双覆盖；D1 消灭以"rotation._etf_value 委托 + grep 无第二份实现"为据。

---

## 四、缺陷与观察清单（按严重度）

1. **[低] round_price_etf 未按原计划迁入 book.py**（W1-c 声明的随 W2 迁入）。实际动 rotation 时文件改动面已大，为避免一个提交混两类改动而顺延。已记 CHANGELOG Wave 2 剩余风险。**处理**：Wave 3 顺手完成（纯搬家，book.py 加函数 + rotation import + 删本地 def + 迁移 test_rotation 的 import 断言）。
2. **[低] legacy_three_state 无独立单测**：函数在生产已弃用，历史研究脚本 import 为主；本次审计以"AST 等价 + 全量测试绿 + research 脚本 py_compile"三重兜底。若未来 research 想固化旧三态行为，可补对照单测（非本次范围）。
3. **[低] executor._external_sells 无订单终态自动回收**：轮动卖出成交后到下一次调仓起手 clear 前仍保留（跨信号窗口）。**还原自旧 `_pending_sells` 语义，行为无变化**——风险敞口与既有 M7（executor._pending 跨日泄漏）同源，已在案接受。建议后续 reconcile 侧按日清扫时一并评估（另立任务，非本波次）。
4. **[观察] 热更 apply 契约缺"禁止直改私有字段"的回归护栏**：现无测试阻止未来代码退回直改。低优先，可选在 test_api 增一条"热更后 4 模块 cfg 引用与私有标量已更新且非同一对象"断言。
5. **[观察] research/index_regime min_periods 60→200 只影响重跑产物**：已存 parquet 不受影响；重跑则 2019 年初约 10 个月标签从"有值"变 range（口径统一，符合用户拍板）。研究文档如需与重跑产物一致须同步，已提醒。

---

## 五、与治理 II/III 计划对照

- 治理 II 13 项现状：P1-1 变体完成（09-02 view_calc）+ 本日 Wave2 消化 P0-2/3/4 三项 → **治理 II P0 档全部落地**；P1 余 4 项 + P2 余 5 项 = 下一阶段 Wave 3/4。
- 治理 III 四波：Wave1 ✅、Wave2 ✅；Wave 3（接口债 4 项 + round_price_etf 顺延项）、Wave 4（结构项，条件触发）待做。
- CHANGELOG 两条（Wave1/Wave2）与 git 提交、验收 grep 三方一致（交叉核验无漂移）。

## 六、审计结论

**建议：放行进入 Wave 3。** 阻断级/高危问题：0。遗留均为低风险顺延或"行为不变"的观察项，全部有案可查、有明确处置路径。
